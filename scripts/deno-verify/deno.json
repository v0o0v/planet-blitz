{
  "//": "M4 선행 스파이크 — 시뮬 코어 Deno 결정론 검증. sloppy-imports(unstable)로 소스의 .js specifier를 .ts에 resolve하여 src/sim·src/items 무수정 import. 실행: deno task verify",
  "unstable": ["sloppy-imports"],
  "lock": false,
  "tasks": {
    "verify": "deno run --allow-read verify.ts",
    "verify-run": "deno run --allow-read verifyRun.ts",
    "verify-invasion": "deno run --allow-read verifyInvasion.ts"
  }
}
