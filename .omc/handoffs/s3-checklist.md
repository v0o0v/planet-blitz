# S3 체크리스트 — 다음 세션의 재개 지점

**현재 main: `<배치5 머지 후 sha>`** (배치5 머지 완료) · **배선 176 / 210 (계측으로 확정)** · **앵커 43개**

| 기체 | 배선 | | 기체 | 배선 |
|---|---|---|---|---|
| **아크캐스터** | **30** / 30 ✅ | | 브루저 | **27** / 30 |
| **말로우** | **30** / 30 ✅ | | 팬텀 | **22** / 30 |
| 해츨링 | **26** / 30 | | 버블 | **21** / 30 |
| | | | 스트라이커 | **20** / 30 |
| | | | **합계** | **176 / 210** |

⭐ **배치5 에서 아크캐스터·말로우가 30/30 으로 완주했다** — 처음으로 기체가 닫혔다.

**앵커는 40 → 43개다.** `skillHooks.ts` 에 41개 · `chainHooks.ts` 에 2개.
배치5 가 더한 셋은 **벽 축**이다: `onWallHit`(탄↔벽 겹침 직후 · `w.hp` 감산 **앞**) ·
`onWallDestroyed`(`wall.dead = true` 직후) · `onWallShockResolve`(투사체 순회 **밖** — 지연 스폰).
⚠️ **세 자리인 것이 계약이다** — BL7 은 감산 앞, MO7 은 파괴 뒤, BL7 충격파는 순회 밖이라야
스폰이 안전하다. 하나로 합치면 셋 중 둘이 원리적으로 죽는다(사유 전문은 `skillHooks.ts`).
⛔ **동그라미 번호(①②③…)는 ㉖ 에서 끝났다 — 새 앵커에 번호를 붙이지 마라.** 배치4 에서
네 레인이 병렬로 앵커를 세우며 **㉗㉘ 가 세 갈래로 중복**됐고(공유·해츨링·말로우) **git 은
그 충돌을 전혀 몰랐다**. 앵커의 정본 이름은 **함수 이름**이고, 기계가 검사하는 레지스트리는
`tests/skillAnchors.test.ts` 의 **export 전수 표**다. 사유 전문은 `skillHooks.ts` 헤더.

이 수는 **`tests/skillWiringCensus.test.ts` 가 매 실행마다 다시 센다** — 배선이 늘거나 줄면
골든 표와 어긋나 테스트가 깨지고, **어떤 스킬이 늘고 줄었는지**를 실패 메시지가 찍는다.
⚠️ **액티브 42종(6×7)은 이 210 밖이다** — `data/ships/actives/` 의 문자열 ID 레지스트리이고
`activeHandlers/` 에 `Sk.` 참조가 0건이다. **두 수를 더하지 마라.**
⚠️ **이 계측이 증명하지 않는 것**: "enum 을 읽는다 ≠ 효과가 발동한다"이고 **반쪽 배선도 1종으로
세어진다.** 배선의 진짜 물증은 레인별 **뮤테이션 단위 테스트**다.

정본 인계는 `.omc/handoffs/skill-catalyst-merged-lane.md` §11. 아래 §「다음 할 일」만 읽어도
재개할 수 있다 — 나머지는 **왜 그렇게 됐는지의 근거**다.

---

## 🎯 다음 할 일 (2026-08-07 배치5 종료 시점)

### 남은 34종 — **기체별 전수**

| 기체 | 남은 것 |
|---|---|
| 스트라이커 (10) | F7 · F10 · S5 · S7 · S9 · M2 · M4 · M7 · M8 · M9 |
| 버블 (9) | PO4 · PO8 · PO10 · DR2 · DR3 · DR7 · DR8 · FI6 · FI7 |
| 팬텀 (8) | AS1 · AS6 · AS7 · AS8 · PH5 · PH9 · DI9 · DI10 |
| 해츨링 (4) | BD4 · NU3 · SH4 · SH8 |
| 브루저 (3) | BL1 · FO3 · FO10 |
| 아크캐스터 · 말로우 | **없음 — 30/30 완주** |

### ⭐⭐ 배치5 가 확정한 것 — **남은 것은 「얹기」가 아니다**

배치4 가 세운 공유 앵커 셋에 소비처를 얹는 일은 **끝났다**(25종 후보 중 얹을 수 있는 것은
전부 얹었다). 남은 34종은 **전부 새 지점을 요구한다.** 배치5 네 레인이 서로를 모른 채 같은
사유를 냈고, 부류로 묶으면 다음 배치가 열어야 할 자리가 바로 보인다:

| 막는 자리 | 걸린 스킬 | 실측 근거 |
|---|---|---|
| **액티브 「만료」 앵커 0건** | 브루저 FO10 · 버블 DR3(대체 셋 다 막힘) | `ACTIVE_EXPIRE` 에 훅 호출 0건. `onActiveFired` 는 **발동** 직후라 만료를 원리적으로 못 본다 |
| **젬 흡인 루프 「안」** | 스트라이커 M4 · 브루저 MO2 절반 | `onGemMagnetParams` 는 루프 **밖**에서 틱당 1회 · 스칼라 `radius` 만 넘긴다. 젬 개체별 판정(비등방·출처)이 불가 |
| **기믹 픽업 접촉 반경** | 버블 DR8 | 정본이 `world.ts:4432` 의 지역 변수 `pickR = player.radius`. `stepGems` 가 안 쥔다 |
| **파열 산술 내부** | 버블 PO4 · PO8 · FI7 · PO9 절반 | `resolveFilmBurst` 안. pre/post 훅 분리가 선결이고 `skills/bubble.ts → filmBurst.ts` 는 순환이다 |
| **플레이어↔벽 충돌** | 팬텀 **DI9** | ⚠️ 아래 별도 절 |
| **자동 조준의 차폐 판정** | 팬텀 AS10 의 **체감 범위** | `segmentBlocked`(벽 너머 적을 후보에서 제외)에 앵커가 없다. AS10 은 배선됐으나 탄만 통과하고 조준은 여전히 벽 너머를 안 고른다 |
| **신규 WorldState 정수 + 해시 꼬리 폴드** | 버블 DR2 | ⚠️ 아래 별도 절 |
| 대시 방향 정본이 지역 변수 | 스트라이커 M2 | `resolveDirFallback(mx,my,angle)` 의 `mx/my` 가 `stepPlayer` 지역. `onPlayerMoveParams` 는 그것도 「발동 틱」도 안 넘긴다 |
| 적 이동 정지 상태이상 부재 | 스트라이커 S9 | `stun\|freeze` 25건은 전부 연출 |
| **「활성」 리더 부재**(에코·조우) | 스트라이커 M7 · 팬텀 PH9 | 종료 판정만 export |
| 분열·소환 분기 | 팬텀 AS6 | 앵커 ⑪ 은 파편이 이미 태어난 뒤 |
| 접촉 상대 적을 넘기는 앵커 부재 | 브루저 FO3 | |

**→ 다음 배치의 1순위는 「액티브 만료 앵커」와 「젬 흡인 루프 안 앵커」다.** 그 둘이 각각
2종·2.5종이라 회수는 배치4 의 공유 앵커 셋보다 작다 — **남은 34종은 배선 단가가 올랐다.**
한 배치에 25종이 착지하던 국면은 끝났다고 보는 편이 정확하다.

### ⛔ DR2 는 **일부러 멈춘 것**이다 — 해시 꼬리 폴드를 건드리지 마라
DR2「표면장력 세례」의 술어(막 있음 + 젬 수거 60틱 창)에 카운터가 필요한데 기존 필드에
얹을 자리가 없다. **신규 `WorldState` 정수를 해시 꼬리 폴드에 넣으면 항상 0 인 필드라도
지금 초록인 골든까지 전부 갈린다.** 배치5 는 그래서 배선하지 않았다. D 단계 골든 재동결과
**같은 커밋**에 묶어서 하는 것이 옳다.

### ⛔ DI9 는 벽 파괴 축이 **아니다** — 배치5 가 grep 으로 확정했다
배치4 인계와 배치5 프롬프트가 DI9「유령 선체」를 브루저 BL7·MO7 · 팬텀 AS10 과 같은
「벽 파괴 앵커」 축에 묶어 뒀는데 **틀렸다.** 문면이 *"피격 무적 동안 **선체**가 벽을
통과한다"* 라 탄↔벽이 아니라 **플레이어↔벽** 경로다:
- 선체↔벽 정본 = `stepPlayer` 안의 `slideCircleWalls`(`world.ts:2306`·`2351`) +
  `modes/blockBreak.ts:195` 의 `isPinnedByWall`. `player.radius` 로 **원을 밀어낸다.**
- 탄↔벽 정본 = `sweptCircleOverlapsWall` → `w.hp -= e.damage`(`world.ts:3931`). **탄을 죽인다.**
- **함수도 술어도 겹치지 않는다.** 벽 축 앵커 셋이 섰어도 DI9 는 거기서 돌지 않는다.

DI9 의 자리는 `slideCircleWalls` 호출을 **건너뛰는** 분기이고 그건 별도 앵커 신설이다.

### ⚠️ 배치5 가 남긴 정정 — 다음 프롬프트가 복사하지 마라
1. **`void Sk.XX` 를 「미배선 표시」로 쓰지 마라.** `skillWiringCensus` 의 `extractWired` 가
   `void Sk.x` 를 배선에서 빼고, 그 멤버는 곧바로 **「유령 선언」 게이트**(`unread`)에 걸려
   테스트가 빨개진다. 미배선은 **enum 멤버를 아예 만들지 말고** 사유를 산문 주석으로 남겨라.
2. **DR5 의 앵커는 `onGemMagnetParams` 다**(배치4 인계 표의 `onPlayerMoveParams` 는 오기).
   문면이 *"콤보 스택이 **자석 반경**에 곱해진다"*(`data/ships/bubble.ts:51`).
3. **격리 워크트리 레인은 리드 브랜치가 아니라 `main` 에서 갈라진다.** 배치5 에서 팬텀 레인이
   그 때문에 벽 축 앵커를 못 보고 AS10 을 미배선으로 남겼다(리드가 통합에서 얹었다).
   ⭐ 레인 프롬프트에 **"앵커 커밋 `<sha>` 를 먼저 `git merge` 로 당겨라"** 를 명시해라.

### ⭐ 병렬 머지 — 배치5 실측
fallthrough 함정이 **3레인 머지에서 3번 전부** 재현됐다(누적 8건). 앞 레인의 `case` 와 뒤 레인의
`case` 가 `break;` 하나를 공유하는 형태로 자동 병합된다. **해소는 Edit 툴로** 하고, 머지마다
`case` 수 + 1 == `break` 수를 앵커별로 세어라(`sed -n '/^export function <앵커>/,/^}/p'`).

<details>
<summary>▼ 이력 — 배치3·4 가 확정한 것 (근거로만 남긴다)</summary>

### ⭐⭐ 배치3 이 확정한 것 — **병목은 「배선 노동」이 아니라 「앵커 부재」다**

배치3 은 네 기체(버블·브루저·스트라이커·팬텀)에 **격리 워크트리 4레인**을 띄웠다. 남은 후보는
60종이었고 **배선된 것은 11종**이다. 그리고 **네 레인이 서로를 모른 채 같은 보고를 냈다** —
미배선 49종의 사유가 거의 전부 *"소비 지점이 앵커 26개 밖"* 이다. 부류로 묶으면:

| 막는 자리 | 걸린 스킬 | 비고 |
|---|---|---|
| **액티브 핸들러에 앵커 0건** | 브루저 BL5·BL10·MO5·MO10·FO10 · 버블 PO9·DR3·DR9 · 팬텀 PH2 | `grep -rn "skillHooks" src/sim/activeHandlers/` → **0건**. 단일 최대 부류 |
| `stepPlayer` 이동 배율·감속 적용부 | 브루저 MO3 · 스트라이커 M10 · 버블 DR4·DR5 · 팬텀 PH4 | `world.ts:2207-2222` |
| `stepGems` 자성·흡인 | 브루저 MO2 · 스트라이커 M4 · 버블 DR8·DR10 | `world.ts:3894-3897` |
| 벽 파괴·차폐 판정 | 브루저 BL7·MO7 · 팬텀 AS10·DI9 | `world.ts:3856` |
| 파열 산술 **내부**(pre/post 분리 필요) | 버블 PO4·PO8·FI7 | `resolveFilmBurst` |
| 분열·소환 분기 | 팬텀 AS6 | 앵커 ⑪ 은 파편이 이미 태어난 뒤 |
| **「활성」 리더 부재**(에코·조우) | 스트라이커 M7 · 팬텀 PH9 | 종료 판정만 export |
| 적 이동 정지 상태이상 부재 | 스트라이커 S9 | `stun\|freeze` 25건은 전부 연출 |

**→ 다음 배치는 「배선」이 아니라 「앵커 신설」이어야 한다.** 위 표의 위 세 줄만 열어도
**20종 이상**이 한꺼번에 풀린다. 특히 **액티브 핸들러 앵커**가 투자 대비 회수가 가장 크다.

### ⭐⭐ 배치4 가 앵커를 세웠다 — **다음 배치는 「얹기만」 하면 된다 (25종)**

배치4 가 **공유 앵커 셋**을 세우고 각각에 **증명 소비처 1종만** 배선했다(앵커가 죽지 않았음의
물증). **나머지 소비처 25종은 일부러 안 얹었다** — 같은 앵커를 여러 레인이 동시에 소비하면
시그니처가 흔들리기 때문이다. 앵커가 main 에 있으니 **이제는 그냥 얹으면 된다.**

| 앵커 | 자리 | **남은 소비처** |
|---|---|---|
| `onActiveFired` | `actives.ts`(핸들러 호출 **직후**) | **13종** — 브루저 BL5·BL10·MO5·MO10·FO10 · 버블 PO9·DR3·DR9 · 아크캐스터 CH7·CH10·BA1·BA4·BA6 |
| `onGemMagnetParams` | `world.ts` `stepGems` | **6종** — 해츨링 NU1 · 아크캐스터 BA2 · 브루저 MO2 · 스트라이커 M4 · 버블 DR8·DR10 |
| `onPlayerMoveParams` | `world.ts` `stepPlayer` | **6종** — 말로우 ME3 · 브루저 MO3 · 스트라이커 M10 · 버블 DR4·DR5 · 팬텀 PH4 |

**`onActiveFired` 를 쓸 때 알아야 할 것 (앵커 doc 에도 있다)**
- **착지점 인자가 없다** — 앵커가 핸들러 **뒤**라 `player.x/y` 가 **이미 착지점**이다.
  *출발* 지점이 필요하면 `origin.preX/preY` 를 써라(버블 DR9·아크캐스터 BA6 가 그렇다).
- `origin.preAux0` = 호출 전 `player.aux0`(아크캐스터 CH7 의 "소모한 정지 시간" 차분).
- `origin.spawnWatermark` = 호출 전 `state.entities.length`(아크캐스터 CH10 이 *그 발동이 낳은
  탄만* 고르는 용도).
- ⭐ **이 앵커는 엔티티 순회 밖이라 스폰이 안전하다**(BA1 원형 볼리·BA6 포탑이 여기서 가능).
  앵커 ⑥ 이 순회 **안**이라 스폰 금지인 것과 대비된다.
- `onGemMagnetParams` 의 **`broodRadius` 는 아직 소비처가 없다**(해츨링 NU1 이 얹힐 자리). `stepGems` 가 읽지도
  않는다 — **필수 필드를 나중에 더하면 다른 레인 픽스처가 `Partial` 스프레드로 깨지므로**
  미리 잡아 둔 것이다(배치1 에서 실제로 났다).

### 바로 할 수 있는 것
1. **⭐ 위 표의 25종을 얹어라** — 앵커가 이미 서 있어 배선 노동만 남았다. **다음 배치 1순위.**
2. **버블 DR2** — 술어(막 있음 + 젬 수거 60틱 창)에 신규 WorldState 정수 1개가 필요하다.
3. **스트라이커 M2 · 말로우 SQ7 계열** — "대시 방향" 정본이 `resolveDirFallback(mx,my,angle)` 인데
   `mx/my` 가 `stepPlayer` 지역 변수다. `player.vx/vy` 는 이동 성분 합산 뒤라 **방향 규칙의 두 번째
   사본**이 된다. ㉙ 이 열어 준 문으로 풀리는지 확인해 볼 값이 있다.
4. **브루저 MO4 의 「0틱 무효화」** — ㉙ 이 그 문을 열었다(`PlayerMoveParams.slowTicks`).
   배치3 이 "한 틱 늦다"로 남긴 건이다.

### ⛔ 앵커로는 안 풀리는 것 — **모델·루프 신설이다** (정찰 실측)
- **해츨링 NU3** — **대시 도착 좌표가 존재하지 않는다**(`world.ts:2222-2234` 는 속도 임펄스만
  싣는다). 앵커가 아니라 **대시 모델이 경로 종점을 산출**해야 한다.
- **해츨링 SH8 · SH4 절반** — **적탄↔병아리 충돌 경로가 코드에 0건**이다(`world.ts` 의 enemyBullet
  처리는 플레이어 충돌 4364 뿐, `collision.ts`/`bullets.ts` 에 포탑 대상 판정 grep 0건).
  **새 충돌 루프** 신설이다.
- **버블 PO4·PO8·FI7** — `resolveFilmBurst` 산술 **내부**(pre/post 훅 분리가 선결).
- **벽 파괴 축**(브루저 BL7·MO7 · 팬텀 AS10·DI9) — `world.ts:3856` 에 앵커가 필요하다. 이건
  앵커 신설로 풀리니 다음다음 배치 후보다.
  ⚠️ **배치5 결과**: BL7·MO7·AS10 은 벽 축 앵커 셋으로 배선됐고, **DI9 는 이 축이 아니었다**
  (위 §「DI9 는 벽 파괴 축이 아니다」가 정정본).

</details>

### ⛔⛔ 사전 적색 2건 — **네 것이 아니고, 다시 조사하지도 마라** (2026-08-08 종결)

`pnpm verify` 의 실패 **2건**(`pilotFrameFreeze` · `shipSignaturePhantom`)은 **뿌리가 같고**
**둘 다 판정이 끝났다.** ⚠️ **최소 네 세션이 각자 다시 조사했다** — 결론은 이미
`f57fb20`(2026-08-06) 이 `.omc/handoffs/skill-catalyst-merged-lane.md` **605-680줄**에 남겼는데
체크리스트 머리에도 MEMORY 에도 표지가 없어 전파되지 않았다. **그래서 여기 못박는다.**

**공통 근인: 커밋 `d7445a4`(PR#329)** — 2026-08-06 **사용자 승인 C-3**. 팬텀 은신 배율 토큰
발화 지점을 진입 에지 → **소진(종료) 에지**로 이관(`fireCloakEntry` → `cloakExitCrossed` 신설,
`src/sim/cloak.ts`). P1 실측 360런·1,328,935틱이 옛 설계 전제를 반증해서 고친 것이고,
**커밋 본문이 해시 영향을 자인**한다(침공 aux1 · C-3 토큰 시점 · 병아리 탄 ownerId 3축).

**이분 탐색 실측** (`git bisect`, 워크트리 5개):
`73ddf4d` PASS → `e574fcf` PASS → **`ef8220a`(PR#329) FAIL — 첫 발산** → `79d1232` 5칸 →
HEAD 16칸. ⭐ **배선 배치가 착지할 때마다 발산 칸이 단조 증가한다** — 단일 회귀가 아니라
**다중 의도적 변경의 예상된 누적**이다. 인계 605-680줄이 이걸 사전에 예고했다
(*"스트라이커 하나만 배선해도 깨지고, 7기체가 다 들어오면 18칸이 전부 갈린다"*).

- **`shipSignaturePhantom`**: 증인 시드 238 노후화로 은신 진입 4 → 3. **배선 결함이 아니다** —
  억제 대조군(`ctrl.cloakEnters === 0`)·`kills > 0`·해시 다양성 단언이 **전부 그린**이고 런은
  완주한다(`DURABLE_HP = 100,000,000`). **`PLAYER_DAMAGE_TAKEN_MULT` 2배는 이 계측과 인과 무관**
  이다(히트 *크기*만 바꾸고 *빈도*는 아니다 — 이 오해를 두 세션이 했다).
- **`pilotFrameFreeze`**: 18칸 중 16칸 발산. 같은 근인 + 배선 누적.

⛔ **둘 다 「A = 재동결이 맞다」. 단 지금 재동결하지 마라.** 원 저자 지시:
*"단언을 3으로 낮추지 마라. **210스킬 배선이 착지한 뒤** 재선정한다."* 지금 하면 다음 배선
커밋이 즉시 또 깨뜨린다. **D 단계(배선 완결 후 1회)에 일괄**로 이미 계획돼 있다.

**고치는 법은 알려져 있다** — 형제 `shipSignatureWiring` 은 증인 시드를 **113 으로 재선정**해
같은 사건을 해소했다. `shipSignaturePhantom` 도 자기 재선정 이력(48 → 84 → 156 → 238)을 적고 있다.

**⚠️ 이 판정의 유일한 약한 고리** — 이 두 골든은 **현재 사실상 무력화된 게이트**다. 어떤 발산이든
"배선 때문"으로 설명 가능해 보이므로, **그 안에 숨은 별도 회귀가 있어도 지금은 구분이 안 된다.**
그래서 배선 검증을 이 골든에 기대면 안 된다 — **물증은 레인별 뮤테이션 단위 테스트다.**

**착수 조건: 배선이 210/210 에 도달했을 때.** 그 전에는 손대지 마라.

### ⛔ 막힌 것 — **사용자 판단이 필요하다**
4. **촉매 48종** — 선결 미완으로 **착수 불가**. 완성된 3종이 브랜치에 파킹돼 있다.
   상세는 §「촉매 48종 배선」. **혼자 밀어붙이지 마라.**

### ⚖️ 일부러 안 한 것 — **판단과 근거**
5. **D 단계 골든 재생성을 하지 않았다.** 리포 규약이 *"골든 재생성은 검증이 아니다 —
   근거는 재생성 **전에** 통과시킨 불변식이어야 한다"* 이고, 이번 세션이 **골든이 실제로는
   「무투자에 가까운 스트라이커 런」만 잰다**는 것을 수치로 확정했다(아래 두 §). 지금 재생성하면
   **검증되지 않은 32건의 발산을 통째로 축복**하는 것이 되고 되돌릴 근거가 사라진다.
   → 재생성은 **밸런스가 확정되고 사용자가 승인할 때** 한 번에. 그때도 근거는 단위 테스트다.

---

## ⭐⭐ 배치4 — **앵커 9종 신설**이 본체다 (125 → 152)

배치3 이 *"병목은 앵커 부재"* 라는 부류 지도를 남겼고, 배치4 는 **정찰 → 앵커 → 배선** 순으로
그걸 실행했다. 정찰 2레인(말로우·해츨링 / 아크캐스터)이 **미배선 38종의 필요 앵커를 실측**한 뒤
앵커 레인 4개가 돌았다. 앵커 **26 → 40개**.

| 레인 | 신설 앵커 | 배선 |
|---|---|---|
| 공유(단독 선행) | `onActiveFired` · `onGemMagnetParams` · `onPlayerMoveParams` | 3종(앵커당 증명 1종) |
| 아크캐스터 | `onChainParams`(chainHooks) · `onBulletHitParams` · `onEliteLootRarity` · `onOverchargeAccrual` · `onComboDecay` | 6종 → **24/30** |
| 해츨링 | `onTurretCadence` · `onTurretExpired` | 8종 → **25/30** |
| 말로우 | `onCushionSplit` · `onCushionRecoverBp` · `onObjectiveResolved` · `onEnemyStatusExpired`(chainHooks) | 10종 → **29/30** |

### ⛔ 앵커 번호 체계를 **폐지했다** — 재배번이 아니라 폐지인 이유
배치4 한 번에 **㉗㉘ 가 세 갈래로 겹쳤다**(공유 `onActiveFired`/`onGemMagnetParams` ·
해츨링 `onTurretCadence`/`onTurretExpired` · 말로우 `onCushionSplit`/`onCushionRecoverBp`).
리드가 두 배치를 재배번으로 버텼지만 **레인이 늘수록 비용이 커지고, 무엇보다 번호는 기계가
검사하지 않는다.** 전수 재배번은 doc 주석 50여 곳을 손대며 오라벨 위험이 있는 데 비해 얻는 것이
없다 — 그래서 **㉖ 에서 끊고 이름을 정본으로** 삼았다. ①~㉖ 은 중복이 없으니 이력으로 남긴다.
**기계 검사 레지스트리는 `tests/skillAnchors.test.ts` 의 export 전수 표**이고 이름 기반이다.

### ⭐ 왜 공유 앵커를 **단독으로 먼저** 돌렸는가 (다음 배치도 이렇게 해라)
㉗㉘㉙ 은 **7기체가 전부 지나가는 지점**이다. 기체별 레인이 각자 뚫으면 같은 자리에 시그니처가
다른 훅이 여럿 서고 **그 충돌은 `tsc` 만이 잡는다**. 그래서 **자리를 먼저 하나로 세우고
소비처를 나중에 얹는** 순서로 재발을 구조적으로 막았다. 각 앵커에 **증명 소비처 1종만** 배선한
것도 같은 이유다 — 나머지 25종은 §「다음 할 일」의 표대로 다음 배치가 얹는다.

### ⛔⛔ 이 배치가 잡은 결함 — **두 레인이 같은 순환에 부딪혀 판단이 갈렸다**
`status.ts → skillHooks.ts → skills/<기체>.ts → status.ts` **런타임 순환**.
- **아크캐스터 레인은 피했다** — `src/sim/chainHooks.ts` 라는 별도 leaf 를 만들고 CH2 본체도
  `src/sim/skills/arccasterChain.ts` 로 빼서 `FILE_SHIP` 표에 등록했다(`phantomEntry.ts` 선례).
- **말로우 레인은 만들었다** — *"함수 선언 호이스팅으로 성립하고 build·테스트 무경고"* 라고
  스스로 관찰하고 넘어갔다. 리드가 되돌려보내 `chainHooks.ts` 로 이관시켰다.

⚠️ **`skillHooks.ts` 헤더 23-28줄이 이 순환을 명시적으로 금지한다**: *"번들러가 모듈 초기화를
재배치하면 TDZ — **클라에서 재현 안 되고 검증 EF 에서만 터진다**."*
⭐ **교훈: `build` 통과·테스트 무경고는 이 결함의 부재를 증명하지 않는다.**
`status.ts` 가 부르는 앵커의 정본 자리는 **`chainHooks.ts`** 다. 다음 레인도 거기 넣어라.

### 레인이 「안 한다」고 판단한 것 — 전부 옳았다 (뒤집으려면 근거가 필요하다)
- **해츨링: `fireTurretShot` export 거부.** 하면 핸들러 → `world.ts` 런타임 import 로 모듈
  순환이 된다(현재 `src/sim/**` 에 world 런타임 import **0건**). 대신 **`stepActives`(1833)가
  `stepTurrets`(1917)보다 앞**이라는 순서를 이용해 핸들러가 `cooldown = 0` 만 써서 **같은 틱
  격발**을 얻었다 — BD8 은 그 경로로 배선됐다.
- **해츨링: SH4 절반 거부.** 반대급부(적탄 차폐)가 구조적으로 없어 「사격 정지」만 넣으면
  **순손해 스킬**이다. BD10 이 탄 피해 축 없이 상한만 깎였을 때와 같은 형태이고 그때도 통째
  미배선이 정답이었다. `suppressFire` 칸도 열지 않았다(㉖ doc 의 *"증명한 칸만 연다"*).
- **해츨링 BD4** — 「플레이어 자동 조준 표적」이 `world.ts:2966` 볼리 경로의 **지역 변수**이고
  어디에도 저장되지 않는다. ㉖ 에 `target` 을 실으려면 `nearestTarget`(LOS+가중치)을 옮겨
  적거나 `WorldState` 에 표적 이월 칸을 신설해야 한다 — 전자는 이 리포의 대표 결함, 후자는
  앵커가 아니라 모델 변경이다.

### 이 배치의 설계 판단 (뒤집으려면 근거가 필요하다)
- **아크캐스터 CH5**: `bullet.targetX` 에 **발사 좌표가 아니라 발사 시점 잔여 수명**을 새겼다 —
  좌표에는 *"각인됐는가"* 표식이 없어 미각인 탄이 **원점 발사로 오독**된다.
  ⚠️ **`targetX/targetY` 는 「전역 자유」가 아니다**(리드 직접 검증): `bullets.ts:64`
  `applyBehavior` 가 **적탄**에 거동 파라미터 A/B 로 쓰고 `boss.ts:184·211` 이 나선 기준각으로
  쓴다. **아군탄에서만 비어 있다** → 게이트(`bullet.kind !== 'bullet'`)와 **그 게이트를
  증명하는 부정 단언 + 긍정 짝**을 붙였다.
- **아크캐스터 BA8**: 「감속 장판 *위에서* 정지」를 `playerSlowTicks > 0` 으로 **근사**했다
  (선례 `skills/bruiser.ts:421`). 그리고 「용암 피해 경감」 절반은 정지·장판 술어를 **다시 걸지
  않고** 출처(`DamageSource.hazard`)만 본다 — **문면대로 걸면 사실상 영구 미발동**이다
  (용암은 `HAZARD_SLOW` 가 아니다).
- **해츨링 ㉟ 은 `killChick`(SH1·SH7)과 같은 헬퍼 `chickDespawned` 를 부른다** → 「소멸 경로
  전수」가 세 경로 전부에서 닫혔고, SH9 이 `aux1` 사유 코드의 **첫 독자**가 됐다.
- **말로우 CU1·CU2·CU5·CU6 은 `params.deferred` 단일 손잡이**를 순서대로 지난다
  (CU5 비율 → CU1 초과분 → CU2 한도 → CU6 전액).

### 보고된 설계-코드 불일치 (문서는 고치지 않았다)
1. **말로우 ME5 「현재율」** — S3 레인 코드·주석은 *"ME8 미배선이라 0"* 이라 했지만 설계서
   합산 증명의 b 는 **절대 탕감률(기본 6000)** 이다. 설계서를 따랐고 그 결과
   **ME8 미투자 런에서도 ME5 이월 탕감이 커진다**(기존 기대값 4건 갱신).
2. **말로우 SQ10** — 「레벨 스케일」은 관통 1, 「구현(2R M1)」은 45°·pierce 2. **후자를 썼다.**
3. **말로우 SQ9** — "기본 화염 기준"의 수치가 설계서에 없다. `flaming` 어픽스 하한 2 로 잡았다.
4. **해츨링 BD9** — 문면은 "출격이 **보류 중**인 동안"이지만 그걸 글자대로 재려면 ㉓ 이 확정하는
   `threshold` 가 필요하고 훅엔 `player` 조차 안 온다 → **같은 술어 두 곳 갈림** 위험. world 가
   실제로 보류를 만드는 조건(`live >= maxDrones`)만 읽는 「만석」으로 유도했다.
5. **아크캐스터 BA5** — 「절반 속도」는 Lv1 고정값이고 레벨은 주기를 늘린다(문면에 레벨 스케일 없음).
6. **`BROOD_MAX_DRONES` 독자가 여전히 둘**(`world.ts:2647` · `skills/hatchling.ts` 지역 사본) — 미해소.

### 배치4 가 남긴 위험
- **해츨링 SH9 둥지벽은 침공 3레이어에서만 파괴 불가**(그 모드만 `wallIndex` 빠른 경로가 벽
  피해를 적용 안 한다). 막는 기능 자체는 정상이고 코드 주석에 명시돼 있다.
- **아크캐스터 CH9 의 `world.ts` 호출부는 엘리트 처치 e2e 가 없어 seam 단언이 없다**
  (효과·뮤테이션은 확인됨).
- **`src/sim/skills/` 가 파일 4개로 갈렸다**(`phantomEntry` · `arccasterChain` + 말로우 이관분) —
  `FILE_SHIP` 표 유지보수가 필요하다. **등록을 빠뜨리면 계측이 그 파일을 못 세어 배선 수가
  조용히 줄어든다.**

## ⭐ 배치3 — 4레인 병렬로 11종 (114 → 125)

| 기체 | 배선 | 어디에 |
|---|---|---|
| **브루저 4종** | MO4·FO4·FO8·FO9 | ⑨ 시그니처 스텝(감쇠 분기 **앞** 선점) · ④ 피격 · ⑧ 피해 감소 |
| **스트라이커 3종** | F8·S6·M6 | ⑩ 적 피격 · `stepActives` · ② 대시 |
| **팬텀 3종** | AS9·PH3·PH6 | ⑩(AS3 의 강화탄 표식 재사용) · ⑨+③ · ②예약→⑨집행 |
| **버블 1종** | DR1 | ⑮ 파열 — 젬 **좌표만** 옮기고 수거는 정본 픽업에 맡긴다 |

### ⚠️ 이 배치가 바로잡은 **틀린 문장 하나**
**버블 DR7 의 종전 사유 절반이 틀렸다.** *"활성 술어를 세울 값이 없다"* 는 오류다 —
`state.echoRuntime.state === 1`(`echo.ts:57`)이 정확히 그 값이다. 그래도 배선하지 않은 진짜
사유는 **다르다**: 효과 셋 중 둘이 state 1→2 **엣지**를 요구하는데 `stepEcho`(1878)가
앵커(1874) **뒤**라 엣지가 안 보이고, 설계서가 레벨을 ③ 에만 걸어 ① 만 넣으면 Lv1 = Lv20 이
된다. **사유를 코드에 갈아 끼웠다.** (누적 규약: 레인 보고의 「~가 없다」는 직접 grep 해라.)

### 이 배치의 설계 판단 (뒤집으려면 근거가 필요하다)
- ⭐ **F8 에 상한을 강제했다.** 초안은 보스 `iframes`(과열 취약 창)를 명중마다 연장하는데
  **상한이 없었다**. 스트라이커는 다탄 볼리라 보스 한 마리에 매 틱 명중이 정상이고, 연장 1 ≥
  감소 1 이면 **과열(피해 2배) 창이 영구히 안 닫힌다.** 밸런스가 아니라 결함이라 판단해
  되돌려보냈다. 착지형은 `Math.min(iframes + ext, F8_OVERHEAT_CAP=300)` + **`iframes < CAP`
  게이트 한 쌍** — 게이트가 없으면 이미 천장 위인 창을 F8 이 **깎아** 강화가 약화로 뒤집힌다
  (레인이 지적했고 옳다). 300 의 근거는 `boss.ts:56`·`defenseBosses.ts:135` 가 둘 다 300.
  leaf-local 상수이고 런타임 import 는 없다.
- **DR1**: 설계서의 *"파열 훅에서 `collectGem`"* 은 **구조적으로 불가능**하다(`world.ts`
  모듈-로컬 · export 해도 leaf 규율 위반 · `skillHooks.ts` 로 올리면 순환). 대신 **젬 좌표를
  플레이어 위로 옮겨** 정본 픽업이 걷게 했다 — 콤보·XP·촉매 경로 복제 0.
- **FO4·FO8·FO9 는 같은 지점을 공유한다** — 앵커 ⑨ 가 감쇠 분기보다 앞인 순서를 써서
  `aux1 + 1 >= ARMOR_DECAY_TICKS` 로 성사를 **선점**한다. 우선순위 **FO4 → FO9① → FO8**,
  설계서의 축 내 긴장("감쇠를 막거나 뒤집을수록 FO8 이 죽는다") 그대로다.
- **PH6**: 앵커 ② 예약 → 다음 틱 앵커 ⑨ 가 `aux0 - 1` 집행 → 진입 에지가 예산 리셋.

### 보고된 설계-코드 불일치 **3건** (문서는 고치지 않았다)
1. **브루저 FO4** — 설계의 정지 전용 주기 `60 + 3600/(Lv+11)`(Lv1 = 360틱)를 담을 칸이 없다
   (`aux1` 은 엔진이 180 에서 강제 리셋 · 강화 축 B 예산이 FO2·FO7 로 2/2 포화). 엔진의 180 을
   쓰므로 **Lv1 에서 설계보다 두 배 자주 적립되고 레벨이 게이트로만 작동한다.** 밸런스 소관.
2. **브루저 FO9③** — 설계는 감소 bp 에 **가산**하라는데 앵커 ⑧ 이 장갑 **앞**이라 두 감소가
   **곱해진다**(만재 Lv20 설계 29.6% vs 실제 27.7%). **유리한 쪽으로 틀리지 않아** 이 순서로 뒀다.
   FO6 이 같은 자리에서 같은 사유를 안고 있다.
3. **버블 DR1** — 위 §설계 판단의 `collectGem` 건.

### 배치3 이 남긴 위험 (다음 세션이 알아야 한다)
- **팬텀 PH6 은 `aux0` 을 직접 1 내리는 유일한 코드다.** world 의 `aux0++` 가 곧바로 되돌린다는
  **순서 전제**에 매달려 있어, `stepShipSignature` 안에서 앵커 ⑨ 호출이 적립 뒤로 옮겨지면
  조용히 되감기가 된다. 집행부 주석에 못 박혀 있다. **테스트가 아니라 주석이 지키고 있다.**
- **브루저 MO4 는 한 틱 늦다** — 감속 부여(`resolveCollisions`)가 앵커 ⑨ 뒤이고 이동 배율
  읽기(`stepPlayer`)가 앞이라, 부여된 감속이 다음 틱 이동에 한 번 실린 뒤 지워진다.
  "0틱 무효화"는 **부여 지점에 손잡이**가 필요하다.
- **팬텀 AS1/AS8/DI10 주의** — AS1 을 `setBreakToken` 재장전으로 대체하면 후속 타가 원배율
  (2.5배)을 받아 **AS3 과 값이 같아진다**(설계 후속 배율은 Lv20 ≈ 1.6배로 원배율보다 **작다**).
- **`skillSlots.ts` 의 「팬텀 구간 슬롯」 doc 이 병렬 머지에서 한 칸 밀려 `HatchlingCarry` 에
  붙어 있다.** 팬텀 레인이 사실만 갱신하고 자리는 안 옮겼다(옮기면 해츨링 레인과 충돌).

### 배치3 검증
- `pnpm verify` → `Tests 2 failed | 7397 passed` — 실패 2건은 **사전 적색**이고 기준선
  `1f69a36` 워크트리와 **값까지 대조**했다: `pilotFrameFreeze` 발산 16줄 **0 diff**,
  `shipSignaturePhantom` 은 양쪽 다 `expected 3 to be greater than or equal to 4`.
  ⚠️ **건수 일치가 아니라 값 일치가 근거다** — 팬텀 3종을 배선한 배치라 이 대조는 필수였다.
- `tsc --noEmit` 0 · `eslint` 0 · **fallthrough 기계 감사 0건**(모든 `case SIG_` 가 break/return)
- 충돌 마커 전수 grep 0건. `tests/skillWiringCensus.test.ts` 는 **네 레인이 전부 자기 수로
  합계를 고쳐 와 세 번 연속 충돌**했다 — 합계는 리드가 전 기체 합산으로 다시 계산했다(125).

## ⭐ 배치1 — S3 가 연 자리에 스킬을 태웠다 (96 → 108)

| 기체 | 배선 | 앵커 |
|---|---|---|
| 해츨링 **7종** | BD1·BD2·SH10·NU10 / BD6·NU2·NU7 | ㉓ / ㉔ |
| 버블 FI9 | 1종 | ㉒ |
| 아크캐스터 CH3 | 1종 | ⑯ 각인 → ⑥ `'life'` **2단** |
| 말로우 ME5 | 1종 | ㉕ |
| 스트라이커 F5 | 1종 | ⑯ `aimAngle` |
| 브루저 BL2 · 팬텀 AS3 | 2종 | ⑯ `targetDist` / ⑯ `cloakBreak` + ⑩ |

### ⚠️ 이 배치가 바로잡은 **틀린 문장 셋** (앞 세션들이 남긴 것)
1. **`FILM_PERIOD_TICKS` 는 있다.** `shipSignature.ts:345` 에 `= 420`. S3-5 가 "없다"고
   보고했고 리드가 검증 없이 옮겼다. **레인 보고의 「~가 없다」는 직접 grep 해라.**
2. **`aimAngle` 만으로 SQ7 은 안 열린다.** 두 레인이 독립적으로 같은 결론에 도달했다 —
   술어에 **입력 벡터**가 필요한데 `autoAttack(state, player)` 이 `InputFrame` 을 안 받고
   `WorldState` 에 보관도 없다. **앵커 확장이 아니라 input 배관이 선결이다.**
3. **앵커 ⑩ 은 가해 탄을 넘긴다.** `phantom.ts` 의 *"⑩ 은 탄을 넘기지 않아 어느 탄이 죽였는지
   모른다"* 는 사실이 아니다(`onEnemyDamaged(state, target, dmg, source)`, `target.dead` 확정).
   AS3 을 막던 것은 처음부터 **소진 신호 부재 하나뿐**이었다.

### 이 배치의 설계 판단 (뒤집으려면 근거가 필요하다)
- **FI9**: 설계 산식에 상한이 없어 Lv20·`aux1`=419 에서 70 → `aux0 ≤ FILM_ABSORB_FLAT`(60)
  엔진 불변식 위반. **불변식을 이기게 두고 `Math.min` 만재 절삭.**
- **CH3**: `params.damage` 는 아키타입 분기 **전** 값이라 쌍둥이 항성 배율이 빠진다 → 훅이 아니라
  **world 가 탄 생성 직후 `b.aux1` 에 각인**하는 2단 구조. `aux0` 은 CH1·CH8 이 점유 중.
- **ME5**: CU3 의 `player.aux0 = carry` 를 **`+=` 로 바꿨다**(대입이면 ㉕ 이 앞서 쓴 이월분을
  덮는다). ME5 미투자 런은 `aux0`=0 이라 비트 동일.
- **NU2**: 설계서의 *"젬 기본값의 50%"* 에 해당하는 단일 정본이 리포에 없다(적별 `xpValue`
  3~28 · 폴백 1 · `SUPPLY_GEM_XP` 6 · `DESTRUCTIBLE_GEM_XP` 5). `NU2_SHELL_GEM_XP = 2` 로
  확정하고 사유를 상수 주석에 남겼다 — **다른 기준이 의도였다면 이 상수 하나만 고치면 된다.**
- **BD10 은 여전히 미배선**: 상한 −1 만 넣으면 순손해라 반쪽을 피했다. 탄 피해 배율이
  `stepTurrets`/`fireTurretShot` 소관이라 **거기 앵커가 서야 열린다.**
- **SH10 배치식**(`live % 5`) 5번째 오프셋은 여전히 미결 — 상한·요구치 축만 배선했다.

### 배치1 검증
`tsc` 0 · fallthrough 기계 감사 0건 · `pnpm verify` → `Tests 2 failed | 7221 passed`
(실패 2건은 사전 적색 `pilotFrameFreeze`·`shipSignaturePhantom`, 브루저/팬텀 레인이
기준 `6485c1c` 체크아웃으로 **동일 실패임을 대조 확인**했다).

골든 레인: 실패 집합이 기준선과 **양방향 차집합 0**, 발산 해시 22개 값도 동일,
**무투자 3종 전부 초록**(`kargon-recon/no-invest` · `berdan-engage/no-invest` · `기본 Profile`)
→ 투자 없이 스킬이 발동하지 않는다.

### ⭐⭐ 골든 레인이 실제로 덮는 범위 — **수치로 확정했다 (2026-08-07)**
`tests/fixtures/striker-prem8.json` 의 6빌드 투자 벡터를 전수로 뽑았다:

```
no-invest        []
axis-firepower   [[0,20],[1,20],[2,4]]
axis-survival    [[10,20],[11,20],[12,4]]
axis-mobility    [[20,20],[21,20],[22,4]]
mixed-three      [[0,20],[1,2],[10,20],[20,18]]
near-max         [[0,20],[1,20],[10,20],[11,15],[20,20],[21,4]]
투자된 슬롯 합집합: 0,1,2 · 10,11,12 · 20,21,22
```

`investAxis` 가 **노드당 상한 20으로 순차 탐욕 충전**이라 44포인트 = 20+20+4 에서 멈춘다 —
**각 축의 앞 3개 노드까지만** 닿는다. 결론:

- **골든은 스트라이커 30종 중 9종만 밟는다.** 슬롯 3 이상은 원리적으로 미검출.
- **다른 6기체는 한 종도 안 덮는다**(이 픽스처는 스트라이커 전용이다).
- 그래서 배치1 이 **12종을 배선했는데 발산 값이 한 줄도 안 바뀐 것은 정상**이다
  (F5 는 슬롯 4라 어느 빌드에도 투자되지 않는다). **무연산 신호가 아니다.**
- ⚠️ 뒤집어 말하면 **"골든이 안 갈렸다" 는 배선 검증의 근거가 될 수 없다.** 배선의 물증은
  레인별 **뮤테이션 단위 테스트**뿐이다. 배치1 은 6레인 전부 뮤테이션 적색을 확인했다.

### ⭐ 두 번째 커버리지 구멍 — **sim 레인에 `activeSlots` 가 0건이다**
`fix/blast-death` 레인이 실측했다: `test:sim` 3파일에 `activeSlots` 참조가 **0건**이라
**동결 5종 액티브가 원리적으로 발동 불가**하고, 무투자 런은 `endpointBurst` Lv0 이라
아크캐스터 CH3 도 불발이다. 그래서 그 레인의 **골든 발산 0 은 "안 갈렸다"가 아니라
"안 밟혔다"** 는 뜻이다. 실전 액티브 장착 런에서는 처치·젬·드랍이 늘고 `dropRng` 를
더 소비한다 — **골든이 원리적으로 못 재는 축**이다.

⚠️ 이 두 구멍(투자 슬롯 0~2 · 액티브 0건)을 합치면: **골든은 "무투자에 가까운 스트라이커 런"만
잰다.** 그 밖의 회귀는 전부 단위 테스트가 잡아야 한다.

## S3 — 신규 앵커·필드

진행 브랜치 **`feat/skill-s3-anchors`**(origin 에 push 됨). 각 레인은 자기 브랜치에서
격리 작업 → 이 브랜치로 순차 머지 → 한 PR 로 착지.

- [x] S3-5 막 진입 앵커 **㉒ `onFilmEntry`** — 버블 FI9. 게이트를 넓히지 **않고** 앞에 앵커를
      뒀다(넓히면 파열 훅 9종이 매 치명 피격마다 오발동). `hashWorld` A/B 3경로 동일 실증.
- [x] S3-4 해츨링 출격 앵커 **㉓ `onBroodLaunchParams` · ㉔ `onBroodLaunched`** — 8종 중
      **7종 완전 + BD10 부분**(탄 피해 배율은 `stepTurrets` 앵커가 따로 필요).
- [x] S3-1 `VolleyParams` **`aimAngle`**(읽기 전용) — F5 는 열렸다. **SQ7 은 여전히 반쪽**:
      술어가 *"입력 벡터 · 발사각의 내적"* 인데 **입력 벡터 항이 레코드에 없다.**
- [x] S3-2 앵커 ⑥ 에 **`reason: 'pierce' | 'life'`** — 아크캐스터 CH3. 기본값 없는 필수 인자라
      새 호출부가 사유를 빠뜨리면 `tsc` 가 잡는다. 기존 case 는 F4 하나뿐이라 `'pierce'` 게이트로 봉인.
- [x] S3-3 정산액 확정 직전 앵커 **㉕ `onCushionSettleDue`** — ME5 만 열린다.
      ME8·ME9 는 ㉕ 로도 못 온다(탕감률·임계가 순수 함수 **안**).

**최종 앵커 번호 (리드가 머지하며 재배번)**: ㉒ `onFilmEntry` · ㉓ `onBroodLaunchParams` ·
㉔ `onBroodLaunched` · ㉕ `onCushionSettleDue`. **총 25개 + 공유 술어 1.**

⚠️ **앵커 번호는 리드가 머지하며 다시 매긴다.** S3-4·S3-5 가 둘 다 ㉒ 를, S3-3 이 ㉔ 를
주장해 충돌했고 git 은 그것을 전혀 몰랐다(인계 §6 의 「의미 충돌」). 자동 병합 충돌은 전부
*다른* 곳(import 줄·테스트 계측기)에서 났다. 다음 레인도 번호를 겹쳐 올 것이다.

### 검증 결과 (S3 통합)
- `tsc --noEmit` 0줄 · `eslint` 0줄
- `pnpm verify` → `Tests 2 failed | 7157 passed` — 실패 2건은 main 사전 적색
  (`pilotFrameFreeze` · `shipSignaturePhantom`) 그대로
- **골든**: `test:sim` 을 통합 브랜치와 기준선 워크트리(`0f65a5c`)에서 각각 돌려
  ANSI·시간 정규화 후 `diff` → **3,859줄 0 diff.** 실패 32건 목록도, 발산 해시 22개
  값도, `denoFixture` 단언 diff 도 바이트 동일. ⚠️ **건수 일치는 증명이 아니다** —
  이미 빨간 골든은 값이 갈려도 여전히 빨갛다. 값 대조가 근거다.

### ⚠️ S3 시점의 「반쪽」 목록 — **배치2 가 대부분 해소했다** (이력으로 남긴다)
- ~~말로우 SQ7~~ · ~~해츨링 BD10 탄 피해~~ · ~~말로우 ME9~~ — **전부 배치2 에서 착지.**
- **말로우 ME8 은 아직이다** — 탕감률 축은 배치2 의 개정 범위 밖이었다.
- **버블 DR2 도 아직이다** — 사유는 §「그 뒤」.
- ~~버블 FI9: `FILM_PERIOD_TICKS` 가 없다~~ — **이 보고는 틀렸다.** `shipSignature.ts:345` 에
  `FILM_PERIOD_TICKS = 420` 이 실제로 있다(`FILM_ABSORB_FLAT = 60` 은 347). S3-5 레인이
  잘못 보고했고 리드가 검증 없이 여기 옮겼다. **레인 보고의 「~가 없다」는 직접 grep 해서 확인해라.**
- **아크캐스터 CH3** 배선 시 주의: 앵커 ⑥ 수명 만료 호출부가 `for (const e of state.entities)`
  순회 **안**이라 훅에서 엔티티를 스폰하면 안 된다. `splitSpawns` 처럼 루프 뒤로 미뤄라.

### 보고된 설계 문서 오류 (문서는 고치지 않았다)
1. **BD10** — 설계서가 *"`BROOD_MAX_DRONES` 를 읽는 곳은 `stepHatchBrood` 한 곳"* 이라 하는데
   실제로는 `src/sim/skills/hatchling.ts:103` 에 지역 사본이 있고 SH3 의 만석 술어가 그걸 읽는다.
   **읽는 곳이 둘이다.** world 쪽만 고치면 "만석이 아닌데 만석" 갈림이 조용히 난다.
2. **SH10 배치식**(`live % 5`, 우상단 고정 1칸) — 5번째 자리 오프셋을 임의로 정하면 문서와
   갈리므로 열지 않았다. **어긋남이 아니라 미결.**
3. `skillHooks.ts` 파일 헤더가 아직 "앵커 15개" 로 낡아 있다(현재 25개).

## S3 가 연 자리를 쓰는 배선 — **전부 배치1 에서 착지했다**

해츨링 7종 · 버블 FI9 · 아크캐스터 CH3 · 말로우 ME5 · 스트라이커 F5 · 브루저 BL2 · 팬텀 AS3
= **12종.** 상세·설계 판단은 위 §「배치1」.

⚠️ 배선 레인은 **격리 워크트리 병렬 → 순차 머지**. 프롬프트에 **`break;` 필수**를 명시해라
(fallthrough 누적 5건). 그리고 **앵커 번호·export 목록을 겹쳐 오므로** 리드가 머지하며 조정해라.
배치1 에서도 `VolleyParams` 에 **필수 필드**가 신설돼 다른 레인 픽스처를 깼고, **git 은 몰랐고
`tsc` 만 잡았다** — 누적 실측이 또 같은 말을 했다.

## 그 뒤

- [x] **배치2 — 앵커 확장 셋 + 순수 함수 개정** (108 → 114)
  - **앵커 ㉖ `onTurretShotParams`**(`fireTurretShot`, 표적 확정 뒤·`spawnBullet` 앞) →
    해츨링 **BD10 완결**(상한 −1 · 수명 가산 · **탄 피해 배율** 3축 전부).
    앵커는 병아리·센트리·드론 베이 **전부에서 불리고** 효과 게이트는 훅 첫 줄 — 센트리 회귀를
    같은 런의 긍정 짝과 함께 잠갔다(`ownerId !== BROOD_MARK` 조기 반환 삭제 뮤테이션이 적색).
  - **`VolleyParams.inputX`/`inputY`** → 말로우 **SQ7**. `autoAttack` 호출부가 **딱 1곳**이라
    시그니처 확장 비용이 사실상 0. `onVolleyParams` 자체에 `input` 을 더하면 7기체가 갈리므로 배제.
  - **`onPlayerDamaged(…, sources)` 피해원 비트합** → 브루저 **BL8**.
    ⚠️ **유니온이 아니라 비트합**인 이유: `dmg` 가 합이 아니라 `max` 라, 승자 하나만 실으면
    적탄이 더 아픈 틱마다 접촉이 통째로 삼켜져 BL8 이 조용히 미발동한다.
  - **순수 함수 넷 개정** → **FI8·ME9·CU7** + ⭐ **앵커 ⑰ 부활**.
- [ ] ⚠️ **DR2 는 여전히 미배선** — 효율 사유는 해소됐으나 남은 선결이 **술어**다
      (막 있음 + 젬 수거 60틱 창 = 신규 WorldState 정수 1개 + 감소 + 해시 꼬리 폴드).
      셋 중 하나라도 빠지면 해시에만 접히는 반쪽이라 손대지 않았다.
- [x] **배치3 — 4레인 병렬 배선** (114 → 125). 상세는 위 §「배치3」.
      ⭐ 이 배치의 진짜 산출은 11종이 아니라 **「남은 85종의 병목이 앵커 부재」라는 부류 지도**다
      (§「다음 할 일」의 표). 네 레인이 독립적으로 같은 벽을 보고했다.
## ⛔ 촉매 48종 배선 — **착수 불가다. 인계의 「선결 완료」는 틀린 문장이었다**

2026-08-07 에 4개 레인을 띄웠고 **배선한 촉매는 0종**이다. 레인 두 개가 **독립적으로** 같은
결론에 도달했고, 결정적으로 **설계 문서 자체가 반증한다** —
`.omc/plans/catalyst-rebuild-2026-08-06/catalog-signature.md:405` 에
**「남은 선결 과제 (구현 레인 착수 전)」 12건**이 명시돼 있다. 그중 최소 5건이 미완이다.

### 왜 한 장도 못 얹는가 (실측)
1. ⭐ **`CatalystDef` 가 아직 구 모델이다** — `src/data/catalysts.ts:47` 은
   `reward: {axis, perStack, powerStat}` + `penalty: {axis, perStack}` 라는 **축 기반 버프/디버프**
   모델이다. 48종의 카드별 규칙(죽은 자리 나무·보급선 격추 자원·수확 지대…)을 담을 자리가
   **없다.** → **id 하드코딩 말고는 카드별 분기의 근거가 코드에 없다**(선결 ②).
2. **`id 12·27·29` 는 되돌림 조건이 「대시로 적을 관통해 죽이면」인데 그 메커니즘이 sim 에
   아예 없다.** `world.ts` 의 대시는 vx/vy·`dashCooldown`·`iframes`·적탄 소거뿐이고 대시 피해·
   통과 판정이 0건이다. 게다가 `iframes` 중엔 적도 안 맞는다. **앵커 문제가 아니라 선결 부재이고,
   `audit.md` 미해결 목록에도 없는 신규 건이다.** 세우지 않으면 세 장 다 되돌릴 수 없는
   단조 감소가 되어 헌장 §페널티 규율 3 위반.
3. **적립처와 소비처가 앵커 그룹을 가로지른다** — 예: `id 2·23·43` 은 죽은 자리 생성물의
   소비가 per-tick, `id 15·17·19` 는 적립처가 **보급선 격추**인데 그 앵커가 아예 없다,
   `id 44` 는 **보스 사망 앵커**가 필요하다(⑪ 은 `kind==='enemy'` 게이트라 안 덮는다).
4. **`hasCatalyst` 류 소지 술어가 코드에 없다.** 레인마다 각자 만들면 갈린다 — 한 곳에 세워야 한다.

### 다음 세션이 할 일 (순서)
- [ ] **선결 ② 먼저** — `CatalystDef` 를 48종 규칙을 담는 모델로 재작성 + 48행 데이터.
      ⚠️ **이건 라이브 촉매 시스템(상점·드랍·일일보상·EF·SQL 미러)에 파급된다.**
      혼자 정하지 말고 **사용자 판단을 받아라.**
- [ ] 선결 ①(적 `aux0` 촉매 비트 구역 표) · ⑦(`objective.ts` 등재) · ⑧(촉매 해저드 동시 상한)
- [ ] 「대시 관통 처치」 메커니즘 신설 여부 결정(`id 12·27·29`)
- [ ] 보급선 격추 앵커 · 보스 사망 앵커 신설
- [ ] 그 뒤에야 배선. **지금 얹으면 전부 반쪽이 되어 슬롯이 해시에만 접힌다.**

### ⛔ 파킹된 브랜치 **둘** — 완성됐지만 머지하지 않았다 (origin 에 보존)
- **`feat/catalyst-progress`** (`1754eb7`) — `id 9 epiphany`(3택→1택) · `id 14 mastery`(같은 파워업 셋)
- **`feat/catalyst-defense`** (`b10e54d`) — `id 24 chainreaction`(피해 전이 + 최대HP 하락 + 세그먼트 복구)

둘 다 테스트·뮤테이션·(progress 는) RNG 소비량 불변 증명까지 붙어 있다. **레인들은 옳게
작업했다 — 막은 것은 데이터다.**

**사유 — 머지하면 이중 효과가 된다.** 데이터의 `reward.axis` 는 **런타임에 실제로 소비된다**
(`src/data/catalysts.ts:248` 이 축별로 합산 → 드랍 희귀도·XP 에 쓴다). 그런데 그 데이터는
**ADR-0029 구 수치표**이고 ADR-0052 카탈로그와 축이 다르다:

| id | 코드 `reward.axis` | ADR-0052 문서 |
|---|---|---|
| 9 epiphany | `rarity` | XP ×1.8 |
| 14 mastery | `xp` | 드랍 ×1.6 |
| 24 chainreaction | `catalystDrop` | XP ×2.5 |

즉 지금 머지하면 그 카드는 **구 축 보너스 + 신 고유 효과를 둘 다** 갖는다. 어느 쪽이 정본인지는
**제품 결정**이라 리드가 정할 일이 아니다.

→ **선결 ② 를 끝낸 뒤 두 브랜치를 되살려라.** 작업은 버리지 않았다.

### 선결 하나가 더 드러났다 — **48종 전역 슬롯 배분표가 없다**
`c-defense` 가 `ChainReactionSlot` 로 슬롯 0·1 을 선점했는데, **누가 어느 슬롯을 쓰는지의 정본이
없어** 다른 레인이 같은 칸을 잡으면 조용히 겹친다. 스킬 쪽 `skillSlots.ts` 처럼 **48종 배분표를
한 곳에 세워라.** 선결 목록에 추가할 것(설계 문서 12건에는 없다).

### 이 배치가 남긴 것 (거동 0)
촉매 앵커 14개가 **실제로 불린다는 계측**과 각 카드가 왜 못 얹히는지의 **근거 주석**.
배선 레인은 「앵커가 불린다」는 전제 위에 서므로 이 실증은 그 자체로 값이 있다.
`feat/catalyst-progress` 의 `tests/catalystProgress.test.ts` 는 **RNG 소비량 불변**을
`powerupRng.getState()` 로 직접 잠그는 형태라 뒤 레인이 그대로 재사용할 수 있다.
- [ ] D 단계 골든 재생성 **1회** + EF 재배포
- [ ] 단계 3~7(어픽스 재편 → ADR-0050 §1 → 배포 3단계 → 개연성 캡 → 밸런스)

## ⭐ 좀비 결함 — **한 부류로 3건 이상 있었다** (2026-08-07)

`compact()` 는 **`e.dead === true` 만 수거**한다(`hp <= 0` 단독으로는 안 걷는다). 그래서
`e.hp -=` 만 하고 `dead` 를 안 세우면 그 적은 **좀비**로 남는다 — 계속 움직이고 공격하며
**처치 집계·젬·전리품이 전부 사라진다.** 촉매 설계서의 선결 ⑨ 가 이것이었고, 실제로는
촉매 밖에도 퍼져 있었다.

**부류 완결 — 8건 고쳤다.** 정본 형태는 **`src/sim/status.ts` 111-112 의 두 줄**이다:
`blastDamageAt`(동결 5종 액티브 + CH3) · 말로우 **SQ3** · 브루저 **MO6**·**BL3**·**BL9** ·
버블 **PO1** · 팬텀 **AS4**·**AS5**.

**⭐ 판정축은 「대상이 누구인가」가 아니라 「표적이 탄을 견뎠는가」다.**
표적이 이미 죽었으면 재마킹은 **무연산**이고, **살아남았으면** 격추 판정(`world.ts:4171`)이
`hp > 0` 이라 안 탔으므로 추가분이 hp≤0 을 만들어도 **아무도 `dead` 를 안 세운다** → 좀비.
그래서 `if (t.hp <= 0) t.dead = true;` 는 두 경우 모두에서 안전하다.
앵커 ⑩ 의 금지는 **「`dead` 를 false 로 되돌리는 것」에 한정**된다(`skillHooks.ts:828-830` 이
오히려 이 결함을 경고하고, `striker.ts:400-404`·`436` 이 이미 같은 해석으로 구현돼 있었다).

⚠️ **guardian·core 는 일부러 뺐다** — 그 둘만 `world.ts:4175-4183` 에 **부활 분기**가 있어
마킹하면 부활 충전을 건너뛰고 죽인다. 대상 범위는 `enemy + boss` 다.

**적 대상 `.hp -=` 는 14곳이고 이제 전부 마킹된다. 의도적 미마킹은 하나도 안 남았다.**

### ⚠️⚠️ 이 부류를 잴 때의 계측기 함정 — **두 레인이 실제로 밟았다**
적을 플레이어 **코앞에 두면 자동사격 탄이 같은 틱에 마무리**해서 **수정 전에도 테스트가 통과**한다.
적을 **효과 반경의 바깥 끝**에 두어(자동사격 탄 ≈30px/tick) 사망 경로를 그 효과 하나로 좁혀라.

### 뿌리 — **한 문장의 오해가 결함을 5곳 이상 복제했다**
`bruiser.ts:397` 위 doc 이 *"`compact` 의 처치 게이트가 `hp <= 0`"* 이라 적었다. **틀렸다** —
1차 게이트는 `dead`(`world.ts:4753`)이고 `hp<=0` 은 그 **안쪽**의 킬 집계 게이트(`4763`)다.
SQ3 주석은 거기서 더 나아가 *"`dead` 를 세우면 킬 집계가 두 벌이 된다"* 는 **반대 사실**까지
적고 있었다(`state.kills++` 는 `compact()` 안 단 한 곳이고 순회는 엔티티당 1회다).
**세 자리 모두 정정했고 사유 문장은 남겼다.**

⚠️ 이 부류를 잡는 과정에서 **「의도적」 분류가 두 번 뒤집혔다.** 처음엔 "앵커 ⑩ 뒤라
의도적", 다음엔 "주변 적은 판정을 안 거치니 결함", 마지막엔 "표적 자신도 **견디면** 판정을
안 거치니 결함". **분류를 믿기 전에 그 자리의 시나리오를 실제로 돌려 봐라.**

### 킬 이중 집계는 안 된다 (직접 확인)
`world.ts:4763` 의 `if (e.kind === 'enemy' && e.hp <= 0) state.kills++` 가 `compact()` 안
**단 한 곳**이고 순회는 엔티티당 1회다. 수거된 적은 `survivors` 에 안 실려 다음 틱에 다시 안 보인다.
**`dead` 대입은 수거 대상 표시일 뿐 집계 행위가 아니다** — SQ3 주석이 반대로 적고 있었고 그게 틀렸다.

## 미결(사용자 판단)

⚠️ **아래는 전부 「지금 결정하지 않는 것이 맞다」고 판단해 미룬 것이다** — 리포 규약이 밸런스를
출시 직전 일괄로 잡기 때문이다. 다음 세션이 이걸 보고 "미결이니 지금 물어보자"로 읽지 마라.
**물어야 할 때는 밸런스 일괄 레인을 열 때다.**

### 밸런스 일괄 레인에서 한꺼번에 (지금 묻지 마라)
- **NU2 의 `NU2_SHELL_GEM_XP = 2`** — 설계서 *"젬 기본값의 50%"* 에 해당하는 정본이 리포에
  없어 잡몹 기준으로 확정했다. 다른 기준이 의도였으면 **그 상수 하나만** 고치면 된다.
- **브루저 FO4 의 레벨 축이 사실상 죽어 있다** — 설계의 정지 전용 주기
  `60 + 3600/(Lv+11)`(Lv1 = 360틱)를 담을 칸이 없어 엔진의 180 을 쓴다. 결과: **Lv1 에서 설계보다
  두 배 자주 적립되고 레벨은 게이트로만 작동한다.** 고치려면 강화 축에 상태 예산 1칸이 필요하다.
- **`dailyRewardSelection` 촉매 축 사거리 축소.**
- **브루저 FO9③ 은 가산이 아니라 곱셈이다**(Lv20 만재 설계 29.6% vs 실제 27.7%). 유리한 쪽으로
  틀리지 않아 그대로 뒀다. FO6 이 같은 자리에서 같은 사유를 안고 있다.

### 설계 문서에 정본이 없어 코드가 임의로 정한 것 (문서를 고칠 사람이 필요하다)
- **SH10 배치식**(`live % 5`, 우상단 고정 1칸) — **5번째 자리 오프셋이 문서에 없다.**
  임의로 정하면 문서와 갈리므로 열지 않았다. **어긋남이 아니라 미결이다.**

### 배포·재동결 (승인 시점에 한 번에)
- **D 단계 골든 재생성 1회 + EF 재배포** — 지금 하면 검증 안 된 32건 발산을 통째로 축복하고
  되돌릴 근거가 사라진다. **밸런스 확정 후 승인 시점에.**
- **촉매 SQL 마이그레이션 미배포**(`SLOT_CAP` 8→3) — 클라 먼저, SQL 나중.
  ⚠️ **촉매 전반은 사용자가 다른 세션에서 직접 진행한다**(2026-08-08 지시). 이 레인은 손대지 않는다.
- ~~로컬 `main` 체크아웃이 뒤처져 있다~~ — **2026-08-07 해소.** `D:/ClaudeCowork/shooting` 을
  `6485c1c` 로 fast-forward 했다(워킹트리 clean, `--ff-only`).
