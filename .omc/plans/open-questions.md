# Open Questions (플랜 미결 사항 추적)

## 무한 스크롤 맵 + 개체 2배 스케일 + 맵 기믹 - 2026-07-15

> **착수 전 확정 불필요 — 전부 기본안으로 시작 가능.** OQ1~6 전부 M1 기본(제안)안이 마련돼 있어 실행 착수를 막지 않는다. `[실행중-기본안有]` = 구현하며 기본안대로 진행(필요 시 실행 중 조정), `[실행전-필수]` = 착수 전 확정 필요.

- [ ] OQ1 `[실행중-기본안有]`: 청크 컬링 후 재진입 시 파괴물/이벤트 소비 상태 — 순수 재생성(부활) vs 상호작용 청크 컬링 보류 — 결정론·메모리·게임 체감에 영향. M1 기본 (a) 순수 재생성 제안.
- [ ] OQ2 `[실행중-기본안有]`: 파괴 가능 오브젝트가 이동을 차단할지(벽 겸용) vs 통과(순수 타깃) — 벽과 역할 분리 위해 (b) 통과 제안.
- [ ] OQ3 `[실행중-기본안有]`: 탄(bullet/enemyBullet) radius도 2배로 키울지 — 탄막 가독성 규칙과 충돌 여부. (a) 유지 또는 (b) ×1.5 제안.
- [ ] OQ4 `[실행중-기본안有]`: 이벤트 오브젝트 발동 방식 — 접촉/근접 자동 vs 상호작용 키 — 결정론(입력 프레임 불요) 위해 접촉/근접 제안.
- [x] OQ5 `[실행전-필수/스코프 확정]`: 이벤트 3종 외 추가 종류(스펙 R8 "추가는 설계 위임") 이번 범위 포함 여부 — **스코프 확정: 이번 스코프에는 없음(3종만), 추가는 후속.**
- [ ] OQ6 `[실행중-기본안有]`: 돌격병 fragments 보조 트리거 — 벽 슬라이드 주 트리거만(a) vs 돌진 종료/`fireCooldown` 주기 보조 트리거 추가(b) — 무한 맵에서 기믹 벽 밀도에 공격성이 종속되면 벽 없는 구간에서 돌격병 위협 소멸. Architect C5 반영, (b) 보조 트리거 포함 권장(조건은 F1a 벽 밀도 확정 후 튜닝).

## 액티브 스킬 (active-skills-2026-07-31) - 2026-07-31 · 개정 2

- [ ] AS-OQ1 `[실행전-필수]`: AC-25 는 "인덱스 0~23 와이어 값 불변"만 보증하고 **파워업 가중 총합 불변은 보증하지 않는다**. 계획은 미장착 시 pool 진입 자체를 필터로 차단하는 처방을 냈으나, 스펙 문구를 개정할지 계획 처방으로 갈음할지 결정 필요 — 갈음하면 다음 파워업 append 때 같은 함정이 재발한다.
- [ ] AS-OQ2 `[실행중-기본안有]`: 오토파일럿 발동 조건의 구체 정책(AC-10 은 "조건을 만족하면"만 요구). 조건이 조악하면 `rosterBench` 기준선이 왜곡된다. 기본안 = 쿨다운 해제 즉시 발동, 밸런스 패스에서 재검토.
- [x] AS-OQ3 `[해소]`: `SAVE_VERSION` 8 머지 시점 — **0a 에 포함, 롤백 경로를 계획 B-9 로 명문화**하여 해소. V8 은 되돌리지 않고 레인 폐기 시에도 유지한다.
- [x] AS-OQ4 `[해소]`: placeholder 계수 범위 — **하네스 육안 확인이 가능한 과장값**으로 확정(AC-23 성립 조건). 밸런스 패스에서 일괄 교체.
- [ ] AS-OQ5 `[실행후]`: ADR-0015 개정("액티브 스킬은 유니크와 함께 인스턴스 단위 아이콘 예외" 문장 추가)이 미완 — ADR-0041 귀결에 명시된 후속.
- [x] AS-OQ6 `[해소]`: i18n 84키 값 — `tests/i18n.test.ts:197,211,248,428` 이 `expect(v.length).toBeGreaterThan(0)` 라 **빈 문자열 불가**. **placeholder 문자열(키명 그대로)로 확정**(0a-12), 이모지 금지.
- [ ] AS-OQ7 `[실행중-기본안有]`: 액티브 표시 문자열이 `t()` 경유가 되어 **트리 노드(`data/ships/authoring.ts:16-18` — `node.name` 직접 렌더)와 규약이 둘로 갈린다**. 다만 Pixi 연구소는 이미 크롬 전체를 `t()` 로 그리고 `node.name` 만 예외(`pixi/researchLab.ts:855-868`) — 이관이 오히려 규약을 하나로 되돌린다. 기본안 = 이번엔 이관 안 하고 헤더에 이유만 명시.
- [ ] AS-OQ8 `[실행전-확인]`: 0a-4 신규 `WorldState` 정수 4개(쿨다운 2 + 버프 잔여 2)가 **스냅샷·하네스 `preset`·리플레이 직렬화**를 통과하는지 실측(해시 외 3경로). `src/harness/presets.ts`·`src/harness/core.ts`.
- [ ] AS-OQ9 `[실행전-확인]`: `src/harness/cheatPanel.ts`(1,913줄)의 **치트 등록 배열 삽입 지점 미실측**. 0a-14 착수 전 확인해 0c 계약 문서에 기재.
- [x] AS-OQ10 `[해소]`: `kind='dash'` 가 대시 방향 폴백(`world.ts:1647-1653`)을 재사용하는가 복제하는가 — **재사용으로 확정**(ADR-0041 이 "대시와 동일 규칙"을 명시). `world.ts` 편집이므로 **0a-10 의 세 번째 지점**으로 편입.
- [x] AS-OQ11 `[해소]`: 꼬리 폴드 ①(런타임 정수 4개)과 ②(장착 id 2개)의 상대 순서 — **① 먼저, ② 나중으로 영구 고정**(런타임 상태가 config 스냅샷보다 앞서는 기존 폴드 순서와 정합).
- [x] AS-OQ12 `[해소]`: M-③ 판정 기준값 출처 — **0a 착수 전 커밋 스냅샷과 대조**(상수로 박지 않는다).
- [ ] AS-OQ13 `[0b 착수 시 결정]`: **`strike` 의 대조군 차분을 무엇으로 좁히는가.** 대조군에서도 자동공격 투사체가 생기므로 차이값을 봐야 한다. `def` 가 선언한 투사체 kind 로 좁힐지, 총 개수 차분으로 볼지 미정. 전자가 더 강하나 `observable` 에 kind 정보가 없다.
- [ ] AS-OQ14 `[0a 에서 명시 확인]`: **버프 잔여 틱이 런 중 자연 감소해 0으로 돌아오면 조건부 폴드가 런 도중 켜졌다 꺼졌다 한다.** `planetMultCenti`·`catalysts`(둘 다 런 내내 상수)와 성격이 다르고 선례가 없는 형태다. 결정론은 유지되나(모든 참여자가 같은 상태를 본다) 0a 에서 한 번 명시적으로 확인할 가치가 있다.
- [ ] AS-OQ15 `[착수 시점 미정]`: Follow-up 3(EF `activeSlots ⊆ unlocked(skillInvest)` 검증)을 밸런스 패스와 묶을지 별도 레인으로 둘지.
