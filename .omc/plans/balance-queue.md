# 밸런스 큐

밸런스·수치 조정을 **출시 직전 일괄 처리**하기로 한 항목의 등재처다(전역 규율: "밸런스는 출시
직전 일괄 조정"). 구현 레인이 "지금 고치지 않고 미룬다"고 판단한 수치는 **반드시 여기에 적는다** —
적지 않으면 그 판단은 미룬 것이 아니라 잊은 것이다.

- 이 파일은 `.omc/plans/commission-system-consensus.md` §Phase 0 산출물 2 로 생성됐다(2026-07-31).
- **등재 형식**: 축 · 현재값(또는 "미정") · 왜 미뤘는가 · 무엇을 재야 정할 수 있는가 · 출처.
- **여기 있는 값을 코드에 하드코딩하지 마라.** 상수 모듈을 거친다.

---

## 의뢰서 시스템 (2026-07-31 등재)

출처: `.omc/plans/commission-system-consensus.md` rev7 · `.omc/specs/deep-interview-commission-system.md`

### A. 구간 승계가 만드는 비대칭·곱셈

| # | 축 | 현재 판정 | 재야 할 것 | 출처 |
|---|---|---|---|---|
| R10 | 플레이어 `aux0`/`aux1`(기체 시그니처 런타임) 승계의 6기체 비대칭 | **승계로 확정.** 시그니처는 런 속성이지 무대 속성이 아니다 | 6기체 중 **말로우만 승계가 불리**하다(적립 지연 피해가 무대를 넘어 터진다). 계급별 구간 수 × 기체별 클리어율로 비대칭 폭 실측 | 계획 §A-5 기체별 표 · R10 |
| R10b | 해츨링 `aux0`(kills 스냅샷) ↔ `state.kills` **결합** | 둘 다 승계라 정합 | 구간이 늘 때 출격 주기가 의도대로 유지되는지 | 계획 §A-5 |
| R13-a | 보급 습격 **2기/구간** | `tick` 구간별 0 리셋의 귀결. 페이싱류라 무대 단위가 옳다고 판정·수용 | N구간 런의 총 보급 기수가 난이도에 미치는 영향 | 계획 §A-5 `tick` 리셋 항 |
| R13-b | 캡스톤 레이저 무료 1발/구간 (`tick % LASER_PERIOD`) | 동상 | 동상 | 동상 |
| R13-c | 해츨링 무료 1단계/구간 (`tick % period`) | 동상 | 동상 | 동상 |
| R13-d | 오염 위상 불연속 (경미) | 동상 | 체감 여부만 | 동상 |

### B. 실값 미정 — 착수 중 정하되 최종 조정은 여기서

| # | 축 | 현재 | 무엇이 정하는가 |
|---|---|---|---|
| C1 | 계급별 **구간 수**(정기/우선/특급/최종) | 플레이스홀더 2/3/4/5 | **상한은 PC 실측 게이트**(EF CPU 예산). 그 안에서의 선택이 Phase G 계측 |
| C2 | **구간당 틱 상한** | 플레이스홀더 9,000 | 동상. 초과 시 축소 순서: ①최종 계급 구간 수 → ②특급 구간 수 → ③구간당 틱 상한 |
| C3 | 계급별 **발령 확률** | 미정 | 의뢰 콘텐츠 소비 속도 |
| C4 | **보관 상한**(지시 수신소) | 미정 | "못 깨는 고계급 한 장을 안고 갈지"의 긴장이 성립하는 크기 |
| C5 | **발령 시도 시간당 상한** | **미정 — ⚠️ 착수 조건으로 승격** | 아래 §C 참조 |
| C6 | `mark_commission_active` **시간당 호출 상한** | 미정 | C5 와 **같은 표에 나란히** 둔다(두 축이 어긋나면 방어가 벌어진다) |
| C7 | 회수 **유예** 실값 | 미정 | UX 요건만 남았다(정직한 플레이어가 로딩 중 회수당하지 않을 만큼). 방어 요건은 아니다(계획 §D8 ③) |
| C8 | 리플레이 **TTL** | 미정(침공은 48h) | PC 계약에서 정한다 — 밸런스가 아니라 유실 구제 ↔ "성공 리플레이 쟁여두기" 트레이드오프 |
| C9 | 현상금 표적 **도주 임계**(HP 임계 · 생존 틱) | 미정 | 도주가 "아깝게 놓쳤다"로 읽히는 구간 |
| ~~C10~~ | ~~정예 소집령 **겹침 임계**(직전 정예 처치 이후 경과 틱)~~ | **실측 완료·닫힘(2026-08-01, 아래 C-ter2 참조)** | ~~파워업 0 전제의 별도 난이도 기준선(ADR-0043)~~ |
| C11 | 유니크 **중복 지급률** | 중복 허용 확정 | 계급별 유니크 풀 크기 대비 중복률 — 체감 보상 0 이 되는 빈도 |
| C12 | `CAP_COMMISSION_CREDITS` / `CAP_COMMISSION_MINERALS` | 미정 | 최종 지시 계급의 **최대 확정 보상 + 여유**. 상수 모듈과 SQL 미러를 **함께** 갱신해야 한다 |

### C. ⚠️ 착수 조건으로 승격된 항목

**C5(발령 시도 시간당 상한)는 밸런스 유예가 아니라 PB 착수 조건이다.**

근거 — 발령 빈도를 묶는 겹이 **이것 하나뿐**이기 때문이다:

- 보관 상한(C4)은 **재고**만 캡한다. 소비하면 다시 찬다 — "잡았다"를 반복 신고하면 상한 안에서
  무한 재충전이 성립한다. **재고 캡과 빈도 캡은 다른 축이다**(계획 pre-mortem ⑧ 방어 ②).
- 발령 자격 자체가 **클라 주장**이다(일반 PvE 는 리플레이를 안 낸다 — ADR-0026 · ADR-0044
  §잔여 위험 2).
- **같은 값이 제출 빈도 상한을 겸한다**(계획 §Phase C 실패 런 비용 축 · R19) — 실패 런도 전부
  제출하므로 이 상한이 EF CPU·DB 용량 예산의 유일한 문지기다.

따라서 **"실값 미정"으로 PB 를 착수하면 유일한 방어가 미조달인 채로 배포된다.** PC 실측 게이트가
산출하는 시간당 제출 건수 상한 **안에** 이 값이 들어야 하며, 그 확인 전까지 잠정값을 보수적으로
(작게) 잡는다.

### C-bis. 실측 완료 — `MIN_BOSS_KILL_TICKS` (2026-08-01, 서버 축 레인)

**확정값 60틱(1초).** 서버 계약 rev3 §13-⑫ 가 착수 조건으로 요구한 실측을 수행하고 닫는다.

| 항목 | 값 |
|---|---|
| 관측 최소 승리 `finalTick` | **195틱(3.25초)** — toxar / ship2(arccaster) / stage1 / seed `0x5005` |
| 분위수 (승리 156건) | p1 215 · p5 272 · p10 440 · p50 782 |
| 표본 | 6행성 × 7기체(typeId 0~6) × 5시드, stage 1, Lv100 만렙 표준 최대화력 빌드, 총 210런 |
| 채택값 | **60** (관측 최소의 약 31%) |

**왜 관측 최소보다 훨씬 낮게 잡는가.** 위조 처리량을 실제로 묶는 것은 이 상수가 아니라
`CAP_ISSUE_ATTEMPTS_PER_HOUR`(20/h) 다. 계약 §4-4 가 정한 상한은 `20/h` 와
`실시간 ÷ MIN_BOSS_KILL_TICKS` 중 **작은 쪽**인데, 60틱이면 후자가 시간당 3,600 이라 **전자가
항상 먼저 문다.** 즉 이 값을 낮추는 방어 비용은 사실상 0 이고, 이 상수가 실제로 하는 일은
`finalTick: 1` 같은 퇴화 입력이 쿨다운 누적기를 **문자 그대로 0** 으로 만드는 것을 막는 것뿐이다.
반면 값을 높이는 비용은 **정직한 속공 런의 발령 거부**이며 이는 되돌릴 수 없는 사용자 피해다.
비대칭이 명백하므로 보수적으로(작게) 잡는다.

> placeholder 였던 3,600 은 **관측 최소의 18.5배**였다. 그대로 배포했다면 속공 런은 의뢰서를
> 한 장도 못 받았을 것이고, 증상은 "가끔 의뢰서가 안 나온다"로만 보여 진단이 매우 어려웠을 것이다.

**⚠️ 자인 — 이 측정이 덮지 못한 것 (둘 다 "실제 하한은 더 낮을 수 있다"를 가리킨다):**

- **niflheim(추격 모드)은 측정하지 못했다.** 승리 표본 0/35. 측정용 제네릭 파일럿이 무적 포식자
  (`kind='boss'`, `aux0=0`)를 추격 표적으로 삼는데 그 모드는 무적 포식자 접촉이 iframe 무시 즉사라
  (`src/sim/modes/chase.ts:50-54`) 전멸한다. **측정 실패이지 "니플헤임이 느리다"가 아니다.**
  전용 파일럿 로직이 필요하며 미착수다.
  > **2026-08-01 정정 — 진짜 근인은 파일럿 로직이 아니었다.** 반격 장치가 `isPlayerTargetable`
  > 에서 빠져 있어 **승리 조건 자체를 조준할 수 없었다**(아래 §밸런스 패치 레인 1차). 조준을
  > 고치고 봇에 목표물 추적을 넣자 니플헤임이 정상 측정된다(클리어율 74.7%). **이 절의
  > `MIN_BOSS_KILL_TICKS` 실측은 니플헤임을 빼고 정해진 값이므로, 그 무대를 포함해 다시 재면
  > 관측 최소가 더 내려갈 수 있다** — 채택값 60 은 관측 최소의 31% 라 여유가 크지만, 재측정
  > 시에는 이 경로도 함께 돌려라.
- 시드 5개·표준 빌드 1종만 스윕했다. 관측 최소 195 는 이론적 하한이 아니다.
- toxar 는 16/35 승률(오염 확산 패배). 나머지 4행성은 35/35.

**부수 발견(이 레인 범위 밖, 등재만 한다):** toxar stage1 이 압도적으로 빠른 원인은 **보스 HP 가
침략 단계로 스케일되지 않는데** 장비·스킬은 Lv100 까지 스케일되기 때문으로 보인다. 만렙 캐릭터가
낮은 단계를 "트윙크 스톰프"하는 구조다. 의도된 규칙일 수 있으나 **난이도 곡선 축에서 한 번
판정할 가치가 있다.**

### D. 계측 산출물 (Phase G) — **실측 완료(2026-08-01)**

- **계급별 대표 구간 조합의 96시드 클리어율·평균 소요** — ADR-0046 결과 절이 "난이도 기준선을
  조합 단위로 재야 한다. 모드별 단일 계측에서 자동 파생되지 않는다"를 명시했다.
- **정예 소집령은 별도 기준선**을 갖는다(ADR-0043 결과 절) — 파워업 0 전제라 다른 주문·행성
  모드의 계측에서 파생되지 않는다.
- **PC ↔ G 의 관계는 순서가 아니라 포함**이다: PC 가 상한, G 가 그 안에서의 선택. G 가 목표
  밴드에 못 앉히면 상수를 되돌리는 것이 아니라 **상수 밖 축**(구간별 스테이지 난이도 · 의뢰 보스
  HP · 제약 강도)을 조정한다. 그래도 안 되면 PC 를 다시 **측정**한다 — 상한을 협상하지 않는다.
- **실측 정본**: `.omc/research/commission-band-2026-08-01.md`. 계측 인프라(재사용 가능):
  `src/bench/commissionBench.ts` + 영구 회귀 게이트 `tests/commissionBandMeasure.test.ts`.

### C-ter3. 미계측 — 정예 소집령의 겹침 압박이 실제로 발현되는가 (2026-08-01 등재)

C-ter2 의 수정(`COMMISSION_ELITE_WAVE_SEGMENTS = 1`)은 보스 전 처치 요구를 killGoal
**109 → 10 으로 10.9배** 낮췄다. 틱 상한·클리어율 두 기준은 만족하지만, **겹침이 구간당 몇 번
발현되는지는 재지 않았다.**

- 우려: killGoal 10 이면 `COMMISSION_ELITE_OVERLAP_MAX = 6` 겹침이 구간당 1~2회밖에 안 뜰 수
  있다. 그러면 ADR-0043 이 "잡몹 소거로 사라진 런 길이 캡의 대체물"로 지정한 압박 누적이
  **수치상 통과하면서 체감상 사라진다** — 지표가 초록인 채로 주문 정체성이 비는 형태다.
- 착수 조건: `commissionBench` 에 겹침 발생 횟수(동시 정예 수의 시계열 최댓값·`cap` 상승 횟수)
  카운터를 추가해 96시드로 재측정. 발현이 실질적이지 않으면 `COMMISSION_ELITE_WAVE_SEGMENTS`
  를 올리는 대신 겹침 축(`_DELAY_TICKS` 단축 등)으로 밀도를 앞당기는 쪽을 먼저 본다.
- **이것은 자인이지 미조달이 아니다** — 착수 조건과 다음 수가 위에 배정돼 있다.

### C-ter2. C10 실측 완료 — 정예 소집령이 서버 틱 예산을 초과하고 있었다 (2026-08-01, Phase G)

96시드 계측(`.omc/research/commission-band-2026-08-01.md`)이 **C10 이 기다리던 겹침 임계
실측**을 내놓았는데, 그 값이 "정하는" 문제가 아니라 **현재 배선이 방어 목적(틱 예산) 자체를
못 지키고 있었다**는 결함으로 드러났다.

**증상**: 정예 소집령(계급2 대표) 96시드 중 **34시드**가 `COMMISSION_SEGMENT_TICK_CAP`(9,000틱)
을 넘겼다(니플헤임 제외해도 27시드) — 최대 **53,441틱(상한의 5.9배)**. 클리어율 27.1%.

**근인**: 정예 소집령은 잡몹 유입을 0으로 막는데(ADR-0043) 보스 전 웨이브 세그먼트 상한
(`COMMISSION_WAVE_SEGMENTS_PER_SEGMENT = 3`, killGoal 합계 109)은 카드 잡몹 밀도를 전제로
설계된 값을 그대로 물려받고 있었다. 정예 겹침 소환(최대 6기 동시)만으로 109킬을 채우려니
성장 없는 고정 화력 파일럿에게는 수천~수만 틱이 걸렸다 — **압박(겹침) 파라미터의 문제가
아니라 애초에 채워야 할 킬 수가 그 공급 방식에 비해 과도했다.**

**수정**: `COMMISSION_ELITE_WAVE_SEGMENTS = 1`(신설, `src/run/commissionConstants.ts`) —
정예 소집령만 보스 전 웨이브 세그먼트를 1개(killGoal 10)로 낮췄다. `buildRunConfig`
(`src/run/runConfig.ts`)와 서버 재검증(`supabase/functions/verify-commission/verifyCommissionCore.ts`)
**양쪽에 대칭으로 배선**했다 — 한쪽만 고치면 클라·서버가 다른 `maxSegments` 로 재실행해
정직한 정예 소집령 런까지 `outcome-mismatch` 로 거부된다.

**조정 전/후 대조(같은 96시드)**:

| 구분 | 클리어율(니플헤임제외) | 구간틱 p99(니플헤임제외) | 상한초과(니플헤임제외) |
|---|---|---|---|
| 조정 전 | — | — | 27시드 |
| 조정 후 | **61.4%** | **3,139틱(상한의 34.9%)** | **0** |

두 판정 기준을 함께 만족했다: ① p99 < 상한(80% 미만 여유) ② 클리어율이 `chain`(92~96%대)보다
뚜렷이 낮되(ADR-0043 정체성) 40~85% 목표 밴드 안(완주 가능성이 살아 있음). **`COMMISSION_ELITE_OVERLAP_MIN`/`_MAX`/`_REGROUP_TICKS`/`_DELAY_TICKS`(겹침 압박 파라미터)는 건드리지
않았다** — 단일 축(`COMMISSION_ELITE_WAVE_SEGMENTS`)만으로 두 조건이 동시에 만족돼 최소 변경
원칙을 지켰다. 겹침 임계 자체의 목표값은 이제 "정하지 않아도 되는" 상태다(방어 목적이 이미
달성됐고, 추가로 더 조이는 것은 순수 체감 밸런스 문제로 격하된다).

**해소 안 된 것 — 참고만**: 니플헤임(추격 모드) 봇 한계로 26/96런이 참조봇으로는 측정 불가.
`MIN_BOSS_KILL_TICKS` 실측(C-bis)과 같은 한계다. 실제 사람 플레이의 정예 소집령 니플헤임
난이도는 이 계측으로 알 수 없다.

---

## C-ter. 보안 검토가 남긴 잔여 위험 (2026-08-01, 서버 축 레인)

### 로드아웃 축에는 서버 판정이 없다 — 구조로 못 막는 축

`verify-commission` 게이트 3 은 제출 `config.loadout` 을 `commission_runs.loadout_sealed` 와
대조하는데, **그 기준값 자체가 `consume_commission(p_loadout)` 으로 클라가 넣은 값**이다. 즉
대조가 닫는 것은 "출격 **후** 편집"뿐이고 출격 **전** 위조는 열려 있다. 그 구멍의 대체 방어로
지목돼 있던 게이트 4(미인가 의뢰 전용 유니크)는 `COMMISSION_EXCLUSIVE_UNIQUE_BITS` 가 비어 있어
**구조적으로 아무것도 보지 않는다** — 자인 둘이 겹쳐 상쇄된 상태였다.

**배정된 대체 방어**(자인만 하고 미조달로 남기지 않는다):

| # | 방어 | 무엇을 묶는가 |
|---|---|---|
| ① | EF 게이트 6 **allowlist 재조립** | 코어 스탯·무대·자원 배율 축. 로드아웃 위조의 상한이 "장비로 낼 수 있는 화력"으로 내려온다 |
| ② | `settle_commission` 의 **런 파생분 틱 비례 캡** | 화력 인플레가 재화로 환전되는 폭 |
| ③ | 발령 빈도 상한 20/h + 의뢰서 1장당 지급 1회 | 확정 지급물의 취득 속도 |

**남는 잔여 위험**: "정상 범위를 벗어난 장비로 어려운 의뢰를 깬다". 서버가 장비 원장을 갖기
전까지(ADR-0028 이 연기한 축) 구조로 못 막는다. **출시 전 재검토 대상**이며, 착수 조건은
"서버가 장비 인벤토리를 권위로 들 수 있는가"다.

### 발령 실패는 플레이어에게 보이지 않는다

`issue_commission_for_run` 과 트리거 함수가 각각 서브트랜잭션이라 예외가 나면 `raise warning`
뒤 조용히 미발령으로 끝난다(정산은 살아남는다 — 그것이 이 구조의 목적이다). **자인**: 그래서
발령 실패의 증상은 "가끔 의뢰서가 안 나온다"뿐이다. 배정: 출시 전 `warning` 발생률 관측 수단을
세운다(현재 미조달).

---

## 밸런스 체크 하네스 1차 실측 (2026-08-01)

출처: `.omc/research/balance-check-baseline-2026-08-01.md` (재생성 `pnpm balance`, 절차 정본
`docs/balance-check.md`). **표본 12,600런** = 격자 840셀(행성 6 × 기체 7 × 레벨 20) × 시드 15,
소요 583초.

> 1차 측정(13,440런)의 지표 하나가 고장나 있었다 — §B2 정정 참조. 아래 수치는 **수정 후
> 재측정본**이다.

> ⚠️ 이 레인은 **측정 도구**를 만든 레인이다. 아래 수치는 **하나도 조정하지 않았다** —
> ADR-0037("난이도 곡선은 적 축에서만")과 전역 규율("밸런스는 출시 직전 일괄 조정")을 따라
> 등재만 한다.

| # | 축 | 실측 | 판정 | 무엇을 재야 정할 수 있는가 |
|---|---|---|---|---|
| B1 | **행성 간 클리어율 폭** | ~~81.3pp~~ → **30.6pp**(2026-08-01, 니플헤임 정상화) | 게이트 없음(신설 후보). 남은 드라이버는 **크라스 100.0%·아르케 96.0%** 쪽 | 행성 선택이 난이도 선택이 되어도 되는가. 되면 안 되면 적 축에서 좁힌다 |
| B2 | ~~니플헤임 **보스도달 0.0%**~~ → **측정 도구 결함이었다** | — | ❌ 취소 | 아래 §B2 정정 참조 |
| B3 | **장비유입 0.92/런** → 1.00 · **판정 완료**(드랍 1.55 중 0.55 유실 — 두 축 다) | 목표 2~3 (ADR-0035 §3.1)의 **약 1/3** | ❌ 게이트 실패 | 봇이 못 줍는 것인지(수거 반경·카이팅) 드랍 자체가 적은 것인지. `state.loot` 는 **수거분만** 센다 |
| B4 | **클리어초 33.8s** · 행성별 15.7~102.4s | par 95초의 0.36배, 행성 간 **6.5배** | ❌ 게이트 실패 | 10시간 산술(ADR-0035)의 분모가 런 길이다. 행성마다 6.5배면 **"런 par" 라는 단일 값이 성립하는지**부터 판정해야 한다 |
| B5 | 곡선 **전 구간 71.6~79.5%** | 20개 밴드 전부 60~80% 안 | ✅ **통과** | 1차에서 Lv95=80.5% 로 0.5pp 초과했으나 재측정에서 79.5%. **시드 15개는 SE ≈ ±3.8pp 라 밴드 경계를 가르지 못한다** — 경계 근처 판정이 필요하면 축을 좁혀 표본을 키워라 |
| B6 | **로스터 편차 5.1pp** (sd 1.6pp) | striker 72.4% ↔ bubble 77.5% | ✅ **건강 — 조정 불요** | 기체 축은 이번 실측에서 문제 없음. 여기 등재는 "재봤고 괜찮았다"의 기록이다 |
| **B7** | **Lv100 타임아웃 10.6%** (그중 **니플헤임 44.8%**) | 다른 밴드는 0~1.1% | ✅ **해소(2026-08-01)** → 3.9%. 근인은 밸런스가 아니라 조준 결함이었다 — 아래 §밸런스 패치 레인 1차 참조 | ~~아래 §B7 참조~~ |

**해석 주의**: 봇은 최적 플레이가 아니고 액티브 스킬을 발동하지 않는다. 그래서 절대값보다
**축 간 상대 난이도(B1·B4)** 가 이 표에서 신뢰도가 높다.

### B2 정정 (2026-08-01) — "니플헤임 보스도달 0%" 는 밸런스 신호가 아니라 **지표의 결함**이었다

1차 실측이 니플헤임 보스도달 0.0% 를 **구조 신호**로 등재했으나, 원인을 파고들자 측정 도구가
틀렸던 것으로 판명됐다.

- `bossReachRate` 를 `state.wave.boss`(웨이브 디렉터가 보스 세그먼트인가)로 정의했다.
- 추격 모드는 포식자가 `createWorld` 에서 이미 스폰되고(`placeChaseCourse`), 세그먼트 전진은
  **대피소 도달**이며, 승리는 **반격 장치 5개 파괴 → 취약화 → 처치**로 세그먼트 진행과 무관하게
  성립한다. 즉 **승리 런조차 보스 세그먼트에 닿기 전에 끝난다.**
- 증거: 니플헤임 승리 **422건이 전부 `bossReachRate=0`** 이었다. 승리가 곧 보스 처치인데
  관측기는 한 번도 보스를 보지 않았다.

**수정**: 판정을 "교전 가능한 보스 엔티티가 실재하는가"(`cell.ts` `bossEngageable`)로 바꿨다.
추격 모드는 취약화 전(`aux0=0`) 무적 포식자를 제외한다. `tests/balanceHarness.test.ts` 의
**"승리 런은 보스를 관측한다"** 불변식이 재발을 막는다(뮤테이션 검증 완료 — 옛 게이트로 되돌리면
그 테스트만 빨개진다).

**재측정이 드러낸 진짜 신호** (2행성 840런 예비 측정):

| 축 | 실측 | 무엇을 뜻하는가 |
|---|---|---|
| N1 | 니플헤임 **보스교전률 = 승리율**(21.4% = 21.4%) | **취약화에 성공하면 거의 항상 이긴다.** 병목은 보스전이 아니라 **그 앞 단계**다 |
| N2 | 병목 = **반격 장치 5개 파괴**(`CHASE_COUNTER_DEVICE_COUNT`) | 무적 포식자에게 쫓기면서 장치 5개를 깨는 구간이 실패의 전부다. 조정 후보는 장치 수·HP(40)·배치 링 반경(1100)·포식자 접근 하한(900) — **전부 `chase.ts` 의 `TODO(밸런스)` 플레이스홀더다** |

⚠️ 여기 계수는 전부 "출시 전 일괄 튜닝" 표시가 붙은 플레이스홀더이므로, **이 레인에서 손대지
않는다.** 재야 할 것은 "장치 5개 파괴 시도 중 몇 %가 죽는가"이고, 그건 `--planets=2` 로 좁혀
표본을 키우면 바로 나온다.

---

## B7. Lv100 타임아웃 10.6% — 만렙 추격 런이 **끝나지 않는다** (2026-08-01 신규)

| 축 | 값 |
|---|---|
| Lv100 타임아웃 | **10.6%** (67/630) — 다른 19개 밴드는 전부 0~1.1% |
| 그중 **니플헤임(추격)** | **44.8%** (Lv100 니플헤임 런의 절반 가까이) |
| 카르곤 | 13.3% · 톡사르 5.7% · 베르단/아르케/크라스 **0%** |
| 타임아웃 정의 | 18,000틱(=300초) 상한 도달. 게임 자체엔 타임아웃이 없다 — **이길 수도 질 수도 없는 런**이라는 뜻 |

**왜 만렙에서만 나오는가(가설, 미검증)**: 만렙 표준 빌드는 방어가 충분해 무적 포식자에게 죽지
않는데, 반격 장치 5개를 다 깨지 못하면 승리 조건이 열리지 않는다 → 무한 도주. 즉 B2 의 병목
(반격 장치)과 **같은 뿌리이되 반대 증상**이다. 저레벨은 죽어서 끝나고, 만렙은 안 죽어서 안 끝난다.

**이것이 니플헤임 승률 18.6% 를 부분적으로 설명한다** — 지는 게 아니라 **끝나지 않는 것**이
상당수다. 승률만 보고 "어렵다"로 읽으면 처방이 틀린다.

**재야 할 것**: 타임아웃 런에서 살아남은 반격 장치 수. 0개면 "취약화했는데 못 잡는 것"이고,
1개 이상이면 "장치를 못 깨는 것"이다 — 처방이 완전히 갈린다.

> ⚠️ **이 결함은 게이트가 `scope: 'overall'` 이던 동안 초록이었다**(전체 평균 0.8%). 국소
> 이상을 전체 평균이 가린 사례이며, 그래서 `timeoutRate` 게이트를 `scope: 'level'` 로 바꿨다.
> **새 지표에 목표 밴드를 달 때 "전체 평균으로 성립하면 충분한가"를 먼저 물어라.**

---

## 밸런스 패치 레인 1차 (2026-08-01) — B7·B2 해소, B1 축소, B3 판정

정본: `.omc/research/balance-check-baseline-2026-08-01.md`(갱신본). 이 절은 **무엇이 닫혔고
무엇이 왜 안 닫혔는지**만 적는다.

### 해소 — B7·B2·B1 의 뿌리는 **하나의 sim 결함**이었다

추격 모드의 유일한 승리 조건인 **반격 장치가 `isPlayerTargetable` 에서 빠져 있었다.** 이 게임의
사격은 전부 자동 조준이고 `autoAttack` 은 `input.aim` 을 쓰지 않으므로(그 값은 렌더용
`player.angle`), 조준 목록에 없는 오브젝트는 **플레이어가 의도적으로 부술 수단이 없다.** 장치는
스쳐 가는 유탄이 우연히 맞을 때만 부서지고 있었다.

이 저장소가 같은 부류를 겪은 것은 **세 번째**다(침공 실드 발생기 · 보호막 국면 코어 · 반격 장치).
공통 증상은 "맞기는 하지만 조준되지는 않는다"이고, 공통 결과는 **그 무대가 수학적으로 클리어
불가**다. `world.ts` `isPlayerTargetable` 주석의 "세 목록은 항상 같이 바뀐다"에 이 사례를 더했다.

| 근거(니플헤임 2,660런) | 값 |
|---|---|
| 타임아웃 런의 잔존 장치 | **63/63 이 1개 이상** (0개는 0건) |
| 패배 런의 잔존 장치 | 2,102건 중 0개는 **2건뿐** |
| 승리 런 | 495건 전부 0개(정의상) |

즉 B7 이 요구한 계측("타임아웃 런에서 살아남은 장치 수")의 답은 **1개 이상 = 장치를 못 깨는
것**이었고, 그 이유는 밸런스가 아니라 조준이었다.

**부수로 오토파일럿도** 장치를 이동 표적으로 삼지 않아 이 무대는 **측정 자체가 성립하지
않았다.** 기준선의 "니플헤임 18.6%"는 난이도가 아니라 계측 결함이다 — B2 정정과 같은 부류가
같은 무대에서 한 번 더 났다.

### 조준을 고치자 드러난 것 — 이 무대에는 **난이도 축이 없었다**

수정 직후 니플헤임은 **전 레벨 100.0% · 클리어 5.4초**가 됐다. 장치 HP 가 단계·레벨과 무관한
**고정 40** 이라 소요의 거의 전부가 장치 사이 이동 시간이었다. "어려웠던" 것이 아니라 **조절할
난이도가 애초에 없었다.**

수정: `chaseCounterDeviceHp(stage) = 14000 × (1 + (stageHpMult(stage) − 1) × 0.15)`. 적 HP 와 같은
단계 앵커를 타되 기울기를 감쇠한다 — 감쇠 없이(계수 1.0) 재보니 Lv5 95.4% → Lv90 65.9% 로 30pp
기울었다. 적 앵커는 **수와 밀도가 함께 늘어나는 무리**에 보정된 값인데 장치는 집중 화력을 혼자
받는 **단일 표적**이라 같은 기울기가 과하다.

| 축 | 수정 전 | 수정 후 |
|---|---|---|
| 니플헤임 클리어율 | 18.6% | **74.7%** |
| 행성 간 클리어율 폭(B1) | 81.3pp | **30.6pp** |
| Lv100 타임아웃(B7) | 10.6% | **3.9%** |
| 니플헤임 Lv100 타임아웃 | 44.8% | 0.0% |

회귀 가드: `tests/chaseMode.test.ts` (c-2) 조준 가능성 · (c-3) 봇 수렴. **둘 다 뮤테이션 검증
완료**(각 수정을 되돌리면 그 테스트만 빨개진다). (c-3) 은 처음에 "거리가 줄었다"로 썼다가
**뮤테이션을 통과해 버려** "접촉 거리까지 수렴했는가"로 고쳤다 — 카이팅만 하는 봇도 우연히
가까워진다.

### ⚠️ 이 수정이 드러낸 것 — 기준선의 PASS 두 건은 **결함이 떠받치고 있었다**

니플헤임이 정상 측정되자 게이트가 3실패 → **5실패**가 됐다. 늘어난 것이 아니라 **가려져 있던
것이 보이는 것**이다:

- `clearRate` 는 니플헤임 18.6% 가 평균을 끌어내려 60~80% 안에 있었다. 정상화되자 **전 레벨
  82~86%** 로 위쪽 이탈 — 이 게임은 실제로는 **너무 쉽다**.
- `runLevelUps` 5.1 은 니플헤임의 **끝나지 않는 런**이 부풀린 값이었다. 정상화 후 4.55.

**교훈**: 국소 결함은 게이트를 빨갛게만 만드는 것이 아니라 **초록으로도 만든다.**

### B8 (신규) — 의뢰 니플헤임 구간만 무겁다 (전제 불일치)

`tests/commissionBandMeasure.test.ts` 실측: 계급3/chain 전체 72.9% vs 니플헤임 제외 91.1%
(**격차 18.2pp**, 니플헤임 51/96). 원인은 **두 전제의 불일치**다 — 의뢰 다구간 런은 성장 없는
**고정 화력 파일럿**이 도는데(`commissionBench`), 장치 HP 는 **레벨 대응 표준 빌드**를 전제로
단계 앵커를 탄다.

- 착수 조건: 고정 화력 파일럿의 실효 DPS 를 재고, 장치 HP 를 **단계**가 아니라 **런 입력의
  화력 추정치**로 걸 수 있는지 판정한다.
- 다음 수(후보): §C-ter2 선례처럼 의뢰 전용 상수로 가르거나, 감쇠 계수를 더 낮춘다.
- 지금은 회귀 가드만 걸어 뒀다(`NIFLHEIM_GAP_MAX_PP = 25` — 결함 시절 격차 약 48pp 는 잡는다).

### B3 판정 완료 — **드랍도 적고, 줍지도 못한다**(두 축 다)

`lootGround`(바닥 잔존) 지표를 신설해 7,140런으로 갈랐다. 질문("봇이 못 줍는 것인지 드랍 자체가
적은 것인지")의 답은 **둘 다**다:

| 행성 | 수거 | 바닥 잔존 | 합계(=실드랍) |
|---|---|---|---|
| 카르곤 | 1.11 | **1.14** | 2.25 |
| 톡사르 | 1.01 | **1.11** | 2.12 |
| 베르단 | 0.94 | 0.32 | 1.26 |
| 니플헤임 | 0.93 | 0.33 | 1.26 |
| 아르케 | 0.99 | 0.23 | 1.21 |
| 크라스 | 1.01 | 0.15 | 1.17 |
| **전체** | **1.00** | **0.55** | **1.55** |

- 수거분이 어느 행성에서나 **정확히 1.00 근처**인 것은 그것이 **보스 확정 드랍**이기 때문이다
  (`compact` 가 `state.loot` 에 직접 넣는다). 즉 **엘리트 바닥 전리품은 사실상 전량 유실된다.**
- 실드랍 합계 1.55 는 수거가 완벽해도 목표 하한 2 에 **못 미친다** → 드랍률 축도 올려야 한다.
- 수거 축은 **측정 결함이기도 하다** — 봇은 전리품을 이동 표적으로 삼지 않는다(반격 장치와
  같은 부류). 다만 여기서는 **전역 오토파일럿 변경**이 필요하고, 그것은
  `src/bench/rosterBench.ts` 의 "시드·거동·출력 형식 불변" 계약과 충돌한다. **착수 조건 =
  그 재현 계약을 어떻게 다룰지 먼저 정한다.**

### 미해소 — 이번 레인의 수치 축으로는 못 닫는 것

| 게이트 | 실측 | 왜 이번에 못 닫았는가 |
|---|---|---|
| `clearRate` 82~86%(전 레벨) | 위쪽 이탈 | 드라이버는 **크라스 100.0% · 아르케 96.0% · 베르단 92.1%**. 행성별 난이도 스칼라가 **존재하지 않는다**(`PlanetContent` 에 그런 필드가 없다) — 로스터·카드풀·모드 계수를 무대별로 손대야 하며 **무대마다 이번 레인 크기의 별도 레인**이다 |
| `clearSec` 34.1s(밴드 60~190) | 아래쪽 이탈 | **6행성 중 5개가 15~30초**고 카르곤만 102초다. B4 가 물은 "런 par 라는 단일 값이 성립하는가"의 답은 **성립하지 않는다** 쪽에 가깝다. ⚠️ **밴드를 고치는 것은 승인 사항이라 손대지 않았다** — 아래 §B4 판정 참조 |
| `lootPerRun` 1.00 | 위 B3 | 드랍률 축 + 수거 축(계약 충돌) 둘 다 필요 |
| `timeoutRate` Lv100 3.9% | 잔여 | 10.6% → 3.9% 로 내렸으나 밴드(2%) 위. 니플헤임분은 0.0% 로 사라졌고 **남은 것은 카르곤·톡사르**다 — 뿌리가 다르므로 별도 계측이 필요하다 |

### ⚠️ 크라스·아르케에 **추격과 같은 signature 가 보인다**(미착수, 1순위 후보)

- 크라스(블록격파): 2,380런 **클리어율 100.0%**, 클리어 15.7s±**2.7**.
- 아르케(레이싱): 96.0%, 18.5s±**3.5**, 보스교전 100.0%.

편차가 이렇게 작고 실패가 0 에 가까운 것은 **실패 상태가 존재하지 않는다**는 뜻이고, 그것은
수정 직후의 추격(100.0%/5.4초)과 **같은 모양**이다. 두 모드의 계수도 `TODO(밸런스)`
플레이스홀더다. **다음 레인 1순위**로 등재한다 — 조준·승패 게이트가 의도대로 걸려 있는지부터
확인하고(밸런스 조정이 아니라 결함 점검), 그다음 난이도 축을 넣는다.

### §B4 판정 — 밴드 자체를 의심할 근거가 섰다(승인 필요, 미변경)

`clearSec` 밴드 60~190s 는 `RUN_SECONDS_PAR = 95` 의 0.63~2.0배다. 실측은 **카르곤 102s /
베르단 24.9s / 니플헤임 30.5s / 아르케 18.5s / 톡사르 23.4s / 크라스 15.7s** — 밴드 안에 드는
무대가 **6개 중 1개뿐**이다. 두 해석이 가능하고 **이 레인은 어느 쪽도 단독으로 정할 수 없다**:

1. 게임이 설계보다 6배 빠르다 → 전 무대의 런 길이(세그먼트 예산)를 늘려야 한다. 이 경우
   `runLevelUps`·`lootPerRun` 도 함께 개선된다(셋이 같은 뿌리다).
2. `RUN_SECONDS_PAR` 이 카르곤 한 무대를 보고 정해졌고, **무대마다 런 길이가 다른 것이 설계
   의도**다 → 밴드를 `scope: 'planet'` 으로 바꾸거나 par 를 무대별로 갖는다.

**게이트를 느슨하게 해서 통과시키지 않았다.** 1번이면 적 축(`SEGMENTS` 예산) 작업이고 2번이면
밴드 재정의라 처방이 정반대다.

---

## 무대 페이싱 + 파워업 하향 레인 (2026-08-01/02, 사용자 지시)

지시 두 가지: ①**행성별 보스전까지 1분 30초** ②**3택 수치 하향, 특히 공격력은 최대 +10%**.

### P0. 파워업 강화 폭 — 완료

피해 배율 상한 **+10%** 로 통일(이전 +35/45/30/28/22/20%). 그 외 축도 기준값의 5~15% 안으로
(이동 +12→6% · 대시 −20→−8% · 젬반경 +40→15% · 탄속 +20→8% · 최대HP +25/30/15 → +10/12/8 ·
레일 관통 +2→+1 · 액티브 계열 +4→+2). `desc` 문자열도 함께 고쳤다(화면에 그대로 나가는 값).

- 회귀 게이트: `tests/powerupMagnitude.test.ts` — `POWERUPS` **전수 순회**라 새 파워업이 자동으로
  들어온다. 값 목록을 하드코딩하지 않았다(같은 사실을 두 곳에 적지 않기 위해). 대조군 단언으로
  "아무 파워업도 피해를 안 올려도 초록"인 공허 상태를 막았다. 뮤테이션 검증 완료.
- **남은 이상치(구조상 못 낮춤)**: `twin-shot`·`spread-pellets`·`missile-salvo` 의 **탄환 +1**.
  기본 탄환 수가 1이라 한 픽이 DPS +100% 다 — 정수라 0.1발로 쪼갤 수 없다. 낮추려면 "기본
  탄환 수를 올리고 +1 의 상대 비중을 줄이는" 구조 변경이 필요하다. **등재만 한다.**
- ⚠️ **발사 간격은 정수 틱이라 퍼센트가 그대로 안 먹는다** — 기본 발칸 6틱에서 한 단계가
  1틱(−16.7%)이고 0.9든 0.92든 반올림하면 같은 5틱이다. "퍼센트를 낮췄는데 실측이 안 변한다"면
  이 반올림을 먼저 의심해라.

### P1. 보스 도달 90초 — **6무대 전부 달성**

`bossReachSec`(보스 교전 가능해진 첫 틱) 지표를 신설해 쟀다. **클리어초는 보스전을 포함해
페이싱 목표를 직접 재지 못한다** — 그래서 이 지표가 따로 필요했다.

| 행성(모드) | 조정 전 | 조정 후 | 움직인 축 |
|---|---|---|---|
| 카르곤(뱀서류) | 101.9s | **92.0s** | `SEGMENTS.killGoal` 합 240 → 211 |
| 베르단(수축) | 24.9s | **92.2s** | 초기 반경 2000→5500 · 스폰링 1400→3000 · 유예 120→240 |
| 니플헤임(추격) | 30.4s | **93.2s** | `CHASE_COUNTER_DEVICE_HP_BASE` 14,000→76,000 |
| 아르케(레이싱) | 14.2s | **93.1s** | `RACING_SECTION_LENGTH` 2,000→13,460 |
| 톡사르(오염) | 23.7s | **94.2s** | 확산 간격 150→1,700 · 노드 HP 30→62,000(단계 스케일) |
| 크라스(블록격파) | 13.3s | **90.0s** | `BLOCKBREAK_SECTION_LENGTH` 2,000→14,650 |

**부수 효과 — 게이트 2건이 함께 풀렸다**: `clearSec`(34.8s → 밴드 안)과 `runLevelUps`(4.7 → 밴드
안). 이전 레인 §B4 가 제기한 "셋이 같은 뿌리(런 길이)"라는 가설이 실측으로 확인됐다. **§B4 의
밴드 재정의 논의는 이제 불필요하다** — 밴드가 틀린 게 아니라 런이 짧았다.

**밀도 보존**: 레이싱은 구간당 분기 1개 고정이라 길이만 늘리면 13,460 유닛짜리 빈 복도가 됐을
것이다. `RACING_BRANCH_INTERVAL` 파생으로 바꿔 "몇 유닛마다 갈림길"이 유지된다(길이 2,000
이면 구 배치와 **바이트 동일**). 블록격파는 벽 행이 이미 코스 길이 파생이라 손댈 것이 없었다.

### P2. ⚠️ 오염 노드도 조준 밖이었다 — **같은 결함 4회차**

`isPlayerTargetable` 에 오염 노드(`destructible` + `CONTAMINATION_NODE_MARK`)가 없어 **정화
게이트를 의도적으로 진행시킬 수단이 없었다.** 추격 반격 장치와 같은 부류이고, 그때 술어를
조준 목록에 **직접 덧붙인 방식이 이 재발을 막지 못했다.**

- **탐지 단서**: 노드 HP 를 30 → 120(×4)으로 올렸는데 보스 도달이 23.7 → 26.1초로 **10%밖에**
  안 움직였다. **레버를 4배로 돌렸는데 결과가 10% 움직이면 그 레버는 연결돼 있지 않은 것이다.**
- 조준을 고치자 즉시 26.1s → **11.6s**, 승률 55.7% → **95.2%**(추격이 조준 수정 직후 보였던
  100%/5.4초와 같은 모양).
- **구조 수정**: `src/sim/modes/objective.ts` 신설 — 목표 오브젝트 판정을 한 곳에 모아, 새 모드는
  술어 하나만 추가하면 조준과 봇 추적이 **동시에** 성립한다. 4회 목록도 그 파일 헤더에 남겼다.

### P3. ⚠️ 이 레인이 드러낸 것 — **빠른 무대들은 쉬웠던 게 아니라 짧았다**

페이싱을 맞추자 클리어율이 무너졌다. 이건 페이싱 조정이 만든 새 문제가 아니라 **가려져 있던
난이도가 드러난 것**이다 — 레벨별로 균일하게(아르케 Lv5 1% / Lv100 11%) 죽는다.

| 행성 | 조정 전 승률 | 조정 후 승률 |
|---|---|---|
| 카르곤 | 73.3% | **73.3%** (원래 90초대라 거의 안 변했다 — 대조군) |
| 크라스 | 100.0% | 48.4% |
| 아르케 | 92.8% | **2.6%** |
| 베르단 | 86.4% | 17.9% |
| 톡사르 | 64.5% | 12.7% |
| 니플헤임 | 69.6% | 28.8% |

**카르곤이 대조군이다** — 유일하게 원래부터 90초대였고 유일하게 승률이 안 변했다. 나머지 다섯은
전부 "짧아서 안 죽었을 뿐"이었다.

**다음 레인 1순위 = 아르케(2.6%).** 패배 런의 중앙 생존이 44초(코스 절반)이고 레벨과 무관하게
균일하다 — 초당 치사율이 다른 무대보다 구조적으로 높다. **다만 봇 한계 의심이 크다**: 레이싱은
창이 계속 전진하는데 봇은 적을 피해 **뒤로** 카이팅하므로 후방 압박(`racingRearPressure`)에
갈릴 수 있다. **수치를 만지기 전에 "봇이 못 하는 것인지 사람도 못 하는 것인지"부터 갈라라** —
이 레인에서 같은 착각을 두 번(니플헤임·톡사르) 했다.

### P4. 미달·자인

- **톡사르는 목표를 세 번 시도해 겨우 맞췄고 측정이 불안정하다.** 3분 단일 행성 런에서는
  63.8초, 8분 전체 격자에서는 94.2초가 나왔다. 원인은 `bossReachSec` 이 **승리 런만** 보기
  때문이다 — 승률이 낮으면 "빨리 깬 런"만 표본에 남아 값이 **아래로 편향**된다. 승률이 회복되면
  이 수치는 다시 재야 한다.
- 톡사르의 런 길이 상한은 노드 HP 가 아니라 **실패 마감시계**(확산 간격 × 임계 셀)다. 확산
  간격을 150 → 1,700 으로 11배 늘려서야 90초대가 나왔는데, 이는 **확산 압박의 체감을 그만큼
  희석**한 것이기도 하다. 대안으로 "확산 cadence 는 유지하고 셀 상한·임계를 함께 올리는" 조합도
  실측했다(간격 500 · 상한 16 · 임계 110 → 54.2초 / 19.7%). **어느 쪽이 오염 무대의 정체성에
  맞는지는 밸런스가 아니라 설계 판단**이라 남긴다.
- `lootPerRun` 0.61 로 **더 나빠졌다**(이전 0.99). 런이 길어져 바닥 전리품이 더 많이 버려진다 —
  §B3 의 수거 축 문제가 페이싱 조정으로 증폭됐다.
- `timeoutRate` Lv100 3.6% 잔존.

### P5. ⚠️ 의뢰(다구간)는 무대 길이가 **구간 수만큼 곱해진다** — 가장 큰 파급

무대 하나를 90초로 만들면 계급4 의뢰(구간 5개)는 성장 없는 고정 화력 파일럿으로 **7분 이상**을
버텨야 한다. 실측 클리어율이 그대로 무너졌다:

| 계급(chain, 니플헤임 제외) | 조정 전 | 조정 후 |
|---|---|---|
| 1 | 95.7% | **25.4%** |
| 2 | ~92% | **10.0%** |
| 3 | 91.1% | **4.4%** |
| 4 | 83.3% | **0.0%** |

정예 소집령(57.1%)이 `chain` 보다 **높아져** ADR-0043 의 "정예가 더 어렵다" 방향 계약도 뒤집혔다.

**⚠️ 다음 레인이 반드시 알아야 할 함정**: `COMMISSION_SEGMENT_TICK_CAP`(9,000틱 = 150초) 게이트는
**이번에 초록으로 통과했는데, 그건 페이싱이 맞아서가 아니라 런이 일찍 죽어서다.** 난이도를
보상해 클리어율을 되돌리는 순간 구간틱이 상한 쪽으로 올라간다 — 보스 도달 90초(5,400틱) +
보스전 + 고정 화력 파일럿의 감속을 더하면 여유가 크지 않다. **클리어율을 복구하는 레인은
구간틱 p99 를 같은 표에서 함께 봐야 한다**(상한 초과는 밸런스가 아니라 **서버 거부**다 —
§C-ter2 참조).

---

## 난이도 복구 레인 (2026-08-02) — 계측기가 먼저 고장나 있었다

브랜치 `fix/difficulty-recovery-after-90s-pacing`. **머지하지 않았다** — 아래 §R6 참조.

### R1. ⚠️ 아르케 2.6% 는 난이도가 아니라 **봇 한계**였다 (해소)

착수 지시 1순위("봇 한계인지 실제 난이도인지 먼저 갈라라")의 답은 **봇 한계**다.

판별에 쓴 것은 신설 진단 지표 `후방압박`(`rearPinRate`) — 플레이어가 강제 스크롤 창의 뒤 경계
압박 구역 안에 있던 틱 비율이다. `racingRearPressure` 와 **같은 부등식**을 쓰고, sim 밖(하네스
런 루프)에서만 세므로 거동·해시에 닿지 않는다.

| 지표 | 수정 전 | 수정 후 |
|---|---|---|
| 클리어율 | 1.7% | **93.3%** |
| 후방압박 | **0.428** | 0.041 |
| 보스도달초 | 91.4 | 87.9 (페이싱 유지) |
| 로스터 편차 | 2.9pp | 15.0pp |

**근인 3중**: ① 오토파일럿이 창을 전혀 모른 채 최근접 적에서 물러난다 ② 레이싱은 적을 창
**전방**에 스폰한다(`RACING_SPAWN_AHEAD`) ③ 창 기준 속도 `INVASION_SCROLL_SPEED` 12 u/tick 이
플레이어 기본 이동 속도와 **같은 값**이고 가속은 100→200cp 로 오르기만 해서, 뒤로 밀린 거리가
사실상 만회되지 않는다. 코스가 14초일 땐 안 보였고 90초가 되자 드러났다.

처방(`src/sim/autopilot.ts`): 임계 320u 안이면 회피·카이팅보다 먼저 전진 · 여유 880u 안이면
뒤로 가는 성분만 절단(옆 회피는 보존) · **정지 기본값을 전진으로 교체**(창이 전진하므로 정지가
곧 후퇴다). `scrollRuntime` + `planetMode` 게이트라 타 무대·침공은 바이트 불변.

### R2. ⚠️ 블록격파에 같은 처방을 걸면 **해롭다** — 창만 같고 지형이 다르다

"강제 스크롤이면 전진"으로 일반화했다가 교환 대조에서 잡혔다. 레이싱의 뒤 경계는 **빈 공간**
이라 전진이 곧 회피지만, 블록격파의 전방은 **파괴 가능한 벽 행**이라 전진 편향이 봇을 벽에
밀어붙여 `crushBlockBreak` 을 자초한다. 같은 축(크라스 단독 · Lv5/25/60/80/100):
**49.0% → 16.7%**. 그래서 모드를 명시 열거한다.

### R3. 이 레인의 클린 기준선 (봇 수정 후, 3,780런, HEAD `d47eca9`)

| 행성(모드) | 클리어율 | 보스도달초 | 비고 |
|---|---|---|---|
| 아르케(레이싱) | **92.2%** | 87.4 | 밴드 **위** |
| 카르곤(뱀서류) | 78.9% | 90.9 | 밴드 안(상단) · 대조군 |
| 크라스(블록격파) | 49.0% | 89.9 | |
| 니플헤임(추격) | 30.6% | 90.3 | 장치잔존 2.89/5 |
| 베르단(수축) | 21.1% | 91.3 | |
| 톡사르(오염) | 14.3% | 93.9±58.3 | |

**페이싱은 전 무대 87~94초로 유지된다.** 즉 90초 목표를 깨지 않고도 아르케가 2.6 → 92.2% 로
움직였다 — 페이싱 직후 수치만 보고 적 곡선을 낮췄으면 **정반대 방향**으로 갔을 것이다.

### R4. ⚠️ "무너진 무대는 적이 세다"가 아니다 — 공용 적 축 처방 기각

무너진 무대들은 **적을 더 빨리 죽이면서 더 일찍 죽는다**:

| 행성 | 처치/초 | 생존초 | 클리어율 |
|---|---|---|---|
| 카르곤 | 2.61 | 93.5 | 78.9% |
| 톡사르 | 2.76 | 66.4 | 14.3% |
| 베르단 | 2.85 | 70.5 | 21.1% |
| 니플헤임 | 2.87 | 77.2 | 30.6% |

적 HP·밀도를 낮추는 처방은 여기서 기각된다(그 축은 이미 빠르게 소화되고 있다).

그래서 반대 방향("목표 게이트형 무대는 웨이브가 진행에 기여하지 않는 순수 소모전이니 유입을
줄이자")을 6,720런으로 실측했는데 **그것도 기각**됐다 — 무대 성질에 따라 **부호가 갈린다**:

| 행성(모드) | 계수 전 | 계수 1.6 | 무슨 일이 났는가 |
|---|---|---|---|
| 카르곤(대조군) | 78.9% | 79.1% | 계수 미적용 — 레버가 의도한 무대에만 걸렸다는 증거 |
| 베르단(수축) | 21.1% | **37.8%** | 개선 |
| 니플헤임 | 30.6% | 31.7% | 무반응 |
| 톡사르 | 14.3% | 13.9% | 무반응 |
| 크라스(블록격파) | 49.0% | **22.0%** | 레벨업 6.4 → **4.0** · 처치 73 — **굶어 죽는다** |
| 아르케(레이싱) | 92.2% | 97.1% | 보스도달 87.4 → **77.0s** — **페이싱 붕괴** |

**적 유입은 압박인 동시에 성장 자원이다.** 그리고 레이싱은 적이 줄면 `racingCleared` 가속이
더 자주 걸려 창이 빨라진다 — 이 무대에서 유입은 **페이싱 축이기도 하다**.

### R5. 무대별 처방 — 1건 확정, 1건 기각, 4건 미착수

**확정 — 베르단(수축) 21.1% → 40.3%** (`SHRINK_INTERVAL_SCALE = 1.6`, 보스도달 88.5s).
수축만 이 레버가 맞는 이유는 실측이 아니라 구조다: 여섯 무대 중 **유일하게 적 자체가 진행
게이트**(`shrinkRingCleared` = 안전 반경 안 적 전멸)라, 유입 감소가 피해 감소와 진행 가속으로
이중 작용한다. 아직 밴드(60~80%) 아래이므로 2차 조정이 필요하다.

**기각 — 톡사르**: 두 축을 각각 극단값으로 프로브했는데 **둘 다 무반응**이다.
- 유입 1.6배 → 14.3% → 13.9%
- `CONTAMINATION_CELL_DAMAGE` **6 → 0**(완전 제거) → 14.3% → **15.5%** (SE ≈0.8pp)

즉 오염 무대의 사인은 웨이브 물량도 오염 지형 피해도 아니다. **다음 회차 1순위 = 톡사르
사망 원인 계측**(무엇이 HP 를 깎는지 귀속시키는 진단 지표). 산술로는 노드 10 × 62,000 =
620,000 HP 를 관측 DPS 로 나누면 약 75초가 필요한데 생존이 65.6초다 — 목표 비용과 생존
시간이 정면 충돌하는 구조일 가능성이 크다.

**미착수**: 니플헤임 30.6%(장치잔존 2.89/5) · 크라스 49.0% · **아르케 92.2%(밴드 위 — 이제
난이도를 더해야 한다)** · 카르곤 78.9%(밴드 상단).

### R6. ⚠️ 머지하지 않았다 — 그리고 그 판단의 근거

완료 게이트(`vitest` 전건 통과)가 성립하지 않는다. main 이 이미 65건 적색이고, 그 내역은:

| 성격 | 건수 |
|---|---|
| 골든 재생성 대기 | 39 (`shipHashBaseline` 27 · `encounterHashInvariance` 10 · `denoFixture` 1 · `planetPopularity` 1) |
| 픽스처가 옛 상수를 하드코딩 | 9 (`racingMode` 4 · `blockBreakMode` 1 · `midClash` 1 · `tutorialShort` 2 · …) |
| **실제 밸런스 하한 위반** | 17 (`invasionBalance` 8 · `commissionBandMeasure` 5 · `fullRun` 3 · `planetTierCompletion` 2) |

골든 재생성은 **방향이 확정된 뒤에만** 해야 하는데 위 R5 대로 4무대가 미착수다. 이 브랜치는
새 실패를 추가하지 않는다(같은 13건이 그대로다) — 즉 **엄격한 개선**이지만 초록은 아니다.

### R7. 침공(PvP) 붕괴는 페이싱이 아니라 **파워업 너프 단독** 소행이다

침공은 `updateWaves` 를 아예 돌리지 않고 페이싱 변경은 전부 PvE 무대 상수다. 두 축이 만나는
지점은 파워업뿐이고, 실측이 그것을 확인한다:

| 밴드/게이트 | 목표 | 실측 |
|---|---|---|
| 중하 밴드 | 55~80% | **39.9%** |
| 중위 밴드 | 25~55% | **11.1%** |
| 기체 1~4 · 기지 #18(사거리 임계) | 33~ | 8.3 / 4.2 / 0.0 / **0.0%** |
| 기체 4 · 기지 #20 | 5~ | 4.2% |

의뢰도 같은 뿌리다(계급 1/2/3/4 = 25.4 / 10.0 / 4.4 / **0.0%**, 정예 소집령 57.1% 가 chain 을
역전해 ADR-0043 방향 계약이 뒤집혔다).

⚠️ **여기에 갈림길이 있고 이 레인은 단독으로 정하지 않았다.** 파워업 상한 +10% 는 사용자
지시라 되돌릴 수 없으므로, 남은 선택지는 둘이고 처방이 정반대다:
1. **적/방어 축 보정** — 침공 방어 기지·의뢰 구간 비용을 낮춘다(ADR-0037 정합, 레인 규모 큼).
2. **플레이어 기본 축 복원** — 파워업이 아닌 기본 화력(기체·무기 기준값)을 올린다. PvE·PvP·
   의뢰가 **한 번에** 움직이지만 파워업 하향의 취지(런 내 성장 폭 축소)와의 정합을 사용자가
   판단해야 한다.

**사용자 판정(2026-08-02)**: 두 시스템을 **각자 자기 축에서** 닫는다 — **침공은 방어 기지 강도**,
**의뢰는 계급별 구간 수**(`COMMISSION_SEGMENT_COUNT` 2/3/4/5). 플레이어 기본 화력은 올리지
않는다(파워업 하향의 취지를 보존). 회차가 둘로 갈리는 대신 두 시스템의 성격 차이가 유지된다.

⚠️ 의뢰 구간 수를 줄일 때 **구간틱 p99 를 같은 표에서 함께 보라** — 상한
`COMMISSION_SEGMENT_TICK_CAP`(9,000틱) 초과는 밸런스가 아니라 **서버 거부**다. 구간 수를 줄이면
총 예산(`commissionReplayBudgetTicks` = 구간 수 × 상한)도 함께 줄어드는 점에 주의.

---

## 난이도 복구 레인 2차 (2026-08-02) — 계측기가 **또** 먼저 고장나 있었다

브랜치 `fix/difficulty-recovery-round2`. main `0da9e6a`(PR #241) 후속.

### R8. ⚠️ 한 레벨의 장비 표본이 **7배 부풀려져** 있었다 — 레벨 곡선이 장비 운의 위장이었다

`standardEquipped(level, seed, planet)` 은 **기체를 인자로 받지 않는다.** 그래서 한 라운드(시드
하나) 안에서 기체 7종이 **전부 똑같은 장비**로 돌았다. 셀당 11시드면 `(행성, 레벨)` 한 점의
런은 77건이지만 **장비 추첨은 11번뿐**이다.

그리고 이 무대들에서 승패는 사실상 장비 추첨이 정한다. 시드별 승수가 이랬다:

| 셀 | 시드별 승수(7기체 중) |
|---|---|
| 톡사르 Lv25 | `0/7` ×9 · `6/7` · `7/7` |
| 크라스 Lv25 | `0/7` ×6 · `4/7` · `5/7` · `6/7` ×2 · `7/7` |
| 니플헤임 Lv15 | `0/7` ×10 · `2/7` |

**기체가 아니라 시드가 결과를 가른다.** 근인은 표준 세트의 **주무기 타입 추첨**이다 — 8칸
어픽스 수는 어느 레벨에서나 44~48 로 같은데 피해 배율은 시드 묶음에 따라 2.2 ~ 4.7 로 벌어진다
(주무기 타입이 `applyWeaponTypeBase` 로 출발점을 정하므로). 목표 게이트형 무대는 그 위에 DPS
문턱을 세우니 결과가 이분된다.

그 상태로 **레벨별** 클리어율 게이트를 읽으면 장비 운이 레벨 곡선의 굴곡으로 위장한다. 수정
전에는 Lv15·Lv70·Lv80 에 "전 기체 0%" 구멍이 보였고, 그것이 표준 빌드의 구조적 약점처럼
읽혔다 — **없는 문제였다.**

**처방**: 장비 시드에 기체를 섞는다(`cell.ts` `gearSeed`, SplitMix32). sim 무변경 · 결정론 유지.
대가는 기체 비교의 짝짓기를 잃는 것인데, 로스터 편차는 기체당 수백 런으로 접어 장비 운이
평균되는 반면 레벨별 게이트는 접을 곳이 없어 그대로 오염되므로 정밀도를 게이트 쪽에 준다.

**수정 후 레벨 곡선(4,550런)** — 굴곡이 사라지고 **저레벨 단일 문제**로 정리됐다:

| Lv | 카르곤 | 베르단 | 니플헤임 | 아르케 | 톡사르 | 크라스 | 전체 |
|---|---|---|---|---|---|---|---|
| 5 | 79.1% | 48.4% | 6.6% | 86.8% | **0.0%** | 13.2% | **39.0%** |
| 25 | 84.6% | 46.2% | 39.6% | 96.7% | 14.3% | 35.2% | 52.7% |
| 60 | 84.6% | 57.1% | 29.7% | 97.8% | 12.1% | 50.5% | 55.3% |
| 80 | 78.0% | 31.9% | 26.4% | 87.9% | 11.0% | 65.9% | 50.2% |
| 100 | 91.2% | 50.5% | 56.0% | 98.9% | 33.0% | 92.3% | **70.3%** |

행성 평균은 거의 안 움직였다(카르곤 83.5 · 베르단 46.8 · 니플헤임 31.6 · 아르케 93.6 · 톡사르
14.1 · 크라스 51.4) — **이 수정이 고친 것은 평균이 아니라 셀 안의 분산**이다.

### R9. 사망 원인 귀속 지표 4종 신설 (관측 전용)

R5 가 남긴 1순위("톡사르는 무엇이 HP 를 깎는지부터 재라")의 답이다. sim 을 한 바이트도 건드리지
않고 하네스 런 루프에서만 센다(`rearPinRate` 선례).

| 지표 | 무엇을 가르는가 |
|---|---|
| `맵상실`(`mapLossRate`) | 패배가 **플레이어 사망이 아닌** 비율. 오염 임계 · 무적 포식자 접촉 즉사 |
| `정화율`(`purifyEnd`) | 종료 시점의 목표 진척(오염 전용) |
| `지형피해분`(`hpLossHazardShare`) | 잃은 HP 중 활성 해저드 귀속분 |
| `접촉피해분`(`hpLossContactShare`) | 적·보스 몸통 귀속분. 잔차 = 원거리 탄 + 직접 피해(압사·압박) |

귀속 규칙은 sim 의 "겹친 피해원 중 최대"와 동형이되, **후보의 `damage` 가 이번에 잃은 HP 이상일
때만** 범인으로 인정한다 — 적용치는 감액만 받고 절대 커지지 않으므로 `damage < 잃은 HP` 인
후보는 원리적으로 범인이 아니다. 이 조건이 없으면 장판 위에 오래 서 있는 자유추적 무대에서
탄이 죽인 것까지 지형에 실린다.

**톡사르 답**: 패배 런(680건)은 **정화율 0.11** 로 끝난다 — 노드 10개 중 1개를 깨고 61초에
죽는다. 승리 런은 정화율 1.00 · 80.6초. 즉 사인은 확산 압박도 오염 지형 피해도 아니라
**목표 총 HP(10 × 62,000)를 생존 창 안에 감당하지 못하는 것**이다. 유입 1.6배도
`CONTAMINATION_CELL_DAMAGE` 6→0 도 무반응이었던 이유가 이것이다 — 둘 다 구속 조건이 아니었다.
니플헤임도 같은 모양이다(패배 런 **장치잔존 4.14/5**).

### R10. 픽스처 8건 — 옛 상수 하드코딩 해소 (방향 무관, 완료)

전부 "계약이 깨진 게 아니라 관측 예산·파생값이 낡은 것"이었다.

| 파일 | 건수 | 무엇이 낡았나 |
|---|---|---|
| `racingMode` | 4 | 채널 벽 수를 `RACING_SECTION_COUNT` 로 단언(실제는 **구간 × 분기 수** = 42) · 코스 주파 예산 2,000틱 |
| `blockBreakMode` | 1 | 코스 주파 예산 1,600틱 + 보스 처치 예산 30틱 |
| `midClash` | 1 | 격전 세그먼트 전진 예산 3,600틱 |
| `tutorialShort` | 2 | 무장비 파일럿의 자연 화력에 의존 — 파워업 +35%→+10% 로 궤적이 예산 안에 안 끝났다 |

셋 다 **코스 길이 ÷ `INVASION_SCROLL_SPEED`** 파생으로 바꿨고, 튜토리얼은 관측 화력을 못박아
화력 튜닝과 무관하게 만들었다(부수로 38.8초 → 1.3초).

⚠️ **블록격파 보스 처치 예산 30 → 600 의 근거**: 보스는 코스 끝 **창보다 앞**(실측 약 1,100
유닛)에 스폰되고, 창 밖에 스폰한 거대 시험탄은 스크롤 컬링이 **그 틱에** 지운다. 즉 창이 보스에
닿기 전에는 어떤 시험탄도 명중할 수 없다 — "탄이 안 맞는다"로 오진하기 쉬운 자리다.

### R11. 무대별 수치 — 2건 확정, 2건은 **페이싱과 정면 충돌**(사용자 판단 필요)

교환 대조(카르곤·아르케는 계수 미적용 = 두 회차 모두 바이트 동일)로 레버 연결을 먼저 확인했다.

| 행성 | 축 | 값 | 클리어율 | 보스도달 | 판정 |
|---|---|---|---|---|---|
| 크라스 | `BLOCKBREAK_CRUSH_DAMAGE` | 8 → **6** | 51.4% → **70.8%** | 90.4s → 89.1s | ✅ 밴드·페이싱 동시 달성 |
| 니플헤임 | `CHASE_COUNTER_DEVICE_HP_BASE` | 76,000 → **52,000** | 31.6% → 40.8% | 82.6s → **64.8s** | ⚠️ 페이싱 파손 |
| 톡사르 | `CONTAMINATION_NODE_HP_BASE` | 62,000 → **40,000** | 14.1% → 24.5% | 88.2s → **74.9s** | ⚠️ 페이싱 파손 |
| 베르단 | `SHRINK_INTERVAL_SCALE` | 1.6 → 1.9 → **1.6** | 46.8% → 49.7% (레벨업 5.7→5.3) | 89.0 → 88.8s | ❌ 레버 소진, 되돌림 |
| 카르곤·아르케 | (미적용 대조군) | — | 83.5→81.6% · 93.6→93.9% | 불변 | 레버가 의도한 무대에만 걸렸다 |

**레벨별 게이트 위반이 4곳 → 2곳으로 줄었다**(Lv5=42.3% · Lv80=54.9%). Lv25·Lv60·Lv100 은 밴드 안이다.

#### ⚠️ 이 회차가 드러낸 구조 — 목표 게이트형 무대는 **난이도와 페이싱이 같은 하나의 레버다**

톡사르·니플헤임에서 목표 총 HP 는 ① 얼마나 오래 버텨야 하는가(난이도)와 ② 보스에 언제 닿는가
(페이싱)를 **동시에** 정한다. 그래서 "보스 도달 90초"(사용자 지시)와 "클리어율 60~80%"
(ADR-0037)를 한 상수로 둘 다 만족시킬 수 없다. 크라스만 밴드·페이싱을 동시에 잡은 이유가
정확히 이것이다 — 압사는 **진행 게이트가 아니라 순수 피해원**이라 두 축이 분리돼 있다.

**남은 선택지는 셋이고, 이 레인은 단독으로 정하지 않았다:**
1. **생존 축을 따로 만든다** — 이 두 무대에만 걸리는 피격 피해 계수(`SHRINK_INTERVAL_SCALE` 과
   같은 모드 게이트 형태). 목표 HP 는 페이싱이 정한 값으로 되돌리고 난이도는 생존 축이 진다.
   구조상 가장 깨끗하지만 신규 sim 상수가 하나 늘어난다.
2. **페이싱 목표를 무대 성격별로 나눈다** — 목표 게이트형 무대는 90초보다 짧게 인정한다.
3. **현 상태 유지** — 클리어율을 얻고 페이싱 75/65초를 받아들인다(이번 회차가 이 상태다).

#### 잔여(미착수)

- **아르케 93.9%(밴드 위)** — 난이도를 더해야 한다. 후방압박이 0.043 로 낮아 압박 계수는 약한
  레버다. 축 미정.
- **베르단 45.9%** — `SHRINK_INTERVAL_SCALE` 은 소진됐다(1.9 로 올려도 +3pp 인데 런내 레벨업이
  5.3 으로 밴드 하한 5 에 붙는다). 다른 축이 필요하다.
- **톡사르 24.5%** — 위 구조 결론에 걸려 있다.
- **Lv5 전체 42.3%** — 저레벨이 전 무대에서 가장 낮다(톡사르 1.0% · 니플헤임 10.2%).
  목표 HP 의 단계 기울기가 `_HP_SLOPE 0.15` 로 거의 평평한데 플레이어 화력은 레벨로 크게
  오르므로, 저단계가 상대적으로 무겁다. 기울기 재설계가 다음 후보다.
- **장비유입 0.97**(목표 2~3) · **타임아웃 Lv100 3.6%** — 이전 레인에서 이월, 미해소.
- **침공(PvP)·의뢰** — R7 사용자 판정(침공=방어 기지 강도 · 의뢰=계급별 구간 수)대로 착수해야
  한다. 이 회차는 손대지 않았다.
- **골든 39건** — 방향 미확정이라 재생성하지 않았다(R6 과 같은 판단).

### R12. 생존 축 신설 — 사용자 판정 ①번 실행 (완료)

§R11 갈림길에서 **사용자가 ①"생존 축을 따로 만든다"를 선택**(2026-08-02). 결과는 목표대로다.

**신설**: `objectiveModeDamageScale`(`src/sim/modes/objective.ts`) — 목표 게이트형 무대의 피격
피해 배율. `SHRINK_INTERVAL_SCALE` 과 같은 모드 게이트 형태라 타 무대·침공은 바이트 불변이고,
`tests/objectiveModeDamageScale.test.ts` 가 그 계약을 못 박는다(뮤테이션 검증 완료 — 배선을
지우면 실패한다).

| 행성 | 전 | 후 | 보스도달 |
|---|---|---|---|
| 니플헤임(추격) | 42.0% | **65.1%** | 65.7 → 78.1s |
| 톡사르(오염) | 25.1% | **61.7%** | 77.1 → 84.4s |
| 카르곤·베르단·아르케·크라스 | (대조군) | **런 단위로 정확히 불변** | 불변 |

**레벨별 전체 클리어율이 5점 중 4점에서 밴드 안에 들어왔다**(직전 2점):

| Lv | 5 | 25 | 60 | 80 | 100 |
|---|---|---|---|---|---|
| 직전(R11) | 42.3% | 61.2% | 64.3% | 54.9% | 75.2% |
| **지금** | 55.5% | **71.0%** | **78.1%** | **67.4%** | 80.7% |

#### ⚠️ 손잡이는 둘이 아니라 셋이었다

프로브 5회의 결론. 목표 HP 와 피격 배율은 **페이싱을 서로 반대 방향으로 민다**:

| 톡사르 조합 | 클리어율 | 보스도달 | 무엇이 깨졌나 |
|---|---|---|---|
| HP 62,000 · 배율 1.0 | 14.1% | 88.2s | 클리어율 |
| HP 40,000 · 배율 1.0 | 24.5% | 74.9s | 페이싱(빨라짐) |
| HP 62,000 · 배율 0.3 | 37.1% | 105.1s | 페이싱(느려짐) · 레벨업 10.1 |
| **HP 40,000 · 기울기 0.06 · 배율 0.26** | **61.7%** | **84.4s** | — |

세 번째가 **단계 기울기**(`_HP_SLOPE` 0.15 → 0.06)다. 배율만 낮추면 플레이어가 안 죽는 대신
고단계에서 목표를 못 깎아 **패배가 타임아웃으로 바뀐다** — 승률은 오르는데 구조 건전성이
무너진다. 세 상수는 함께 정해졌으므로 **하나만 되돌리면 안 된다.**

#### ⚠️ 악화된 것 — B7(만렙 추격 런이 끝나지 않는다)

타임아웃 Lv100 이 **2.6% → 6.9%** 로 나빠졌고 **전량 니플헤임**이다(그 셀만 26.5%). 만렙 추격
런은 승률 72.9% · **사망 거의 0** · 나머지가 시간 초과다 — 플레이어가 죽는 게 아니라 **장치 5개 +
포식자를 300초 안에 못 깎는다.** 장치 기울기를 0.06 까지 낮춰도 남았으므로, 다음 축은 장치가
아니라 **포식자(보스) HP** 다. 이건 §B7 의 연장이고 이 레인이 새로 만든 문제가 아니라 **생존 축이
가려져 있던 것을 드러낸 것**이다(패배로 끝나던 런이 이제 안 끝난다).

#### 부수 효과 — 의뢰가 크게 회복됐다(미착수인데도)

의뢰 체인은 톡사르를 포함하므로 생존 축이 그대로 실렸다. §P5 대비:

| 계급 | 페이싱 직후 | R11 시점 | **지금** | 목표 |
|---|---|---|---|---|
| 1 | 25.4% | — | **69.8%** | 80%+ |
| 2 | 10.0% | — | **54.3%** | 80%+ |
| 3 | 4.4% | — | **26.7%** | 80%+ |
| 4 | **0.0%** | — | **22.2%** | 80%+ |

⚠️ 대신 **정예 소집령이 85.7% 로 밴드(40~85%) 위를 0.7pp 넘었고**, ADR-0043 의 "정예가 chain
보다 어렵다" 방향 계약도 여전히 뒤집혀 있다(85.7% > 계급2 54.3%). 그래서
`commissionBandMeasure` 적색이 5 → 6 으로 **한 건 늘었다** — 수치는 전 계급이 크게 좋아졌는데
정예가 위로 이탈한 결과다. 닫는 축은 R7 판정대로 `COMMISSION_SEGMENT_COUNT` 이고 미착수다.

#### 잔여

- **Lv5 55.5%** — 여전히 밴드 아래이고 전 무대 공통이다(톡사르 32.9% · 크라스 30.0%).
- **Lv100 80.7%** — 0.7pp 초과. 사실상 경계.
- 아르케 93.4%(밴드 위) · 베르단 50.0% — 축 미정, 이번에도 미착수.
- 침공·의뢰 — R7 판정대로 대기.

---

## 전체 재측정 + B7 해소 레인 (2026-08-03)

### R13. B7 — "다음 축은 포식자 HP" 라는 3개 회차짜리 추정이 **틀렸다**

전체 런(HEAD `963b848` · 4,620런)에서 게이트 2통과/3실패. R12 이후 밸런스 축은 사실상 불변이라
그 뒤 머지된 촉매·스킬 사슬 PR 들은 난이도에 영향이 없었다(레벨별 전부 오차 범위).

남은 실패 중 **Lv100 타임아웃**을 잡았다. §R12 가 "다음 축은 장치가 아니라 포식자(보스) HP"로
남겼는데, **실측이 그 추정을 반증했다**:

| 관측 | 값 | 뜻 |
|---|---|---|
| 승리 런 보스도달 → 클리어 | 83.3s → 84.8s | 취약화 후 **1.5초**에 포식자가 죽는다 |
| 타임아웃 런 중 장치 5/5 잔존 | 64% | 병목은 장치다 |
| 그 런들의 장치 잔여 HP | **0.766** | 화력이 0 도 아니고, 300초에 23%만 깎았다 |
| 타임아웃 런 처치/초 | **6.12** (승리 3.11) | 화력은 센데 **전부 잡몹에 쓴다** |

즉 축은 HP 도 포식자도 아닌 **조준 순번**이었다. 조준 목록에 넣는 것만으로는 부족하다는,
`objective.ts` 머리의 결함 4회 **다음 단계**다 — 목록에는 있는데 순번이 오지 않는다.

**일반형: "레버를 4배 돌렸는데 10% 움직인다"의 자매 신호는 "18% 깎으나 36% 깎으나 같은 자리"다.**
비례하지 않으면 그 축이 아니다. HP 를 1/10 로 만들어야 사라졌다는 사실이 크기 문제가 아님을
가리키고 있었는데, 정수 지표(`chaseDevicesLeft`)만 보는 동안 "5개 그대로"가 *화력 0* 과
*조금씩 깎는 중* 을 같은 값으로 보고했다. **연속 지표(`chaseDeviceHpFrac`)를 신설하고서야 갈렸다.**

#### 처방 — 두 축을 함께(단계 함수 조준 가중치 신설 + 장치 기울기 0.06 → 0.02)

`objectiveAimBias(stage)`: 목표 오브젝트의 제곱거리에 곱하는 우선 가중치. 단계 1 에서 **정확히 1**,
단계 20 에서 0.1 로 선형. 사거리 판정에는 쓰지 않는다(할인된 거리로 사거리를 재면 탄이 닿지
않는 표적을 조준한다).

**단계 함수여야 하는 이유가 실측에 있다.** 고정 우선권은 저단계를 깎는다 — 잡몹이 적어
우선권이 필요 없는 구간에서 코앞의 적을 두고 목표를 쏘다 죽는다:

| 3분 프로브(레벨 축 고정) | 니플 전체 | 니플 to | 니플 Lv5 | 니플 Lv100 | 톡사르 Lv5 |
|---|---|---|---|---|---|
| 기준선 | 64.6% | 5.0% | 38% | 70%/to29% | 27% |
| 기울기 0.02 단독 | 67.7% | 4.4% | **38%(불변)** | 76%/to24% | **27%(불변)** |
| 고정 가중치 0.05 + 기울기 0.04 | 65.8% | 3.0% | **29%** ❌ | 82%/to17% | **23%** ❌ |
| **단계 함수 + 기울기 0.02** | **69.0%** | **3.2%** | **39%** | **83%/to17%** | **27%** |

가중치 하한을 0.1 → 0.02 로 더 밀어도 Lv100 타임아웃은 17 → 16% 라 수확체감이다(변경 폭이
작은 0.1 채택).

#### 잔여 · 자인

- **Lv100 타임아웃 17%** — 밴드(0~2%) 밖이다. 29%에서 절반으로 줄였을 뿐 닫지 못했다.
  다음 축은 HP 가 아니다(수확체감 확인) — **만렙 잡몹 밀도**이거나, 장치를 깨는 동안의
  교전 압박 자체다.
- `nearestTarget` 의 **벽-없음 fast path 는 목표 게이트형 무대에서 실전 도달하지 않는다**
  (추격·오염 둘 다 activeWalls 상시 5개). 같은 규칙을 넣어 뒀지만 뮤테이션으로 잡히지 않는다 —
  `tests/objectiveAimBias.test.ts` 와 `world.ts` 주석에 자인.
- 톡사르(오염)는 이 두 축에 거의 반응하지 않았다(승률 61.8 → 63.3%). 그 무대의 노드는 이미
  깨지고 있었다는 뜻이라, 톡사르 축은 여전히 미착수다.

## 이전 레인에서 이월된 항목

*(현재 없음 — 이 파일이 2026-07-31 에 처음 생성됐다. 이전 레인의 밸런스 유예는 각 레인 문서와
`.omc/research/` 에 흩어져 있으며, 발견되는 대로 여기로 모은다.)*
