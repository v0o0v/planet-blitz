# 드론 사출구(`spawner`) 재설계 — 뒤에 낳는 한 어떤 수치도 기여가 0 이다

- **측정일**: 2026-07-28
- **기준 커밋**: `5bf088d` (main · 침공 난이도 복원 레인 종료 직후)
- **브랜치**: `feat/invasion-spawner-redesign-2026-07-28`
- **선행**:
  - `.omc/research/invasion-dead-facilities-2026-07-27.md` §6 — `spawner` 를 **ⓑ 살릴 수 없다**
    로 판정하고 sim 축 추가를 레인 리드 판단 항목으로 남겼다.
  - `.omc/research/invasion-difficulty-restore-lane-2026-07-27.md` §3.3 · §5-3 — "죽은 레버"
    목록과 다음 레인 우선순위 3번.
  - `.omc/research/invasion-gravwell-slow-axis-2026-07-27.md` — "기여를 나르는 값에 강화축이
    실려 있지 않다"의 선례. 이 문서의 처방은 그와 같은 계열이다.
- **하네스**: 설비 1종만 12소켓(직선형 템플릿 0)에 채운 배치(편대·기물·보스 비움) ·
  `rarity 0` · `ascension 0` · `affixSeed 0` **레벨 단독 스윕** · 지표는 `phase === 1`(L2)
  동안의 플레이어 HP 감소 합 + 근접 지표
- **해상도**: 튜닝 스윕 24시드(레벨 축) · 밴드 회귀 24시드 · 승격 판정 96시드 (ADR-0037)

> **결론 먼저.** 원인은 "드론이 느리다"가 아니라 **낳는 자리**였다. L2 는 강제 스크롤
> 720 u/s 인데 로스터 최고 적 이동 속도가 420 u/s 라, 소켓 옆(플레이어 뒤)에 태어난 드론은
> 초당 300유닛씩 영구히 뒤처진다. 그래서 사출 지점을 **스크롤 창 진행 방향 바로 앞**으로
> 옮겼다(선택지 ②). 최근접 드론 평균 거리 **15,203 → 913 유닛**, L2 누적 피해
> **12(사실상 0) → 915**(lv29 · 24시드)로 축이 살아났다.
>
> 그리고 위치만 고쳐서는 부족했다. 스포너는 **강화 3축이 설비 내구도에만 걸려 있었다** —
> 카탈로그 `damage` 가 0 이라 `scale(0) === 0` 이고, 드론 스탯은 로스터 원본을 그대로 썼다.
> `gravwell` 과 같은 **스케일 대상 누락**이다. 축을 기여를 나르는 값 — **생산물의 내구도와
> 접촉 피해** — 에 태웠다.

---

## 0. 착수 전 재현 — 판정은 그대로였다

| lv | 1 | 5 | 10 | 17 | 25 | 35 | 50 | 70 | 99 |
|---|---|---|---|---|---|---|---|---|---|
| L2 누적 피해(24시드) | 24 | 12 | 12 | 12 | 12 | 12 | 12 | 12 | 12 |
| 최근접 드론 평균 거리 | 15,203 | 15,073 | 14,959 | 14,917 | 14,880 | 14,880 | 14,880 | 14,875 | 14,875 |
| 600유닛 안이던 틱 비율 | 5.0% | 5.0% | 5.3% | 5.4% | 5.4% | 5.4% | 5.4% | 5.4% | 5.4% |

선행 문서의 수치가 그대로 재현된다. 24시드에 드론 1,327기를 낳고 **누적 피해 12** 다
(lv1 의 24 가 lv5 이상보다 큰 것은 노이즈다 — 피격이 각각 2회·1회다).

### 0-1. 재현 과정에서 **새로 드러난 사실** — 플레이어는 L2 의 17% 만 회랑 안에 있다

| 항목 | 값 |
|---|---|
| L2 레이어 예산 | 5,400틱 |
| L2 주파 길이 `INVASION_LAYER_LENGTH[1]` | `trunc(12 × 5400 × 110/100)` = **71,280 유닛** |
| 회랑 길이 `lengthUnits`(전 템플릿) | **12,000 유닛** |
| L2 동안 플레이어 x 평균 | **33,072** |
| 플레이어가 `x ∈ [0, 12000]` 안에 있던 틱 비율 | **17.1%** |

**설비가 놓인 구간은 L2 의 앞 1/6 뿐이다.** 나머지 83% 는 벽도 소켓도 없는 빈 공간을 난다.
이 사실은 스포너뿐 아니라 **모든 L2 설비의 실효 노출 시간**을 규정한다(포탑도 같은 17% 안에서만
싸운다). 이 레인은 이 구조를 고치지 않았다 — 레이어 길이·회랑 길이 정합은 별도 항목이다(§8).

다만 재설계에는 직접 영향이 있다: 사출 지점을 창 앞으로 옮기고 **활성 사거리를 두지 않으면**,
회랑을 20,000유닛 지나친 뒤에도 사출이 계속돼 소켓과의 공간 인과가 통째로 끊긴다. 그래서
`spec.range` 게이트를 함께 넣었다(§3-2).

---

## 1. 선택지 3종 비교

| | ① 소환체를 따라잡게 | ② 사출 위치를 앞으로 | ③ 피해 축을 포기하고 차폐·방해 |
|---|---|---|---|
| 판정 | **기각(실측)** | **채택** | **기각(구조·선례)** |
| 회귀 표면 | `src/sim/patterns/index.ts`(전 게임 공용 이동 경로) | `src/sim/invasion/facility.ts` 스포너 분기 | 아군탄 충돌 화이트리스트(전 게임) |
| 실측 결과 | §2 | §3~§5 | §6 |

### 1-1. 판정 기준

세 선택지 모두 "레벨·등급·승급을 올리면 지표가 단조 증가하는가"를 물었다. 그리고 이 리포가
반복해서 밟은 두 함정을 회피 조건으로 걸었다:

- **지표 포화 금지** — 실효 피해가 참조 플레이어 최대 HP **160** 을 넘으면 지표가 설비가
  아니라 HP 풀을 재기 시작한다(지문: `hits === deaths`). 난이도 복원 레인 §3.2.
- **회귀 표면 최소화** — 이 리포의 반복 결함은 "단위 테스트 그린인데 배선이 통째로 없다"이고,
  전 게임 공용 경로를 건드리는 처방은 그 결함을 만들기 가장 쉬운 자리다.

---

## 2. 선택지 ① — 소환체를 따라잡게 만든다 (**기각**)

### 2-1. 실측

프록시로 `CHARGER.speed`(드론이 쓰는 로스터 값)만 바꿔 격리 스윕을 돌렸다. 드론 전용 속도
필드를 만들기 전에 **속도라는 축 자체가 답이 되는가**를 먼저 물은 것이다.

| 속도(u/s) | lv1 | lv10 | lv17 | lv25 | lv50 | lv99 | 최근접 평균 |
|---|---|---|---|---|---|---|---|
| 300(현행) | 24 | 12 | 12 | 12 | 12 | 12 | 15,203 |
| **780** (스크롤 720 초과, 플레이어 792 미만) | 0 | 0 | 0 | 0 | 12 | 12 | **1,310** |
| **900** (플레이어 초과) | 0 | 18 | 36 | 84 | 102 | 114 | 1,733 |
| **1100** | 120 | 288 | 348 | 444 | 444 | 444 | 1,626 |

### 2-2. 읽는 법 — 속도는 거리를 고치고 피해를 못 고친다

- **780 에서 거리가 15,203 → 1,310 으로 무너지는데 피해는 여전히 0 이다.** 스크롤(720)은
  넘었지만 참조 플레이어 최대 이동 속도(**792** = 720 × `moveSpeedMult` 1.1)는 못 넘어서,
  드론이 **영원히 1,300유닛 뒤에 붙어만 있다**. "따라잡는다"와 "닿는다"가 다른 문제라는 것을
  가장 선명하게 보여주는 행이다.
- 900(플레이어 초과)에서 처음 피해가 생기고 축도 단조지만, **24시드 총 114** 다. 같은 하네스에서
  박격포는 lv17 에 378 · lv50 에 1,755 다. 실효 화력이 두 자릿수 배 모자란다.
- 1100 에서도 lv25 부터 **평탄**하다(444 고정). 소켓이 플레이어 평균 위치보다 20,000유닛
  뒤에 있어(§0-1) 드론이 도달하는 데만 L2 대부분을 쓰기 때문이다 — 속도를 올려도 **소켓의
  위치가 상한을 만든다**.

### 2-3. 기각 사유

1. **효과가 모자란다.** 로스터 최고속(420)의 2.6배인 1100 에서도 상위 설비의 1/5 이고,
   실제 램프 구간(lv1~29)에서는 훨씬 아래다.
2. **회귀 표면이 전 게임이다.** 드론 한정 속도는 `src/sim/patterns/index.ts` 의 `moveCharge`
   (모든 행성·모든 돌격형이 지나는 경로)에 침공 전용 분기를 심어야 나온다. 과제 지시가 금지한
   "로스터 전역 속도 상향"은 피하더라도, **공용 이동 경로에 조건을 하나 더 얹는 비용**은 남는다.
3. **"재미있는 압박인지 처형인지"를 참조봇으로는 가를 수 없다.** 봇 기준으로는 24시드 전부
   `deaths=0` 이라 처형이 아니지만, 그건 봇이 한 순간도 멈추지 않기 때문이다(난이도 복원 레인
   §3.1-3 과 같은 성질). **플레이어보다 빠르고 자동 추적하는 적**은 사람에게는 이탈 자체가
   불가능한 압박이고, 그 성질은 이 계측으로 확인할 수 없다. 확인 불가능한 위험을 지고 얻는
   것이 1/5 화력이면 교환이 성립하지 않는다.

> 채택안(②)은 이 축을 **한 자리도 쓰지 않는다**. 드론 이동 속도는 300 그대로다.

---

## 3. 선택지 ② — 사출 위치를 창 앞으로 (**채택**)

### 3-1. 무엇을 바꿨나

`stepSpawnerFacility`(`src/sim/invasion/facility.ts`)의 사출 좌표를

```
sx = e.x + cos(e.targetX) * spec.spawnOffset      // 소켓 옆 = 플레이어 뒤
```

에서 **스크롤 창 진행 방향 바로 앞**으로 옮겼다.

| 값 | 정의 | 근거 |
|---|---|---|
| 사출 x | `windowCenterX + dir × (INVASION_WINDOW_HALF_W + 120)` | 창 반폭 960 **밖**이라 드론 반경 36 을 더해도 화면 경계에서 84 유닛 남는다 — 눈앞에 튀어나오는 것이 **기하로 불가능**하다 |
| 사출 y | `windowCenterY + launchLaneY(tick, socketIndex)` | 진폭 420 삼각파(반주기 97틱 · 소켓마다 37틱 위상차). 회랑 반높이 540 − 드론 반경 36 − 여유 84 |
| 축이 없으면 | 예전 `spawnOffset` 경로 그대로 | 고정 카메라(L3)·단위 테스트 호환 |

L1(종스크롤)도 같은 규칙으로 처리한다(창 위쪽 밖). 현행 카탈로그에서 스포너가 L1 에 놓이는
경로는 없지만, 축 분기를 스포너 한 곳에 모아 두는 편이 나중에 갈라지지 않는다.

**결정론(ADR-0005)**: RNG·wall-clock 미소비. 레인 y 는 `tick` 과 소켓 인덱스만의 순수 삼각파다.
좌표는 전부 정수 연산이다.

### 3-2. 활성 사거리 — 소켓과의 공간 인과를 지키는 장치

`spec.range = 2600`. 플레이어가 그 밖이면 사출하지 않는다(방어포와 같은 규율).

이게 없으면 §0-1 때문에 **회랑을 20,000유닛 지나친 뒤에도 12기가 계속 사출**한다. 실측으로도
그 구성은 L2 5,400틱 내내 정면 스트림이 유지돼, 다른 설비가 17% 만 노출되는 것과 성질이 갈린다.
`range === 0`(사거리 개념이 없던 시절의 스펙)은 무제한으로 남겨 구 카탈로그와 호환한다.

### 3-3. 벽·기물과 겹치지 않는가 — 좌표를 만들고 **검사**한다

사출 좌표가 `state.activeWalls`(정적 회랑 벽 + 압축 프레스 이동 벽) 중 하나라도 겹치면
**사출을 취소하고 3틱 뒤 재시도**한다. 삼각파 레인이 그 사이 옮겨가므로 곧 빈 자리를 찾는다.

굴곡형(템플릿 1)은 구간마다 회랑 중심 y 가 0 / −300 / +300 으로 달라 진폭 420 이 그대로는
벽에 물린다 — 이 검사가 그 구간을 걸러 낸다. `tests/invasionFacility.test.ts` 가 **전 템플릿
3종에서 사출 좌표 × 전 벽 조합의 겹침 0** 을 단언한다.

### 3-4. 예고 없이 앞에 나타나는 것이 부당한가 — 아니다

사출 지점은 항상 창 반폭 **밖**이다(§3-1, 테스트가 단언). 드론은 화면 밖에서 태어나 스스로
경계를 넘어 들어온다 — 플레이어가 보는 것은 "오른쪽에서 날아 들어오는 적"이지 "눈앞에 생긴
적"이 아니다. 접근 상대속도는 스크롤 720 + 드론 300 ≈ 1,020 u/s 이라 화면을 가로지르는 데
약 1.9초가 걸린다.

---

## 4. 강화 3축을 **생산물**에 실었다 — 위치만 고쳐서는 축이 여전히 죽어 있다

### 4-1. 무엇이 누락돼 있었나

`resolveFacilityStats` 는 거동별로 피해가 실리는 필드를 갈라 쓰는데, 스포너 분기가 없어
`spec.damage`(스포너는 **0**)를 읽었다. `scale(0) === 0` 이다. 드론 내구도는 아예 로스터
원본(`EnemyDef.hp` 34)을 그대로 썼다. 즉 **레벨·등급·승급이 바꾸는 것은 설비 내구도뿐**이었다.
`gravwell` 에서 한 번, 압축 프레스에서 한 번 잡았던 것과 **같은 계열의 세 번째 사례**다.

### 4-2. 조치

| 값 | 원본 | 스케일 | 상한 |
|---|---|---|---|
| 드론 접촉 피해 | `ENEMY_BY_TYPE[spawnEnemyType].contactDamage` (12) | 3축 + 방어체 어픽스(`e.damage` 경로 그대로) | **120** |
| 드론 내구도 | 카탈로그 `spawnDroneHp` (75) | 3축 (`FacilityStats.droneHp` → 스폰 시 `e.timer` 에 굳힌다) | 없음 |

- 접촉 피해 상한 **120** 은 난이도 복원 레인 §3.2 의 임계(참조 플레이어 최대 HP 160)를
  넘지 않게 하는 장치다. 시드 램프 최상단(lv29 · 등급 3 · 승급 3)의 실효값은 **77** 이라
  상한이 무는 곳은 lv99 외삽뿐이다. 격리 lv99 실측에서 `hits 53 ≠ deaths 24` 라 **포화
  지문이 나타나지 않는다**(대조: 같은 조건에서 `rail` 8/8 · `shock` 11/11 · `siegecannon`
  15/15 · `demolisher` 12/12 로 전부 포화 지문이다).
- 촉매·스테이지 배율은 `summonEnemy` 가 로스터 기본값에 이미 곱해 둔 것을 **비율로** 옮겨
  실어 효력을 잃지 않는다(촉매 중립이면 비율이 1 이라 항등).
- 드론 내구도는 `e.timer` 로 나른다. `facilityGun` 의 발사 카운트다운 슬롯이고 스포너에서는
  쓰이지 않던 자리라 **신규 해시 필드가 늘지 않는다**.

### 4-3. 왜 `spawnDroneHp` 라는 새 필드가 필요했나

로스터 기본값(파쇄차 34)으로는 **레벨을 올려도 접촉 자체가 일어나지 않는다.** 참조봇 화력에
평균 **800유닛 밖**에서 전멸하기 때문이다 — 위치를 고쳐도 lv17 까지 누적 피해가 0 이었고,
사출 간격을 150 → 60 → 30 으로 조여 드론 수를 2.6배로 늘려도 lv17 이 여전히 0 이었다
(간격 30 · lv1 에서 1,714기를 낳고 200유닛 안에 들어온 적이 **0회**). **기여의 병목은 수가
아니라 생존이다.** 방어 설비의 사출 드론은 같은 typeIndex 의 야생 개체와 다른 유닛이라는
전제를 데이터로 명시했다.

### 4-4. 값 선정 — 상위 설비(박격포)의 램프 구간 곡선에 맞췄다

`spawnDroneHp` 를 34/50/60/75/90/140 으로 쓸어 24시드 격리 스윕을 돌리고, 같은 하네스의
박격포(catalogId 2)와 **시드 램프 구간(lv1~29)** 에서 겹치는 값을 골랐다.

| lv | 1 | 5 | 10 | 17 | 25 | **29(램프 상한)** |
|---|---|---|---|---|---|---|
| 박격포(대조) | 8 | 22 | 98 | 378 | 805 | **962** |
| `spawnDroneHp` 60 | 0 | 0 | 12 | 6 | 158 | ≈300 |
| **`spawnDroneHp` 75 (채택)** | 0 | 0 | 6 | **126** | **502** | **915** |
| `spawnDroneHp` 90 | 0 | 12 | 27 | 213 | 1,192 | ≈1,700 |
| `spawnDroneHp` 140 | 6 | 102 | 465 | 1,628 | 3,342 | (포화) |

90 이상은 램프 상한에서 박격포를 크게 넘고 lv25 부터 패배가 나오기 시작한다. 60 은 lv17 이
사실상 0 이다. **75 가 램프 상한에서 박격포와 나란히 서고(915 대 962) 램프 전 구간에서
패배 0** 이다.

---

## 5. 강화 3축 스윕 — 전/후

### 5-1. 레벨 (24시드 · `rarity 0` · `ascension 0` · 격리 12소켓)

| lv | 1 | 5 | 10 | 17 | 25 | **29** | 35 | 50 | 70 | 99 |
|---|---|---|---|---|---|---|---|---|---|---|
| **전** L2 누적 피해 | 24 | 12 | 12 | 12 | 12 | — | 12 | 12 | 12 | 12 |
| **후** L2 누적 피해 | 0 | 0 | 6 | **126** | **502** | **915** | 1,947 | 3,548 | 4,119 | 4,210 |
| **후** 최근접 평균 거리 | **913** | 873 | 826 | 763 | 671 | **650** | 631 | 630 | 689 | 747 |
| **후** 600유닛 안 틱 비율 | 27.7% | 34.0% | 40.4% | 49.0% | 61.3% | **62.6%** | 63.9% | 62.3% | 56.6% | 51.7% |
| **후** 200유닛 안 틱 | 1 | 10 | 161 | 650 | 1,807 | **2,616** | 3,546 | 4,618 | 4,268 | 3,330 |
| **후** 패배 수 | 0 | 0 | 0 | 0 | 0 | **0** | 5 | 17 | 23 | 24 |

**읽는 법**

- **최근접 평균 거리가 15,203 → 913 이다.** "구조적으로 못 따라붙는다"는 판정 근거 자체가
  사라졌다. 이 지표는 **lv1 부터 lv50 까지 엄격히 단조**다 — 누적 피해가 lv1~5 에서 0 인
  구간에서도 축이 살아 움직인다는 물증이다.
- **누적 피해는 lv1~5 에서 0 이다.** 드론이 참조봇 화력을 뚫고 접촉하는가라는 **문턱**이 기여를
  지배하기 때문이고, 저레벨에서는 그 문턱을 못 넘는다. 대조군 박격포도 lv1 이 8(피격 1회)이라
  성질이 같다. 레벨이 사는 것은 곧 **드론의 생존**이다.
- **결론의 무게는 lv1~29 에 둔다.** 시드 램프의 레벨 상한은 `1 + 3(nn−1)/2`, nn ≤ 20 → **29** 다.
  그 구간에서 패배는 0 이고 축은 단조다.
- **lv35 이상은 지표가 무효인 외삽 구간이다.** 12소켓 전부 고레벨 스포너라는 배치는 시드
  램프에 존재하지 않고, 이 구간에서는 참조봇이 실제로 죽어 L2 측정 구간 자체가 짧아진다
  (`l2ticks` 82,600 → 15,862). 같은 성질이 `rail`·`shock`·`siegecannon`·`demolisher` 에서도
  lv99 에 나타난다(§4-2 의 포화 지문 대조).

### 5-2. 등급 · 승급 (lv17 고정 · 24시드)

| 축 | 0 | 1 | 2 | 3 |
|---|---|---|---|---|
| 승급(`rarity 0` 고정) | 126 | 192 | 248 | **496** |
| 등급(`ascension 0` 고정) | 126 | 192 | 578 | **1,244** |

셋 다 단조 증가다.

> ⚠️ 등급 축은 **스탯 배율과 어픽스 롤이 섞인 값**이다(등급이 어픽스 개수를 정한다).
> 순수 배율만 보려면 `tests/invasionFacility.test.ts` 의 "강화 3축이 생산물에 실린다" 단언이
> 드론 내구도·접촉 피해를 직접 비교한다. 이 표는 **체감 기여**의 단조성을 보는 표다.
> 선행 문서들이 경고한 "`rarity 3` + `affixSeed 0` 으로 재지 마라"는 **레벨 축을 잴 때의
> 기준선** 이야기이고, 위 레벨 스윕은 규율대로 `rarity 0` 이다.

---

## 6. 선택지 ③ — 피해 축을 포기하고 다른 역할 (**기각**)

### 6-1. 구조적 사유 — 차폐물은 이 리포에서 이미 한 번 게임을 망가뜨렸다

`.omc/research/invasion-core-shot-block-2026-07-27.md`(PR#160)가 정본이다. 실드 발생기 국면의
코어가 **자동 조준에서만 빠지고 아군탄 충돌 화이트리스트에는 남아** 반지름 90 짜리 무적
차폐물로 작동했고, 참조봇은 그 그림자에 들어가면 영원히 빠져나오지 못했다 — **패배 런의
100.0%** 가 그 상태였다. 차폐를 **역할로 설계하면** 그 실패 모드를 의도적으로 재도입하는 것이고,
방어 측이 12소켓을 전부 그걸로 채울 수 있으므로 "회랑 전체가 통과 불가"가 배치로 만들어진다.

### 6-2. 지표 사유 — 새 지표를 정의해야 하는데, 그럴 필요가 없어졌다

`gravwell` 선례(감속 적용 틱)를 따르면 차폐 역할의 지표는 "아군탄이 드론에 흡수된 수" 같은
값이 된다. 그런데 **채택안이 그 역할을 이미 부수 효과로 제공한다** — 정면으로 오는 드론은
`hp > 0` 인 실체라 참조봇의 탄을 실제로 먹는다(§5-1 의 "600유닛 안 틱 비율"이 그 노출량의
대리 지표다). 차이는 **부술 수 있다**는 것뿐이고, 그 차이가 정확히 PR#160 이 만든 사고를
막는 지점이다. 무적 차폐를 새로 만들 이유가 없다.

### 6-3. 회수 방해·시야 차단을 고르지 않은 이유

- **젬·전리품 회수 방해**: 침공 L2 에서 젬은 자석 반경 안에 들어오면 즉시 회수되고, 방해
  메커니즘은 `stepGems` 라는 **전 모드 공용 경로**를 건드려야 한다(회귀 표면이 ① 과 같다).
- **시야 차단**: 이 sim 에 플레이어 시야(FoW) 개념 자체가 없다. 렌더 레이어에만 존재하는
  차단은 sim 지표로 잴 수 없어, 강화축 단조성을 **검증할 방법이 없다**. 과제가 요구한
  "지표를 정의하고 단조 증가를 검증하라"를 만족시킬 수 없다.

---

## 7. 회귀 대조

### 7-1. 다른 설비 — 7종 × 4레벨 × 24시드 = **28행 바이트 동일**

같은 트리에서 변경본과 되돌림본(`git stash`)을 각각 돌려 행 문자열을 `diff` 했다.
비교 항목은 L2 누적 피해 · 피격 수 · 사망 수 · L2 틱 합 · L3 도달 · 승리 수 · 플레이어 x 평균
전부다.

| 대조군 | `rapid`(0) · `mortar`(2) · `laser`(3) · `press`(6) · `gravwell`(7) · `shock`(8) · `demolisher`(16) |
|---|---|
| 레벨 | 1 / 17 / 50 / 99 |
| 행 수 | **28** |
| **차이** | **0** |

당연한 결과다 — 바뀐 경로는 전부 `behavior === FACILITY_BEHAVIOR_SPAWNER` 게이트 뒤에 있고,
카탈로그에서 그 거동은 **17종 중 1종**(catalogId 5)뿐이다. `FacilitySpec` 에 새로 붙인
`spawnDroneHp` 는 `BASE` 가 0 이라 다른 16종의 값이 한 비트도 바뀌지 않는다.

### 7-2. 침공 밴드 (24시드)

| 밴드 | 전(`5bf088d`) | **후** | 목표 |
|---|---|---|---|
| 하위 (#01~#07) | 100.00 | **100.00** | 85+ ✓ |
| 중하 (#08~#14) | 71.43 | **72.02** | 55~80 ✓ |
| 중위 (#15~#20) | 31.94 | **29.86** | 25~55 ✓ |

**세 밴드 모두 목표 안에 남는다.**

기지별:

| | #08 | #09 | #10 | #11 | #12 | #13 | #14 |
|---|---|---|---|---|---|---|---|
| 전 | 100 | 100 | 91.7 | 58.3 | 16.7 | 50.0 | 83.3 |
| **후** | 100 | 100 | 91.7 | 62.5 | 12.5 | 50.0 | 87.5 |

| | #15 | #16 | #17 | #18 | #19 | #20 |
|---|---|---|---|---|---|---|
| 전 | 41.7 | 4.2 | 8.3 | 41.7 | 29.2 | 66.7 |
| **후** | 54.2 | 8.3 | **0.0** | 33.3 | 20.8 | 62.5 |

스포너는 시드 램프상 `facilityKinds = min(9, 2 + nn·2/5) > 5` 인 **nn ≥ 10** 에서만, 그것도
소켓 1자리에 배정된다. #08·#09 가 한 자리도 안 움직인 것이 그 대조군이다.

### 7-3. ⚠️ `#17` 이 24시드에서 0 이 된다 — **클리어 불가가 아니라 표본 문제다**

96시드로 승격해 재판정했다:

| 기지 | 전(96시드) | **후(96시드)** |
|---|---|---|
| #17 | 11/96 = **11.46%** | 6/96 = **6.25%** |

**#17 은 여전히 클리어 가능하다.** 승률 6.25% 인 기지가 고정 24시드에서 0 으로 관측될 확률은
0.9375²⁴ ≈ **21%** 이고, 이번이 그 경우다. `tests/invasionBalance.test.ts` 의 `BALANCE_SEEDS`
주석이 "sim 이 조금만 어려워지면 이 단언이 먼저 깨진다"고 예고한 바로 그 지점이다.

#### 강도를 낮춰서는 못 되돌린다 — 실측

`#17` 이 24시드 승리를 잃는 것은 스포너가 **세져서**가 아니다. 사출 위치 변경 자체가 원인이다.

| 구성 | #17 (24시드) |
|---|---|
| 변경 전 | 8.3% (2/24) |
| 위치만 변경(드론 내구도 = 로스터 34) | **0.0%** |
| `spawnDroneHp` 50 | **0.0%** |
| `spawnDroneHp` 60 | **0.0%** |
| `spawnDroneHp` 75(채택) | **0.0%** |
| 사거리 600 · 간격 600 · 상한 1 (**사실상 무력화**) | **0.0%** |

**런당 드론이 한 자릿수인 구성에서도 결과가 같다.** 이 지표가 `#16`·`#17` 부근에서 거의
**계단 함수**라는 난이도 복원 레인 §5.1 의 진단이 다시 확인된 것이고, `#17` 의 2승은 난이도
여유가 아니라 **파워업 추첨 실현 하나**였다는 뜻이다.

#### 처방 — 상한을 여는 대신 판정 해상도를 올렸다

`tests/invasionBalance.test.ts` 의 "클리어 불가 기지가 없다" 단언을 다음과 같이 바꿨다:

> 24시드에서 승률 0 인 기지가 나오면 **그 기지 하나만** 96시드로 다시 돌려 재판정한다.

- 단언이 재려던 명제("**수학적으로** 클리어 불가한 기지가 있는가")는 **강해진다**.
- 상시 CI 비용은 **0** 이다 — 0 인 기지가 없으면 승격이 아예 돌지 않고, 승격도 첫 승리에서 끊는다.
- 상한을 여는 방향(그 주석이 금지한 방향)이 아니다.

근본 처방은 여전히 **기지 구성 정상화**이고(난이도 복원 레인 §5.1 이 `#16` 에 대해 남긴 항목과
같은 계열), 그건 램프 마이그레이션 레인의 몫이다 — §8-1.

### 7-4. 로스터 간섭 게이트 재기준 (24시드 · 기체 1~6)

| 기지 | 전 | **후** |
|---|---|---|
| #12 | 50.0 / 37.5 / 50.0 / 33.3 / 83.3 / 70.8 (33.3~83.3) | 54.2 / 41.7 / 33.3 / 33.3 / 83.3 / 75.0 (**33.3~83.3**) |
| #20 | 79.2 / 70.8 / 70.8 / 70.8 / 83.3 / 87.5 (70.8~87.5) | 79.2 / **33.3** / 66.7 / 66.7 / 79.2 / 87.5 (33.3~87.5) |

- **`#12` 는 폭이 한 칸도 안 움직였다** → `lo 5 / hi 92` 그대로.
- `#20` 은 기체 2 가 70.8 → 33.3 으로 내려와 `lo 62` 를 위반한다. 정면으로 오는 드론이
  **기체별 회피 성능을 실제로 가른다**는 뜻이고(나머지 5기체는 66.7 이상 유지), 게이트가
  재려는 "기체 간 격차"가 **커진** 것이지 게이트가 무뎌진 것이 아니다.
  → `#20 lo 62 → 25`. 새 바닥 33.3 아래로 **2눈금**(29.2 · 25.0)으로, 5세대와 같은 여유 규율이다.
- 담당 배정(#12 상한 · #20 하한)은 유지한다. `#20` 의 바닥 33.3 은 여전히 `#18`(25.0) ·
  `#19`(8.3)보다 높다(재측정: #18 = 66.7/25.0/25.0/25.0/62.5/70.8 ·
  #19 = 20.8/8.3/12.5/25.0/33.3/41.7).

#### 감도 실증 (이 트리에서 수행하고 원복 · `data/ships/bruiser.ts` `baseBp` 4축)

| 변조 | 결과 | 판정 |
|---|---|---|
| `{-9000,-9000,-9000,-9000}` | #20 기체 1 = 79.2 → **0.0** · #12 기체 1 = 54.2 → **0.0** | `#20 lo 25` · `#12 lo 5` 위반 → 실패 ✓ |
| `{20000,20000,20000,20000}` | #12 기체 1 = 54.2 → **100.0** · #20 기체 1 = 79.2 → **100.0** | `#12 hi 92` 위반 → 실패 ✓ |

두 변조 모두 **기체 2~6 열은 한 자리도 바뀌지 않았다** — 하네스가 변조 기체만 격리해 잰다는
증거다. 원복 후 두 기지·전 기체 그린.

### 7-5. 침공 골든 해시

`tests/fixtures/encounter-baseline.json` 의 침공 골든은 `catalogId 1`(`rail`)로 12소켓을
채우므로 스포너를 스폰하지 않는다 — **바이트 불변**이고 재녹화가 필요 없다(전량 통과로 확인).

> ⚠️ **골든 불변은 EF 재배포 불필요의 근거가 못 된다** — 이 리포가 반복해서 경고한 함정이다.
> 실제 침공 기지 #10~#20 은 스포너를 스폰하므로 서버 재실행(`verify-invasion`)이 새 sim 을
> 알아야 한다. **`src/sim/**` · `data/**` 가 바뀌었으므로 재배포는 필수다**(§9).

---

## 8. 이 레인이 손대지 않은 것

### 8-1. `#17` 구성 정상화

여유가 96시드 6승뿐이다. 난이도 복원 레인 §5.1 이 `#16` 에 대해 남긴 항목과 같은 계열이고,
램프 SQL(`supabase/migrations/…_invasion_band_restore.sql`)을 덮어쓰는 신규 마이그레이션이
필요하므로 이 레인 범위 밖이다.

### 8-2. L2 레이어 길이 대 회랑 길이 (§0-1)

**주파 길이 71,280 대 회랑 길이 12,000.** 플레이어는 L2 의 83% 를 빈 공간에서 보낸다. 이건
스포너만의 문제가 아니라 **모든 L2 설비의 실효 노출 시간을 6분의 1로 깎는 구조**다. 고치면
(회랑을 늘리든 레이어 예산을 줄이든) 설비 17종 전체가 동시에 세지므로 밴드 전면 재측정이
필요하다 — **다음 레인 후보 1순위로 남긴다.**

### 8-3. 사람 기준 난이도

참조봇에는 회피 의도가 없다(난이도 복원 레인 §3.1). 정면에서 오는 드론이 사람에게 어떤
난이도인지는 이 계측으로 알 수 없다 — 플레이테스트 항목이다.

---

## 9. 게이트

| 항목 | 결과 |
|---|---|
| `npx tsc --noEmit` | 0 |
| `npx eslint . --max-warnings 0` | 0 |
| `npx vitest run` | 189 파일 / 3,598 테스트 전량 통과 |
| `npx vite build` | ✓ |
| 임시 계측 파일 | 삭제 완료 |
| `verify-invasion` EF 재배포 | **필수**(`src/sim/**` · `data/**` 변경). 절차 정본은
  `.omc/skills/planet-blitz-supabase-deploy-workflow.md` + 루트 `README.md` `## 서버 배포` |

## 10. 재현 방법

임시 계측 파일은 규율대로 삭제했다. 재현하려면 `tests/` 에 아래 골자로 다시 만든다.

- 배치: `templateId 0` · 12소켓 전부 `catalogId 5` · `l1.waveSlots` 전부 `null` ·
  `l3.boss/props/guardians` 전부 `null` · `rarity 0` · `ascension 0` · `affixSeed 0`
- 런: `createWorld` → `stepWorld(state, autopilotInput(state))` 정규 경로 · `INVASION_TOTAL_TICKS`
- 집계: `state.invasion3.phase === 1` 인 틱에서 플레이어 HP 감소 합 · 피격 수 · 사망 수와,
  같은 틱의 스포너 자식(`enemy.aux1 !== 0`) 중 **최근접 거리** · 동시 생존 수 · 누적 사출 수
- 시드: `src/bench/rosterBench.ts` 의 `SEEDS` 앞 24개(승격 판정은 앞 96개)

> ⚠️ **빈 소켓은 비어 있지 않다.** `null` 소켓은 기본 수비대(속사포)가 자동 충원한다.
> 단일 설비 격리를 원하면 12소켓을 **전부** 채워라.
>
> ⚠️ **`rarity 3` + `affixSeed 0` 으로 레벨 축을 재지 마라** — 12소켓이 전부 같은 어픽스를
> 받아 강화축과 섞인다(선행 문서 머리말과 같은 함정).
