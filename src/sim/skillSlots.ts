
/**
 * 스킬 슬롯의 **폭·접근 규약과 7기체 배정표**(ADR-0049 S0 공유 기반).
 *
 * `WorldState.skillCarry` / `WorldState.skillStage` 두 배열의 **유일한 접근 경로**이고,
 * 7기체가 어느 슬롯 번호를 무엇에 쓰는지 한 파일에서 볼 수 있게 모아 두는 자리다.
 * 순수 leaf 다 — 런타임 import 가 0 이라 `world.ts` 와 순환이 구조적으로 생기지 않는다.
 *
 * ## 왜 고정폭 8 인가
 * 기체 7문서의 `구현: B` 전수 집계상 **한 런의 동시 필요분 최대 6칸**이다(런당 기체가 1대라
 * 36칸이 동시에 필요할 수 없다). 폭 8 = 실측 6 + 여유 2.
 *
 * ⚠️ **"넉넉하게 32"는 반대다.** 미배정 슬롯이 영구 0 으로 남으면 오인덱스가 **조용한
 * 무연산**이 된다 — 이 저장소의 지배적 실패 모드다. 그래서 폭을 좁게 두고, 아래 접근자가
 * 범위 밖 인덱스를 **던져서** 알린다(0 을 돌려주면 그 조용한 무연산이 되살아난다).
 *
 * ## 두 배열의 차이 — 섞어 쓰지 마라
 *  - `skillCarry` = **구간을 넘어 사는 상태**(런당 1회 소진 표식 · 누적 저금 · 락온 스택).
 *    의뢰 다구간 런에서 `carryAcrossSegment` 가 **값 복사**로 승계한다(참조 공유 아님 —
 *    그 함수 주석이 근거).
 *  - `skillStage` = **구간마다 새로 시작하는 상태**(창 잔여 틱 · 이번 구간 킬 스냅샷).
 *    승계하지 않는다 = 새 월드의 0 초기값을 그대로 쓴다.
 *
 * ## 값 규약 (`hashWorld` 조건부 폴드의 전제)
 *  1. **0 = "없음"** 이다. `-1` 센티넬을 쓰지 마라 — `(-1) >>> 0 === 4294967295` 라 폴드가
 *     켜져 미투자 런의 해시가 갈린다. 엔티티 id 는 1부터라 0 이 자연 센티넬이다.
 *  2. **비음 정수만.** {@link writeSlot} 이 `Math.trunc` + `max(0, ·)` 를 강제한다. 소수를
 *     넣으면 u32 폴드에서 소수부가 조용히 잘려 클라와 서버 재실행이 갈린다.
 *  3. **모든 쓰기는 투자 게이트 안쪽**이다(`skillLv(...) >= 1`). 게이트 밖에서 쓰면
 *     "전 슬롯 0 이면 무폴드" 가 깨진다.
 *  4. **슬롯 번호는 기체별로 겹친다**(런당 기체 1대라 가능). 그래서 다른 기체 코드가 게이트
 *     없이 쓰면 안 된다 — 쓰기는 항상 앵커의 `switch (state.sigBit)` 안에서만 일어난다.
 */

/**
 * 앵커 ④(`onPlayerDamaged`)의 **피해원 비트** — 그 틱 피격에 기여한 접촉원의 종류.
 *
 * ## ⚠️ 왜 단일 유니온이 아니라 **비트합**인가
 * `world.ts` 의 수집 루프는 피해를 **더하지 않고 `max`** 로 고른다(`if (t.damage > dmg)`).
 * 단일 유니온으로 "이긴 쪽" 하나만 실으면, 같은 틱에 적탄(20)과 몸통 접촉(10)이 함께 닿았을 때
 * 접촉 사실이 `max` 에 통째로 삼켜진다 — 접촉을 트리거로 쓰는 스킬(브루저 BL8)이 조용히
 * 미발동한다. 설계 정본(`bruiser.md` BL8)이 *"max 가 적탄이어도 접촉 기여가 있으면 적립"* 이라고
 * 명시한 것이 바로 이 실패 모드다. 그래서 **기여한 종류를 전부** 싣는다.
 *
 * ⚠️ 비트는 **`t.damage > 0` 인 접촉원만** 세운다 — 피해 0 짜리 접촉은 `dmg` 에 한 점도
 * 기여하지 않으므로 "피해원" 이 아니다.
 *
 * ## 왜 `skillHooks.ts` 가 아니라 여기인가
 * 스킬 모듈(`skills/*.ts`)이 이 값을 **런타임에** 읽어야 하는데, 그 파일들의 `skillHooks.js`
 * import 는 전부 `import type` 다 — `skillHooks` 가 스킬 모듈을 런타임 import 하므로 값 import
 * 를 열면 순환이 생긴다. 이 파일은 import 0 인 순수 leaf 라 그 순환이 구조적으로 없다.
 */
export const DamageSource = {
  /** 몸통 접촉 — `enemy`·`boss`·`guardian`·`defenseBoss`(설계 R-4: kind 전부 포함). */
  contact: 1,
  /** 적탄 — `enemyBullet`. */
  bullet: 2,
  /** 해저드 장판 — `hazard`(활성 상태). */
  hazard: 4,
} as const;

/** {@link DamageSource} 의 개별 비트. */
export type DamageSourceBit = (typeof DamageSource)[keyof typeof DamageSource];

/** {@link DamageSource} 비트합. 0 은 원리적으로 오지 않는다(`dmg > 0` 이 앵커의 전제다). */
export type DamageSourceMask = number;

/** 비트합에 그 피해원이 들어 있는가. */
export function hasDamageSource(mask: DamageSourceMask, bit: DamageSourceBit): boolean {
  return (mask & bit) !== 0;
}

/**
 * 두 배열의 **고정 폭**. `hashWorld` 가 이 상수 × 2 칸을 고정 폭으로 접으므로, 늘리면
 * 골든이 전량 재생성 대상이 된다 — 늘리기 전에 `replay.ts` 의 스킬 슬롯 폴드 주석을 읽어라.
 */
export const SKILL_SLOT_COUNT = 8;

/** 전 슬롯 0 인 새 배열. `createWorld` 가 두 벌을 각각 이것으로 만든다. */
export function createSkillSlots(): number[] {
  return new Array<number>(SKILL_SLOT_COUNT).fill(0);
}

function assertSlot(slot: number): void {
  if (!Number.isInteger(slot) || slot < 0 || slot >= SKILL_SLOT_COUNT) {
    throw new Error(`skill slot out of range: ${slot} (폭 ${SKILL_SLOT_COUNT})`);
  }
}

/**
 * 슬롯 읽기. **범위 밖은 던진다** — 0 을 돌려주면 오인덱스가 조용한 무연산이 되는데, 그것이
 * 폭 8 을 고른 이유 자체다(헤더 참조). 슬롯 번호는 항상 아래 배정표의 `const enum` 리터럴이라
 * 정상 경로에서는 도달하지 않는다.
 */
export function readSlot(slots: readonly number[], slot: number): number {
  assertSlot(slot);
  return slots[slot] ?? 0;
}

/**
 * 슬롯 쓰기 — **`Math.trunc` + `max(0, ·)` 강제**(값 규약 2). 이 함수를 우회해 배열에 직접
 * 대입하지 마라: 소수·음수가 들어가면 u32 폴드에서 조용히 다른 값이 되어 클라와 검증 EF 가
 * 갈린다(`aux0` 소수 적립이 같은 형태로 남긴 경고와 동일).
 */
export function writeSlot(slots: number[], slot: number, value: number): void {
  assertSlot(slot);
  const n = Math.trunc(value);
  slots[slot] = n > 0 ? n : 0;
}

// ---------------------------------------------------------------------------
// 7기체 슬롯 배정표 — **각 배선 레인이 자기 블록만 채운다**
// ---------------------------------------------------------------------------
//
// ## 채우는 규약
// 레인은 아래 자기 기체 자리에 `const enum` 두 벌(`{Ship}Carry` · `{Ship}Stage`)을 선언하고,
// 슬롯 번호가 **자기 블록 안에서 유일한지**만 보장하면 된다(기체 간 중복은 정상 — 런당
// 기체 1대). 형태:
//
//     /** 스트라이커 이월 슬롯. */
//     export const enum StrikerCarry {
//       lethalSpent = 0,   // FO5 치명 생존 무효화 소진 표식(런당 1회)
//     }
//     /** 스트라이커 구간 슬롯. */
//     export const enum StrikerStage {
//       wallWindow = 0,    // S4 벽 접촉 창 잔여 틱
//     }
//
// ⚠️ **`Carry` 와 `Stage` 를 한 enum 에 섞지 마라.** 두 배열은 승계 규칙이 정반대라, 섞으면
// "구간을 넘어 살아야 하는 표식"이 구간마다 리셋되는(또는 그 반대의) 결함이 **화면에도
// 테스트에도 흔적을 안 남기고** 들어온다.
//
// ⚠️ **여기 없는 번호를 `world.ts`/`skills/{ship}.ts` 에 리터럴로 적지 마라.** 이 파일이
// 중복·누락을 한눈에 보이게 하려고 존재한다.
//
// S0 시점에는 **전 기체 미배정**이다 — 그래서 전 슬롯이 런 끝까지 0 이고 `hashWorld` 의
// 스킬 슬롯 폴드가 한 번도 실행되지 않는다(기존 골든 바이트 불변).
//
//   스트라이커 (SIG_STRIKER_MARKSMAN) — StrikerCarry / StrikerStage
//   아크캐스터 (SIG_ARC_OVERCHARGE)   — ArccasterCarry / ArccasterStage
//   브루저     (SIG_BRUISER_ARMOR)    — BruiserCarry / BruiserStage
//   해츨링     (SIG_HATCHLING_BROOD)  — HatchlingCarry / HatchlingStage
//   말로우     (SIG_MALLOW_CUSHION)   — MallowCarry / MallowStage (S2 확장으로 각 1칸 — SQ8·SQ5)
//   팬텀       (SIG_PHANTOM_CLOAK)    — PhantomCarry / PhantomStage
//   버블       (SIG_BUBBLE_FILM)      — BubbleCarry(0칸) / BubbleStage(배치6 에서 1칸 — DR3)
//
// ⚠️ 이 색인은 **병렬 배선 머지에서 중복되기 쉽다** — 레인마다 자기 줄을 고치는데 git 이
// 양쪽을 다 살리기 때문이다(실제로 브루저·아크캐스터가 2줄씩 중복된 채 들어왔다).
// 머지 후 이 목록의 기체 수가 **정확히 7** 인지 눈으로 확인하라.

/**
 * **스트라이커 이월 슬롯**(ADR-0049 배치 1). 효과 본체는 `src/sim/skills/striker.ts`.
 *
 * 둘 다 S10「선체 증축」의 저금이라 **구간을 넘어 살아야 한다** — 의뢰 다구간 런에서 구간이
 * 바뀔 때마다 리셋되면 400XP 임계에 영영 못 닿는 구간이 생긴다. 그래서 `Stage` 가 아니라
 * `Carry` 다.
 */
export const enum StrikerCarry {
  /**
   * S10 — **직전에 관측한 `state.xp`**. `state.xp` 는 레벨업마다 `-= need` 되는 잔여 풀이라
   * 누적이 아니고, 그래서 "늘어난 만큼만" 적립하려면 직전 값이 필요하다. 0 = 아직 관측 없음
   * (런 시작 xp 가 0 이라 자연 센티넬 — 값 규약 1).
   */
  hullXpSeen = 0,
  /** S10 — 아직 임계(400)를 못 넘긴 **잔여 누적 XP**. 지급할 때마다 400 씩 뺀다. */
  hullXpPool = 1,
}

/**
 * **스트라이커 구간 슬롯** — 배치 1 이 배선한 9종은 한 칸도 안 썼고, **배치 5 가 M10 으로
 * 2칸을 처음 잡았다**(그래서 `unassigned` 자리표시자를 지웠다).
 *
 * 설계서가 `구현: B`(신규 상태)로 표시한 스트라이커 6종 중 배치 1 이 닿은 것은 M5·S4 둘인데,
 * 둘의 술어("직전 틱 벽 접촉")는 S0 의 E5 가 세운 {@link WorldState.wallContactTicks} 가 이미
 * 제공한다 — 같은 술어를 슬롯에 다시 적으면 두 곳이 조용히 갈린다. 남은 셋(F7 락온 정수 2 ·
 * F8 창 연장 카운터 1 · S9 적 정지 틱)은 아직 앵커 밖이라 **코드가 없고**, 그 레인이 자기
 * 번호를 여기에 선언한다.
 *
 * ## 왜 `Carry` 가 아니라 `Stage` 인가
 * M10 의 재충전은 Lv1 에서 906틱(약 15초)이라 아크캐스터 `backflowCooldown`(3484틱)과 달리
 * 의뢰 구간 길이 안에 들어온다. 구간 전환에서 리셋되면 "여분 대시 1회를 구간마다 새로 받는다"
 * 가 되는데, 그것이 곧 **구간이 짧을수록 이득**이라는 부호이고 설계서의 억제(느린 재충전)를
 * 우회한다 — 고 볼 수도 있으나, 반대로 `Carry` 로 두면 **보류 중인 기본 충전 잔여**(아래
 * `twinHold`)가 구간을 넘어 살아남아 새 구간 첫 틱의 `dashCooldown` 을 과거 값으로 되돌린다.
 * 후자가 더 나쁘다(대시 게이트가 구간 경계에서 조용히 갈린다). 그래서 `Stage` 이고, 구간
 * 전환의 여분 1회는 **의도된 수용**이다.
 *
 * ⚠️ 지금 예약 번호를 미리 적지 마라 — 미사용 슬롯이 영구 0 으로 남는 것이 이 파일 헤더가
 * 폭을 8 로 좁힌 이유 자체다(오인덱스가 조용한 무연산이 된다).
 */
export const enum StrikerStage {
  /**
   * M10「이중 추진」 — **2번째 충전의 재충전 잔여 틱**. `0` = 충전 완료(= 여분 대시 보유).
   *
   * 값 규약 1 그대로 0 이 자연 센티넬이고, 그래서 **런 시작이 곧 "여분 1회 보유"** 다(설계서
   * "1레벨: 비상용 여분 대시 1회").
   */
  twinRecharge = 0,
  /**
   * M10 — **임시 개방 중 보류한 기본 충전 잔여 + 1**. `0` = 보류 없음.
   *
   * ⚠️ **+1 이 인코딩의 전부다.** `writeSlot` 이 음수를 0 으로 깎으므로 "기본 충전 잔여가
   * 0 인데 보류 중" 과 "보류 없음" 을 날값으로는 구분할 수 없다. 그래서 1 을 얹어 저장하고
   * 읽는 쪽이 뺀다 — 이 오프셋을 지우면 보류 해제가 한 틱 일찍 일어나 기본 충전이 공짜로
   * 회복된다.
   */
  twinHold = 1,
  /**
   * F7「표적 고정」 — **직전 명중 표적의 `Entity.id` + 1**. `0` = 표적 없음.
   *
   * ⚠️ **+1 이 인코딩의 전부다.** 플레이어가 `state.entities[0]` 이라 엔티티 id 0 은 아군탄의
   * 표적이 될 수 없지만, 그렇다고 날값 0 을 "표적 없음" 으로 쓰면 **id 0 을 가진 개체가 언젠가
   * 표적이 되는 순간** 스택이 영영 안 쌓인다(조용한 무연산). 그래서 1 을 얹어 저장하고 읽는
   * 쪽이 뺀다 — M10 `twinHold` 와 같은 관용구다.
   *
   * ⚠️ **`Stage` 다.** 락온은 "지금 쏘고 있는 표적" 이라 구간을 넘겨 살아야 할 이유가 없고,
   * 살아남으면 새 구간 첫 명중이 **이전 구간에서 죽은 개체의 id** 와 우연히 같을 때
   * (id 는 구간마다 0 부터 다시 세지 않는다는 보장이 없다) 스택을 물려받는다.
   */
  lockTarget = 2,
  /**
   * F7 — 같은 표적 **연속 명중 스택**. 표적이 바뀌면 0 으로 리셋된다.
   *
   * 첫 명중은 0 이다(그 명중은 증폭을 안 받는다). 설계 문면이 *"연속 명중할 때마다 쌓인다"*
   * 라 「쌓인 결과」가 증폭이고, 첫 발부터 증폭하면 락온이 아니라 상시 증폭이 된다.
   */
  lockStack = 3,
}

/**
 * **아크캐스터 이월 슬롯**(ADR-0049 배치 3). 효과 본체는 `src/sim/skills/arccaster.ts`.
 *
 * ⚠️ BR10「최후 접지」의 **「런당 1회」 억제는 여기 없다** — 플레이어 `targetX` 를 쓴다
 * (`skillHooks.ts` 의 `survivedLethalBlow` 주석이 지정한 칸이고, 이미 `commissionCarry` 의
 * `ENTITY_CARRY` 에 있어 의뢰 다구간에서도 "런당 1회"가 정확히 성립한다). 슬롯을 잡으면
 * 같은 억제가 두 곳에 생겨 조용히 갈린다.
 */
export const enum ArccasterCarry {
  /**
   * BR6「전하 역류」의 **내부 쿨다운 잔여 틱**. 0 = 발동 준비됨(값 규약 1 — 0 이 자연 센티넬).
   *
   * `Stage` 가 아니라 `Carry` 인 이유: 쿨다운이 Lv1 에서 4800틱(80초)이라 의뢰 구간 길이를
   * 우습게 넘는다. 구간마다 리셋하면 "구간 전환 = 쿨다운 초기화"가 되어 억제가 사실상
   * 사라진다(설계서가 이 스킬의 억제로 명시한 것이 바로 내부 쿨다운이다).
   */
  backflowCooldown = 0,
}

/**
 * **아크캐스터 구간 슬롯.** 둘 다 구간을 넘겨 살면 안 되는 값이다.
 */
export const enum ArccasterStage {
  /**
   * BR4「잉여 전하 방벽」의 **적립된 흡수량**. 새 구간은 aux0 0 에서 다시 쌓는 것이 맞고,
   * 넘기면 구간 전환이 공짜 방어 자원이 된다.
   */
  surplusStore = 0,
  /**
   * CH4「진입 뇌격」의 **직전 틱 말 aux0 스냅샷**. 통과 판정(`이전 < 90 && 현재 ≥ 90`)의
   * 비교 기준이다. 구간이 바뀌면 월드가 새로 서고 aux0 도 0 이므로 넘기면 안 된다 —
   * 만충 상태로 구간을 마친 런이 새 구간 첫 틱에 **유령 통과**를 만든다.
   */
  entryAux0Seen = 1,
  /**
   * BA7「연발 축전기」의 **다음 볼리까지의 처치 진척**(0..6). 6 = 장전 완료 — 별도 플래그를
   * 두지 않고 **상한값 자체를 충전 표식으로 쓴다**(슬롯 1칸 절약 + 두 칸이 갈릴 여지 제거).
   * 볼리가 소비하면 0 으로 돌아간다.
   *
   * `Carry` 가 아니라 `Stage` 인 이유: 진척은 이번 구간의 전투 리듬이고, 구간을 넘겨 살면
   * 새 구간 첫 볼리가 **이전 구간의 처치로 장전된 채** 시작한다(억제가 구간 경계에서 새는
   * 형태 — BR4 흡수 적립과 같은 사유).
   */
  killCapacitorCharge = 2,
}

/**
 * **브루저 이월 슬롯**(ADR-0049 배치 2). 효과 본체는 `src/sim/skills/bruiser.ts`.
 *
 * 셋 다 **런 단위 자원**이라 구간을 넘어 살아야 한다 — 의뢰 다구간 런에서 구간마다 리셋되면
 * 응혈 풀은 영영 정산되지 못하고(만재 엣지가 구간 초반에 잘 안 뜬다), FO7 의 50% 상한은
 * 구간 수만큼 곱해져 상한이 사실상 사라진다. 그래서 `Stage` 가 아니라 `Carry` 다.
 */
export const enum BruiserCarry {
  /**
   * FO2 — **응혈 풀**. 실피격으로 잃은 HP 중 적립된 몫(정수 HP). 장갑이 상한에 도달하는
   * **상승 엣지**에서 60% 가 회복으로 정산되고 나머지 40% 는 소멸한다. 0 = 적립 없음.
   */
  clotPool = 0,
  /**
   * FO7 — **런 시작 최대 HP**(누적 상한 50% 의 기준선). 첫 `stepShipSignature` 에 한 번만
   * 잡는다. 0 = 아직 관측 없음 — `maxHp` 는 항상 양수라 자연 센티넬이다(값 규약 1).
   */
  trophyBaseHp = 1,
  /**
   * FO7 — 이 런에서 **FO7 이 실제로 준 최대 HP 누적분**. 파워업(`reinforced-hull` 등)이 같은
   * `maxHp` 를 수시로 가산해 차분으로는 FO7 몫을 분리할 수 없어 전용 칸이 필요하다(설계서 R-6).
   */
  trophyGranted = 2,
}

/**
 * **브루저 구간 슬롯**. 둘 다 구간이 바뀌면 새로 시작하는 것이 옳다 — 명중 카운터와 직전 스택
 * 관측값은 **그 구간의 진행 상태**이지 저금이 아니다(새 월드의 `aux0` 도 0 에서 다시 시작한다).
 */
export const enum BruiserStage {
  /**
   * FO2 — **직전 틱에 관측한 장갑 스택**. 만재 상승 엣지(`prev < 상한 && cur >= 상한`)를 재는
   * 데만 쓴다. 레벨 술어(`aux0 == 상한`)로 대체하면 fortify 액티브의 SUSTAIN 이 매 틱 만재를
   * 재설정하는 동안 **매 틱 정산**이 된다.
   */
  prevArmorStacks = 0,
  /**
   * BL9 — **N 주기 명중 카운터**. 강타가 터진 틱에 0 으로 되돌아간다. N 은 고정이 아니라
   * 장갑 스택에서 매 명중마다 파생되므로(`max(1, round(48/(4+스택)))`) 이 칸에는 카운트만 산다.
   */
  cadenceHits = 1,
  /**
   * BL8 — **담금질 탄 적립 수**. 몸통 접촉으로 실피격당할 때마다 1 씩 늘고(상한은 레벨 파생),
   * 다음 볼리의 선두탄이 **1 발** 소모한다. 0 = 적립 없음.
   *
   * 구간 슬롯인 이유는 BL9 와 같다 — 이 칸은 저금이 아니라 **그 구간의 탄약**이고, 새 무대에서
   * 다시 부딪혀 모으는 것이 설계다(*"몸으로 부딪히는 플레이에 보상"*).
   */
  temperCharges = 2,
  /**
   * MO4 — **장갑 활주의 전용 내부 쿨다운 잔여 틱**. 0 = 발동 준비됨(값 규약 1).
   *
   * ⚠️ **이 칸은 구조 필수다**(설계서 MO4 구현 태그 B — 1판 A 에서 승격). 감속 장판은
   * `world.ts` 의 적용부가 **매 틱 감속을 재부여**하므로, 내부 쿨이 없으면 장판 위 8틱에
   * 8스택이 증발한다. `playerSlowTicks` 잔여로 겸용하는 대안은 자기모순이다 — 무효화에
   * 성공하면 잔여가 0 이 되어 쿨 판정의 근거 자체가 사라진다.
   *
   * `Carry` 가 아니라 `Stage` 인 이유는 길이다 — 쿨은 Lv1 에서 180틱(3초)이라 의뢰 구간
   * 경계에서 초기화돼도 억제가 새지 않는다(아크캐스터 BR6 이 4800틱이라 `Carry` 인 것과
   * 정확히 반대 근거다).
   */
  skidCooldown = 3,
  /**
   * MO3 — **연속으로 같은 방향으로 달린 틱 수**. 방향이 바뀌거나 정지하면 0 으로 되돌아간다.
   *
   * `Carry` 가 아니라 `Stage` 인 이유: 관성은 저금이 아니라 *지금 달리고 있는 상태*라
   * 새 구간은 정지에서 시작하는 것이 옳다(`cadenceHits` 와 같은 사유).
   */
  momentumTicks = 4,
  /**
   * MO3 — **직전 틱의 양자화 이동 방향 코드**(1..9 · 5 = 정지). 0 = 아직 관측 없음
   * (값 규약 1 — 코드 0 은 `momentumDirCode` 가 낼 수 없는 값이라 자연 센티넬이다).
   *
   * 방향을 각도가 아니라 부호 3×3 격자로 양자화하는 이유는 `skills/bruiser.ts` 의
   * `momentumDirCode` doc 에 있다.
   */
  momentumDir = 5,
  /**
   * BL1「응전 사출」 — **반격 볼리의 내부 쿨다운 잔여 틱**. 0 = 발사 가능(값 규약 1).
   *
   * ⚠️ **칼날 축(BL) 상태 예산 2→3 개정으로 신설된 칸이다**(이 레인, 2026-08-07 사용자
   * 승인 — 사유는 `prerequisites.md` §0-A 개정 각주). 설계서 처방(`aux1 < 60` 내부 쿨)이
   * 성립하지 않는다: `aux1` 은 엔진 소유의 장갑 감쇠 타이머라 피격 시점에 이미 0 으로
   * 리셋돼 있어 술어가 항상 참이다 — 즉 그 술어로는 쿨다운이 원리적으로 걸리지 않는다.
   * 그래서 전용 칸이 불가피했다.
   *
   * `Carry` 가 아니라 `Stage` 인 이유는 `skidCooldown` 과 같다 — 반격 쿨다운은 짧아
   * 의뢰 구간 경계에서 초기화돼도 억제가 새지 않는다.
   */
  retortCooldown = 6,
}

/**
 * **해츨링 이월 슬롯**(ADR-0049 배치 5) — 이 배치가 배선한 9종은 **한 칸도 쓰지 않는다.**
 *
 * 설계서가 `구현: B`(신규 상태)로 표시한 해츨링 3종은 BD2(출격 카운터)·BD4(표적 id·명중 틱)·
 * NU10(저금)인데, 셋 다 이 배치의 미배선 목록에 있다(효과 지점이 `stepHatchBrood` 안 또는
 * 앵커 ⑩ 의 사후 시점 — 사유는 `skills/hatchling.ts` 헤더). 그중 **NU10 저금만이 `Carry` 후보**
 * 다(만석 보류가 의뢰 구간을 넘어 이어질 수 있다) — 그 레인이 자기 번호를 여기에 선언한다.
 * **팬텀 이월 슬롯**(ADR-0049 배치 4). 효과 본체는 `src/sim/skills/phantom.ts`.
 *
 * ⚠️ 팬텀의 `player.aux0`/`aux1` 은 **시그니처(은신)가 점유**한다(aux0 = 연속 무피격 틱 ·
 * aux1 = 해제 첫 타 배율 토큰). 스킬 상태를 그 두 칸에 얹지 마라 — 사이클과 조용히 갈린다.
 */
export const enum PhantomCarry {
  /**
   * DI5「최후 위상」의 **내부 쿨다운 잔여 틱**. 0 = 발동 준비됨(값 규약 1 — 0 이 자연 센티넬).
   *
   * `Stage` 가 아니라 `Carry` 인 이유는 아크캐스터 `backflowCooldown` 과 같다: 쿨다운이 Lv1 에서
   * 3484틱(약 58초)이라 의뢰 구간 길이를 우습게 넘는다. 구간마다 리셋하면 "구간 전환 = 쿨다운
   * 초기화"가 되어 설계서가 이 스킬의 유일한 억제로 명시한 내부 쿨다운이 사실상 사라진다.
   */
  lastPhaseCooldown = 0,
  /**
   * AS8「처형인의 적공」의 **아직 소비되지 않은 처치 스택**. 0 = 빈 상태(값 규약 1).
   *
   * `Stage` 가 아니라 `Carry` 인 이유: 설계 문면이 *"처치가 스택으로 쌓여 **다음** 은신 해제
   * 첫 타 배율에 가산된다"* 라, 쌓는 사건(처치)과 쓰는 사건(해제 첫 타) 사이에 구간 전환이
   * 끼는 것이 정상이다. 구간마다 리셋하면 "구간 끝에서 쌓은 스택이 조용히 사라지는" 형태가
   * 되고, 그 손실은 화면에도 테스트에도 흔적을 안 남긴다.
   *
   * ⚠️ 상한은 슬롯이 아니라 효과 본체(`skills/phantom.ts` 의 `TALLY_STACK_CAP`)가 건다 —
   * 슬롯은 값의 의미만 정하고 밸런스 상수를 이중으로 적지 않는다.
   */
  executionerStacks = 1,
}

/**
 * **팬텀 구간 슬롯 안내**(⚠️ 이 doc 은 병렬 머지에서 한 칸 밀려 아래 `HatchlingCarry` 에
 * 붙어 있다 — 실제 선언은 파일 뒤쪽 {@link PhantomStage} 다. 사실만 갱신하고 자리 이동은
 * 별 레인으로 미룬다: 여기서 옮기면 해츨링 레인의 doc 과 충돌한다).
 *
 * 설계서가 `구현: B`(신규 상태)로 표시한 팬텀 6종 중 배선된 것은 **DI5(위 `Carry`)와
 * PH6(`PhantomStage` 2칸)** 둘이다. 나머지 넷(AS7 원한 표적 정수 1 · AS8 적공 스택 1 ·
 * PH5 유지 창 config 파생 승격 · DI6 벽 접촉 플래그)은 앵커 밖이거나(AS7·AS8·PH5) 이미 엔진
 * 상태가 제공한다(DI6 = `state.wallContactTicks`).
 *
 * ⚠️ 지금 예약 번호를 미리 적지 마라 — 미사용 슬롯이 영구 0 으로 남는 것이 이 파일 헤더가
 * 폭을 8 로 좁힌 이유 자체다(오인덱스가 조용한 무연산이 된다).
 */
export const enum HatchlingCarry {
  /**
   * 자리표시자 — **읽지도 쓰지도 않는다**(`StrikerStage.unassigned` 선례). 실제 배정이 생기면
   * 이 줄을 **지우고** 0번부터 다시 매겨라.
   */
  unassigned = 0,
}

/**
 * **버블 이월 슬롯**(ADR-0049 배치 4). 효과 본체는 `src/sim/skills/bubble.ts`.
 */
export const enum BubbleCarry {
  /**
   * 자리표시자 — **읽지도 쓰지도 않는다.** 형태는 `StrikerStage.unassigned` 와 같다.
   *
   * 배치 4(버블)가 배선한 9종은 슬롯을 **한 칸도 쓰지 않는다** — 전부 기존 필드(`aux0`·
   * `aux1`·`iframes`·`dashCooldown`·`playerSlowTicks`)와 `state.tick` 파생만 만진다.
   * 설계서가 `구현: B`(신규 상태)로 표시한 버블 5종의 현행(배치6 기준):
   *  · PO10(창 틱 + kills 스냅샷 2칸) — `aux0 ≤ FILM_ABSORB_FLAT` 불변식 개정이 선결. 밖.
   *  · DR2(효율 창 1칸) — 밖. **`Stage` 로 옮길 수 있으나 이 배치가 금지한 축**이다
   *    (`skills/bubble.ts` 의 DR2 주석이 정본 — D 단계 골든 재동결과 한 커밋으로 묶는다).
   *  · FI6(흡수 누적 1칸) — 밖. 사유는 슬롯이 아니라 *"불멸 막 지속 중"* 술어다.
   *  · **DR3 — 배치6 이 {@link BubbleStage.blinkMagnet} 로 배선했다**(이월이 아니라 구간).
   *  · FI7(벽 접촉 플래그) — 슬롯이 아니라 `state.wallContactTicks` 로 이미 있다. 배치6 배선.
   *
   * ⚠️ 실제 배정이 생기면 이 줄을 **지우고** 0번부터 다시 매겨라. 예약 번호를 미리 적지
   * 마라 — 미사용 슬롯이 영구 0 으로 남는 것이 이 파일 헤더가 폭을 8 로 좁힌 이유다.
   */
  unassigned = 0,
}

/**
 * **말로우 이월 슬롯**(ADR-0049 배치 4 · S2 확장). 효과 본체는 `src/sim/skills/mallow.ts`.
 *
 * 배치 4 시점에는 배정 0칸이었다 — 설계서가 `구현: B` 로 표시한 말로우 6종이 전부 정산 분기
 * 또는 지연 전환 분기를 요구했고 그 둘에 앵커가 없었기 때문이다. **S2 의 앵커 ⑳**
 * (`onCushionSettled`)이 정산 분기를 열면서 그중 SQ5·SQ8 둘이 살아났고, 그 둘만 칸을 잡는다.
 *
 * 나머지 넷도 칸을 안 잡는다. **ME5(분할 이월)는 S3 의 앵커 ㉕ 으로 배선됐지만 슬롯이
 * 필요 없었다** — 설계서 본체가 이월분을 *"그 사이 피격되면 신규 적립과 합산"* 으로 정의하고
 * 그 술어가 곧 완충 풀 `player.aux0` 의 의미라, 별도 칸을 잡으면 같은 값이 두 곳에 산다
 * (설계서 `구현: B` 표기는 앵커가 없던 시절의 상정이다). CU6(런당 표식)은 지연 전환 **산술에
 * 개입**해야 하는데 그 분기에 아직 앵커가 없고, ME9(벽 접촉 카운터)는 슬롯이 아니라
 * {@link WorldState.wallContactTicks} 가 이미 제공하며, CU10(지급 카운터)은 회당 상한만 있으면
 * 되는 것으로 실측돼 칸이 필요 없었다(`maxHp` 자체가 누적을 들고 있다).
 *
 * ⚠️ ME9「솜틀 요양」의 **연속 벽 접촉 카운터를 여기 잡지 마라.** 설계서 ⑥-4 가 그것을
 * 스트라이커 M5 와 **한 벌**로 못 박았고, S0 의 E5 가 이미 {@link WorldState.wallContactTicks}
 * 로 엔진 상태에 세워 두었다 — ME9 배선 레인은 그 값을 읽기만 하면 된다.
 */
export const enum MallowCarry {
  /**
   * SQ8「흉터 포문」의 **런 내 누적 선체행**(정산으로 hp 에서 실제로 깎인 양의 합). 앵커 ⑳ 이
   * 넘기는 `applied` 를 더하고, 앵커 ⑯ 이 볼리 피해 증폭 bp 로 읽는다. 0 = 아직 갚은 적 없음.
   *
   * `Stage` 가 아니라 `Carry` 인 이유는 스킬 본체 그 자체다 — *"런이 아프게 흘러갈수록 세진다"*
   * 는 **런 단위 이력**이라, 의뢰 다구간에서 구간마다 리셋되면 스킬 이름값이 사라진다.
   */
  scarApplied = 0,
  /**
   * CU6「파산 보호」의 **런당 소진 표식**(0 = 아직 안 씀 · 1 = 소진). 이 파일 헤더가 예고한
   * 그대로 `Stage` 가 아니라 `Carry` 다 — *"런당 1회"* 가 설계 문언이라 의뢰 다구간에서
   * 구간마다 되살아나면 목숨이 구간 수만큼 늘어난다.
   *
   * ⚠️ 생존 캡스톤(치명타 1회 무효)의 `player.targetX` 와 **별개 표식**이다(설계서 CU6 연계
   * 항). 같은 칸을 쓰면 둘 중 하나가 다른 하나의 소진에 조용히 딸려 죽는다.
   */
  bankruptcyUsed = 1,
}

/**
 * **해츨링 구간 슬롯**. 한 칸뿐이고 `Carry` 가 아니라 `Stage` 인 이유가 이 칸의 의미 자체다 —
 * 아래 스냅샷은 **그 구간의 `aux0` 진행 상태**이고, 의뢰 구간이 바뀌면 월드가 새로 서면서
 * `player.aux0` 도 0 에서 다시 시작한다. 승계하면 새 구간 첫 틱에 "감소"로 읽혀 유령 판정이 된다.
 */
export const enum HatchlingStage {
  /**
   * SH6 알막 — **직전 틱에 관측한 `player.aux0`**(부화 스냅샷). 출격 성공만이 `aux0` 를
   * **올리는**(`aux0 = state.kills`) 유일한 경로라, `cur > seen` 이 곧 "직전 틱에 출격했다"다.
   * 반대 방향(감소)은 액티브 6종의 `advanceHatch` 와 NU5 뿐이라 오검출이 없다.
   * 0 = 아직 관측 없음 — 런 시작 `aux0` 가 0 이라 자연 센티넬이다(값 규약 1).
   */
  launchAux0Seen = 0,
  /**
   * BD2 쌍둥이 부화 — **이번 구간의 출격 "사건" 누적 수**(기수가 아니다: 쌍둥이 출격도 1 이다).
   * `count % N === 0` 인 사건에서 2기가 나간다. 0 = 아직 한 번도 안 나갔다.
   *
   * ⚠️ **{@link launchAux0Seen} 을 재활용하지 마라.** SH6 은 앵커 ⑨(`stepShipSignature` 진입)
   * 에서 같은 틱에 그 칸을 먼저 읽고 덮는다 — 뒤에 도는 앵커 ㉓ 이 같은 칸을 보면 증가를
   * 영영 못 본다. 게다가 그 칸은 `sh6 >= 1` 일 때만 쓰이므로 SH6 미투자면 아예 안 선다.
   */
  twinLaunchCount = 1,
  /**
   * NU10 알 저금 — **저금 잔액**(만석 보류 중 쌓인 처치). 출격 틱에 `min(잔액, 임계−1)` 만큼
   * 다음 임계에 선납되고 그만큼만 줄어든다(설계 2R R5: 소각 없음 — 잔액 보존).
   */
  eggBank = 2,
  /**
   * NU10 — **직전 틱에 관측한 `state.kills`**. 적립원이 "보류 중 처치 델타"라(설계 구현란)
   * 델타를 내려면 직전 값이 필요하다. 0 = 아직 관측 없음(런 시작 `kills` 가 0 이라 자연 센티넬).
   */
  eggBankKillsSeen = 3,
  /**
   * NU4 둥지 소집 — **연사 창 잔여 틱**. nurture 액티브 발동 틱에 핸들러가 세우고,
   * 앵커 ⑨(`stepShipSignature` 진입 · `stepTurrets` 보다 **앞**)가 매 틱 1 씩 깎으며,
   * 앵커 ㉗(`onTurretCadence`)이 0 보다 큰 동안 병아리 발사 간격을 깎는다.
   *
   * ⚠️ **세 지점의 순서가 곧 계약이다** — 발동(world.ts:1833) → ⑨ 감산(1874) → ㉗ 소비(1917)
   * 가 전부 같은 틱 안이라, 세운 그 틱부터 창이 실제로 열린다. 감산을 ⑨ 이 아니라 포탑 루프
   * 안에 두면 병아리 **기당** 한 번씩 깎여 창이 대수에 반비례해 짧아진다.
   */
  nestRecallTicks = 4,
  /**
   * BD4「표적 공유 증폭」 — **플레이어 자동조준이 이번에 고른 표적의 `Entity.id` + 1**.
   * `0` = 표적 없음. ⚠️ **+1 이 인코딩의 전부다**(`StrikerStage.lockTarget` 과 같은 관용구 —
   * 플레이어가 `state.entities[0]` 이라 id 0 이 날값으로 "표적 없음"과 구분 안 된다).
   *
   * `Carry` 가 아니라 `Stage` 인 이유: 표적은 *지금 조준 중인 대상*이라 구간을 넘겨 살
   * 이유가 없다(`StrikerStage.lockTarget` 과 동일 사유) — 새 구간 첫 판정이 이전 구간에서
   * 죽은 개체의 id 를 물려받는 사고를 막는다.
   */
  shareTargetId = 5,
  /**
   * BD4 — **플레이어 탄이 {@link shareTargetId} 표적에 마지막으로 명중한 `state.tick`**.
   * 0 = 아직 명중 없음(런 시작 `tick` 이 0 이 아니게 시작할 수 있으므로 엄밀한 센티넬은
   * 아니나, `shareTargetId` 가 0(표적 없음)일 때는 이 칸을 읽지 않는 것이 계약이다).
   *
   * `shareTargetId` 와 생명주기가 같아 `Stage` 다.
   */
  shareHitTick = 6,
}

/**
 * **말로우 구간 슬롯** — {@link MallowCarry} 와 같은 이유로 **미배정**이다.
 *
 * ⚠️ `Carry` 와 `Stage` 를 한 enum 에 섞지 마라(이 파일 헤더). 말로우에서 그 구분이 특히 날카롭다:
 * CU6「파산 보호」의 런당 1회 표식은 **구간을 넘어 살아야** 하고(`Carry`), SQ5「탕감 장전」의
 * 잔량은 구간마다 새로 시작해야 한다(`Stage`). 두 벌을 미리 갈라 둔 것은 나중 레인이 그 판단을
 * 다시 하지 않게 하기 위함이다.
 */
export const enum MallowStage {
  /**
   * SQ5「탕감 장전」의 **장전 잔량**. 정산에서 탕감된 양(앵커 ⑳ 의 `recovered`)의 일부가 여기
   * 쌓이고, 볼리마다(앵커 ⑯) 잔량의 25% 가 추가 피해로 소진된다. 0 = 빈 탄창.
   *
   * `Carry` 가 아니라 `Stage` 인 이유는 이 파일 헤더가 예고한 그대로다 — 장전은 **이번 구간의
   * 전투 리듬**이고, 구간을 넘겨 살면 새 구간 첫 볼리가 이전 구간의 탕감으로 굵어진다
   * (아크캐스터 `killCapacitorCharge` 와 같은 사유).
   */
  forgivenessLoad = 0,
  /**
   * CU6「파산 보호」가 **이번 피격에 요구한 무적 틱**(0 = 요구 없음). 앵커 ㉗ 에서 세우고
   * 앵커 ④ 에서 `max` 로 소비한 뒤 0 으로 비운다.
   *
   * ## ⚠️ 왜 ㉗ 에서 `player.iframes` 를 직접 못 세우는가
   * ㉗ 바로 뒤에 `world.ts` 가 `player.iframes = state.config.hitIframes` 를 **대입**한다.
   * 거기서 세운 값은 예외 없이 덮인다 — 설계서 CU6 이 *"하한을 안 걸면 Lv1~3 의 무적이 통상
   * 피격 무적보다 짧아 효과가 통째로 무효가 된다"* 고 경고한 바로 그 형태의 조용한 미발현이
   * 되고, 그것도 **전 레벨에서** 그렇게 된다. 그래서 요구를 한 틱 미뤄 ④ 에서 집행한다.
   */
  bankruptcyIframes = 1,
  /**
   * SQ10「만기 일제」의 **만기 표식**(0 = 일반 정산 · 1 = 직전 만기 정산). 방어 액티브
   * cushion_hi(전량 유예)의 EXPIRE 훅이 세우고, 다음 정산(앵커 ⑳)이 소비하며 0 으로 비운다.
   *
   * ## ⚠️ 1틱 타이머로 두지 않는다
   * EXPIRE 는 `aux1 = CUSHION_RECOVER_TICKS` 를 주입해 **다음 시그니처 스텝의 정산을 예약**
   * 한다. 그 스텝이 같은 틱인지 다음 틱인지는 두 시스템의 호출 순서에 달렸고, 그 순서에
   * 스킬 발동 여부를 걸면 배선이 조용히 반쪽이 된다. 표식은 **소비될 때만** 지운다.
   */
  maturityDue = 2,
}
export const enum PhantomStage {
  /**
   * PH6「정지된 시계」 — **대시가 예약한 정지 1틱**(0 = 없음 · 1 = 다음 시그니처 스텝에서 소비).
   *
   * 왜 예약이 필요한가: 앵커 ⑨(`stepShipSignature` 진입점)는 팬텀 적립(`aux0++`)보다 **앞**이고
   * 앵커 ②(대시)는 그보다 **뒤**다(world.ts 1874 vs 2250). 즉 대시 틱에는 그 틱의 증가가 이미
   * 끝나 있어 막을 수 없다 — 그래서 대시가 플래그만 세우고 **다음 틱의 앵커 ⑨** 가 정지를
   * 집행한다. 한 틱 밀리지만 창당 정지 **총량**은 예산과 정확히 같다(DI6 의 "직전 틱 vs 이번
   * 틱" 과 같은 형태의 등가 교환).
   */
  frozenClockPending = 0,
  /**
   * PH6 — **이번 은신 창에서 이미 쓴 정지 틱 수**. 예산은
   * `min(12 + floor(2.4×Lv), CLOAK_HOLD_TICKS/2)` 이고 소진되면 대시가 정지를 못 산다.
   *
   * `Carry` 가 아니라 `Stage` 인 이유: 예산은 **은신 창 하나**의 수명을 넘지 않는다(진입 에지
   * `phantomCloakEntry` 가 0 으로 되돌린다). 구간을 넘겨 살 이유가 없고, 살면 "구간 전환 =
   * 예산 이월"이 되어 설계서가 심각-2 로 유계화한 상한이 새어 나간다.
   */
  frozenClockUsed = 1,
  /**
   * PH5「연장 위상」 — **이번 은신 창에서 이미 쓴 연장 틱 수**. 예산은
   * `floor(CLOAK_HOLD_TICKS × Lv / 20)` 이고 소진되면 창이 정상 종료된다.
   *
   * `frozenClockUsed` 와 같은 사유로 `Stage` 다 — 예산의 수명이 은신 창 하나이고, 진입 에지
   * (`phantomCloakEntry`)가 0 으로 되돌린다. 구간을 넘겨 살면 "구간 전환 = 창 무한 연장" 이
   * 된다.
   */
  extendedHoldUsed = 2,
  /**
   * AS1「이중 각인」 — **해제 첫 타 다음 볼리에 후속 배율을 실을 예약**(0 = 없음 · 1 = 대기).
   *
   * 예약이 필요한 이유는 PH6 과 같은 결의 시점 문제다: 앵커 ⑯ 은 한 볼리의 파라미터만 보므로
   * *"직전 볼리가 해제 첫 타였는가"* 를 그 자리에서 알 방법이 없다. 그래서 첫 타 볼리가
   * 플래그를 세우고 **다음 볼리**가 소비한다.
   *
   * `Carry` 가 아니라 `Stage` 인 이유: 예약의 수명은 볼리 한 번(수 틱)이고, 구간을 넘겨 살면
   * 새 구간 첫 발이 이유 없이 후속 배율을 받는다.
   */
  twinMarkPending = 3,
  /**
   * AS7「원한」의 **원한 표적 `Entity.id`**. 0 = 표적 없음(값 규약 1 — 엔티티 id 는 1부터라
   * 자연 센티넬).
   *
   * `Carry` 가 아니라 `Stage` 인 이유: 원한 표적은 *이번 은신 사이클이 고른 대상*이라
   * `lockTarget`(`StrikerStage`)과 같은 결이다 — 구간을 넘겨 살면 새 구간 첫 표적 판정이
   * 이전 구간에서 이미 죽었을 개체의 id 를 우연히 물려받을 수 있다(id 는 구간마다 0 부터
   * 다시 세지 않는다는 보장이 없다).
   */
  grudgeTargetId = 4,
}

/**
 * **버블 구간 슬롯** — 배치6 이 DR3 으로 **1칸을 처음 잡았다**(그래서 `unassigned`
 * 자리표시자를 지웠다). {@link BubbleCarry} 는 여전히 실배정 0칸이다.
 */
export const enum BubbleStage {
  /**
   * DR3「도약 자기장」 — **전용 자석 확장 버프의 잔여 틱**. 0 = 버프 없음(값 규약 1 —
   * 0 이 자연 센티넬이라 미투자 런은 런 끝까지 0 이고 해시 폴드가 한 번도 안 돈다).
   *
   * ## ⚠️ 왜 신규 `WorldState` 필드가 아니라 이 칸인가 — **해시 꼬리를 안 건드린다**
   * 배치5 버블 레인은 DR3 을 *"신규 `WorldState` 정수 + 해시 꼬리 폴드가 선결"* 로 판정하고
   * 닫았는데, 그 판정이 놓친 갈래가 이 배열이다. `hashWorld` 의 스킬 슬롯 폴드는 **16칸이
   * 전부 0 이면 통째로 건너뛴다**(`replay.ts` 의 `skillSlotAny`). 그래서 이 칸을 쓰는 것은
   * *DR3 을 찍은 버블 런의 해시만* 갈리게 하고, 그 밖의 모든 런(골든 픽스처 포함)은 바이트가
   * 그대로다. 신규 필드는 **항상 0 이어도** 꼬리 폴드에 들어가 전량을 갈아엎는다 — 그 차이가
   * 이 스킬의 가부를 갈랐다.
   *
   * ## ⚠️ 배치5 가 기각한 세 대안은 지금도 기각이다
   * `state.magnetBuffTicks` 재사용(설계서 명시 금지 — 희귀 기믹 픽업의 보상 칸) ·
   * `state.activeBuff0/1` 재사용(`observable: 'buffTicks'` 계측 오염) · 쿨다운 역산(세운
   * 최댓값이 어디에도 안 남는다). 사유 전문은 `skills/bubble.ts` 의 DR3 블록 주석이 정본이다.
   *
   * `Carry` 가 아니라 `Stage` 인 이유: 지속이 Lv20 에서도 210틱(3.5초)이라 의뢰 구간 길이
   * 안에서 스스로 끝난다. 구간을 넘겨 살면 새 구간 첫 틱이 **지난 구간의 도약으로 켜진
   * 자석**을 물려받는데, 그건 액티브 착지가 트리거라는 술어 자체와 어긋난다.
   */
  blinkMagnet = 0,
  /**
   * DR2「표면장력 세례」 — **막 흡수 효율 창의 잔여 틱**. 0 = 창 없음(값 규약 1).
   *
   * ## ⚠️ 배치5·배치6 이 이 스킬을 두 번 닫았고, 두 사유 다 같은 갈래를 놓쳤다
   * 배치5 는 *"신규 `WorldState` 정수 + 해시 꼬리 폴드가 선결"*, 배치6 버블 레인은
   * *"설계서가 「구현: B」로 못 박은 신규 `WorldState` 정수 1개를 요구한다"* 로 닫았다.
   * 그런데 설계서의 `구현: B`(신규 상태)를 담는 자리가 **바로 이 배열**이고(이 파일 헤더의
   * 「채우는 규약」), 그 폴드는 16칸이 전부 0 이면 통째로 건너뛴다(`replay.ts` 의
   * `skillSlotAny`). 같은 배치의 DR3 이 그것을 실증한 뒤에도 DR2 의 사유만 갱신되지 않았다.
   * ⇒ **「신규 상태가 필요하다」는 「해시가 갈린다」와 같은 말이 아니다.**
   *
   * `Carry` 가 아니라 `Stage` 인 이유는 DR3 과 같다 — 창이 스스로 끝나고(수 초), 구간을
   * 넘겨 살면 새 구간 첫 틱이 지난 구간의 수거로 켜진 효율을 물려받는다.
   */
  tensionWindow = 1,
  /**
   * FI6「불멸의 막」의 **막 지속 중 흡수 누적**. 막이 활성인 동안 실제로 막아낸 피해량을
   * 이 칸에 더하고, 막이 꺼지면(자연 만료·조기 소비 불문) 0 으로 비운다. 0 = 막 비활성
   * 또는 흡수 없음(값 규약 1).
   *
   * `Carry` 가 아니라 `Stage` 인 이유: 이 누적은 *이번 막 한 장*의 이력이라 구간을 넘겨
   * 살 이유가 없다 — 새 구간 첫 막이 지난 구간 막의 흡수량을 물려받으면 막 하나의 성능이
   * 부풀어 설계서의 "막 1장당" 전제가 깨진다.
   */
  offeringPool = 2,
  /**
   * PO10「연쇄 파열」의 **창 잔여 틱**. 0 = 창 없음(값 규약 1).
   *
   * `Carry` 가 아니라 `Stage` 인 이유: 창은 발동 시점의 순간 버프라 초 단위로 스스로
   * 끝나고, 구간을 넘겨 살면 새 구간 첫 틱이 지난 구간에서 연 창을 물려받는다.
   */
  chainWindow = 3,
  /**
   * PO10 — **창 시작 시점의 `state.kills` 스냅샷**. 창이 열리는 순간의 처치 수를 찍어 두고,
   * 창이 끝날 때 `state.kills − chainKillsSnap` 으로 창 안에서 실제로 늘어난 처치 수를
   * 판정한다. 0 = 아직 관측 없음(런 시작 `kills` 가 0 이라 자연 센티넬 — 값 규약 1).
   *
   * `chainWindow` 과 생명주기가 같아 `Stage` 다 — 창 하나의 스냅샷이 다음 구간까지 살아남을
   * 이유가 없다.
   */
  chainKillsSnap = 4,
}
