/**
 * 설계도 지급 축의 **서버 계약 상수** — SQL 미러.
 *
 * `commissionServerConstants.ts` 와 같은 성격이다: 같은 수치가 SQL 본문과 여기 두 곳에 살고,
 * 한쪽만 고치면 **런타임에 아무 신호가 없다**(서버는 자기 값으로 거절하고 클라는 자기 값으로
 * 화면을 그린다). `tests/blueprintGrantCap.test.ts` 가 두 쪽을 대조한다.
 *
 * ⚠️ 이 값들은 **클라 런타임 판정에 쓰이지 않는다.** 클라는 캡을 예측하지 않고 그냥 부른다
 * (`grantBlueprintDrops` 는 fire-and-forget). 여기 두는 이유는 ①드리프트 검출 ②캡 유도를
 * 코드 근처에 남기는 것 두 가지다. 클라가 캡을 미리 계산해 호출을 아끼려 들면 **분모가 클라
 * 통제 밖이라는 성질**을 잃는다 — 그러지 마라.
 */

/**
 * 프로필당 **시간당 설계도 적립 상한**(총 장수, 호출 수가 아니다).
 *
 * ## 유도
 * 정직한 분모는 서버가 강제하는 런 수다. 축 D 가 시간당 런을 60 으로 묶고
 * (`CAP_RUNS_PER_HOUR`, `20260808000000_pve_run_registration.sql`), 설계도는 클리어당 3% ·
 * 최대 1장이다(`BLUEPRINT_RUN_CHANCE_CP`, `src/sim/drops.ts`).
 *
 *   시간당 기대 = 60 × 0.03 = **1.8장**. Poisson(1.8) 에서 P(X ≥ 12) ≈ 1.5e-6 (약 5.5σ).
 *
 * 실측 런 길이가 2~5분이라 시간당 12~30런이 전형이고 60 은 캡이지 전형값이 아니다 —
 * 위 계산은 가장 관대한 쪽이다. 즉 이 상한은 **정직한 플레이를 벌하지 않는다.**
 *
 * SQL 미러: `CAP_BLUEPRINTS_PER_HOUR` (`20260808080000_blueprint_grant_cap.sql`).
 */
export const CAP_BLUEPRINTS_PER_HOUR = 12;

/**
 * 프로필당 **하루 설계도 적립 상한**(총 장수).
 *
 * ⚠️ **실질 구속은 이쪽이다.** 시간 캡만 두면 참을성 있는 공격자가 하루 288장(12 × 24)까지
 * 쓸 수 있어 방어가 반쪽이 된다.
 *
 * ## 유도
 * 헤비 유저를 가장 관대하게 가정해 하루 16시간 × 60런 = 960런 → 기대 28.8장(sd 5.3).
 * 60 은 약 5.9σ 라 이 경우에도 벌하지 않는다.
 *
 * SQL 미러: `CAP_BLUEPRINTS_PER_DAY` (`20260808080000_blueprint_grant_cap.sql`).
 */
export const CAP_BLUEPRINTS_PER_DAY = 60;

/**
 * 이 캡이 왜 생겼는지 — 한 문장으로 남긴다(값만 보면 임의값처럼 읽힌다).
 *
 * ADR-0026 의 수용 기준은 절대 기준이 아니라 **상대** 기준이다("치터의 이득이 최상위 정직
 * 유저의 파밍 속도로 유계된다"). 2026-08-08 확률 레인이 정직한 획득률을 런당 약 7~14% →
 * 정확히 3% 로 약 5배 낮추는 동안 `grant_blueprints` 의 위조 천장은 **무제한**이었다 —
 * 비율 기준이 5배 벌어진 것이고, 보안 리뷰가 그것을 적발했다. 이 상수 둘이 그 천장을
 * 시간당 12 · 하루 60 으로 닫는다.
 */
export const BLUEPRINT_CAP_RATIONALE =
  'ADR-0026 relative-bound: honest 3%/clear under axis-D 60 runs/h ⇒ ~1.8/h expected';
