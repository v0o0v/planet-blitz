# Planet Blitz — 작업 규약

## 검증 3단 — 언제 무엇을 돌리는가

전체 스위트를 편집 한 건마다 돌리지 않는다. 실측(2026-08-04, 16코어)으로 비용 차이가 **34배**다.

| 시점 | 명령 | 실측 벽시계 |
|---|---|---|
| **편집 중** | `npx vitest run tests/<고친 것의 짝>.test.ts` | 약 7초 |
| **커밋 전** | `pnpm test:changed` (= `vitest run --changed origin/main`) | 약 34초 |
| **PR 올리기 전** | `pnpm verify` (= 전체 `vitest run` + `tsc --noEmit` + `eslint`) | 약 1분 30초 |
| **`src/sim/**` 을 만졌으면 추가로** | `pnpm test:sim` (골든·결정론 3파일) | 약 7분 25초 |

(2026-08-06 실측, 16코어: 전체 스위트 **55초**(311파일 6707건) + `tsc` 14초 + `eslint` 20초.
스위트는 241초 → 113초 → **55초** 로 두 번 줄었다. 마지막 절반은 ADR-0051 이 봇 계측·완주
e2e 를 게이트에서 내리고 배선 증명을 최소 틱으로 다시 쓴 결과다 — 근거는 `vite.config.ts` 의
`SIM_LANE_FILES` 주석.)

- **편집 중 직접 지정이 가장 싸다.** `--changed` 는 34초 중 **약 29초가 그래프 빌드 고정비**라, 파일을
  아는 상황에서는 지정이 5배 빠르다.
- **커밋 전 `test:changed` 는 기준점이 `origin/main`** 이다. 기본값(미커밋 변경만)으로 두면 레인에
  이미 쌓인 커밋의 회귀가 PR 직전까지 안 보인다.
- **`--changed` 는 임포트 그래프만 본다.** 그래서 놓치는 축이 있다 — `data/**` 나 `assets/**` 를
  파일시스템으로 훑는 자산 존재 검사(`*AssetPresence.test.ts`), 골든 fixture, 자산 추가/삭제.
  자산·데이터를 건드렸으면 그 테스트를 **직접 지정해서** 같이 돌려라. 최종 안전망은 `pnpm verify` 다.
- **`pnpm verify` 를 파이프에 물리지 마라** (`| tail` 금지). exit code 가 `tail` 것이 되어 거짓 그린이 된다.

## 기본 스위트는 main 에서 초록이다

**2026-08-06(ADR-0051)부터 `pnpm test` 는 main 에서 전량 초록이다.** 그 전까지 12건이 상시
빨갛던 절이 여기 있었고, 그 12건은 전부 **봇 완주 e2e** 였다 — 피격 피해가 2배가 되면서
(`PLAYER_DAMAGE_TAKEN_MULT`) 무입력 스크립트 파일럿이 런을 완주하지 못했고, 드랍·XP·침공
배선을 재려던 파일들이 잴 것을 아예 못 재고 있었다.

ADR-0051 이 그 축을 게이트에서 내렸다(단언이 "봇이 이길 수 있는가"에 의존하면 내린다). 대신
**배선 증명은 내구 파일럿 + 최소 틱으로 다시 써서 살렸다** — 삭제로 도망친 게 아니다.

**빨간 것이 보이면 그건 네 것이다.** `git stash` 대조 절차는 이제 필요 없다.

⚠️ 단 **`pnpm test` 그린이 골든 그린을 뜻하지는 않는다** — 아래 sim 레인을 보라.

## sim 레인은 따로 돈다 — **결정론 골든 전용**

`pnpm test` 는 골든·결정론 파일 **3개**를 제외한다(`vite.config.ts` 의 `SIM_LANE_FILES`).
`pnpm test:sim` 으로 따로 돈다. 실측 **445초(7분 25초)** 이고 그중 `denoFixture` 혼자 443.7초다.

**돌려야 하는 때: `src/sim/**` 을 건드려 해시·거동이 갈릴 수 있을 때.** 밸런스 수치를 만졌을
때도 같다. `src/sim/**` 을 만진 PR 은 **올리기 전에 반드시 한 번** 돌려라 — 기본 스위트는
골든 발산을 원리적으로 못 잡는다.

⚠️ **골든 재생성은 검증이 아니다.** 다시 뜬 골든으로 그 골든을 검사하면 항진이다. 근거는
재생성 **전에** 통과시킨 불변식이어야 한다.

## 계측은 게이트가 아니라 CLI 다

통계 계측(클리어율·구간틱·밴드 균형)은 `tests/` 글롭 **밖**의 `bench/` 로 나갔다 — vitest
`include` 가 `tests/**` 라 **구조적으로 수집 불가**다(ADR-0051). 손으로 돌린다:

```
pnpm bench:curve       # 행성×레벨 곡선 — clearRate·clearSec·bossReachRate·xpTotal·lootCount
pnpm bench:invasion    # 침공 밴드
pnpm bench:commission  # 의뢰 밴드
```

축소는 `SEEDS=3 pnpm bench:curve`, 인자는 `pnpm bench:curve -- --levels=10,40`.

**밸런스 회귀는 이제 스위트가 자동으로 안 잡는다.** 이것은 손실이 아니라 현상 인정이다 —
그 게이트는 이미 빨간 채 방치돼 작동하지 않고 있었다. 밸런스는 출시 후 텔레메트리가 본선이고,
출시 직전에 위 CLI 로 한 번 훑는다.

## 계획 문서의 "매 커밋 pnpm test 전체" 는 낡았다

`.omc/plans/**` 다수에 그 문구가 있다. 과거 레인의 기록이므로 고치지 않고 두되, **새 레인이 복사하지
마라.** 이 파일의 3단이 현행이다.
